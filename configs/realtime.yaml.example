# Parcel — hosted Realtime companion lane (cards R1 … R1.6/R3).
#
# COPY THIS FILE TO configs/realtime.yaml TO SATISFY THE DEFAULT LAUNCHER.
#
# The real configs/realtime.yaml is deliberately NOT in the repository. The
# production launcher nevertheless requires it and a credential; absence makes
# `scripts/launch_stack.sh` refuse before starting any service. Only an explicit
# `--legacy` launch selects the local cascade. Every key below is validated on
# load — an unknown key, a wrong type, a non-positive number or an unknown mode
# is a REFUSAL, never a silent default (a typo'd `enabled: ture` reading as false
# would be a bad day; a typo'd budget reading as "unlimited" would be worse).
#
# THE CREDENTIAL IS NOT IN THIS FILE AND NEVER WILL BE.
# It lives outside the repository, mode 600, and is loaded into the environment
# before the process starts:
#
#     install -m 600 /dev/null ~/.config/parcel/realtime.env
#     printf 'OPENAI_API_KEY=sk-...\n' > ~/.config/parcel/realtime.env
#     set -a; . ~/.config/parcel/realtime.env; set +a
#
# scripts/launch_stack.sh sources that file and refuses loudly if the key is
# missing. `--realtime` remains an accepted no-op because Realtime is already
# the default.

# Consent to the FEATURE. Not consent to a session: the lane still needs the
# panel's handshake token and an explicit owner gesture before it opens one.
enabled: true

# PERSONALITY IS JUST PROMPT TEXT (owner directive, 2026-08-18).
# Write who the robot is, in plain prose. It is handed to the model verbatim in
# place of the personality-profile block — no YAML profile to author, no file to
# add. Everything that is NOT personality rides along regardless: the companion
# preamble, the voice guardrails ("never claim to have arrived anywhere"), and
# the developer-note contract. A persona cannot prompt those away.
#
# Editing this line changes the session's SI digest, which is recorded per
# session and in every corpus fixture — so a transcript is always attributable
# to the exact words that produced it. That is the whole discipline; there is no
# version to bump for free text.
#
# Delete the key entirely to fall back to a preset profile (see si_profile);
# leaving it BLANK is a refusal, not a fallback.
persona: "You are a lively conversational agent that likes to go around New York."

# Preset personality id from prompts/personalities, used ONLY when `persona` is
# absent. Delete it to inherit the runtime's own personality setting.
# si_profile: gentle_companion

# text  — the manual-testing path: the panel's text box talks to the live
#         session, replies land in the chat pane and the ledger, no microphone,
#         no speaker, no audio gateway. It remains the cheapest setup default.
# audio — additionally starts the loopback audio gateway and serves the panel's
#         microphone button; the browser becomes both mouth and ear. This path
#         uses browser capture/playback and does not require Python PortAudio.
mode: text

# gpt-realtime-2.1-mini is the cheap one and is what every live run in the
# status docs used. gpt-realtime-2.1 is the full-size sibling.
model: gpt-realtime-2.1-mini

# Hosted voice id.
voice: cedar

# The watchdog: seconds of provider silence, while a response is expected,
# before the lane reconnects. Not a latency budget — a liveness bound.
stall_timeout_s: 8.0

# Hard session cap. The provider bills per session and the lane's memory tail
# is re-injected on every reconnect, so a rollover is invisible to the owner.
session_max_s: 3600.0

# HOW LONG A SESSION NOBODY IS TALKING TO STAYS OPEN (card R16).
#
# On 2026-08-20 a conversation ended at 05:39, nobody said anything else until
# 14:11, and the lane sat there the whole time renewing itself once an hour —
# seven `[session rollover]` rows between 06:23 and 12:23 with not one word
# between them, because a rollover renews whatever it finds and nothing in the
# product had an opinion about a conversation that had plainly ended.
#
# Past this many seconds of CONVERSATIONAL silence the lane hangs up: the
# socket closes, a
# `[idle hang-up after Ns]` row goes in the ledger, the driver stops pumping and
# — in `mode: audio` — the browser's microphone button goes back to "Enable
# microphone".
#
# Nothing is lost by hanging up. The conversation lives in the ledger, not in
# the provider, so the next thing you say opens a fresh session with the same
# memory tail replayed into it, exactly as a rollover always did.
#
# "Conversational" is the load-bearing word, and it means four things and no
# others: you typed, the provider's own voice-activity detector heard you start
# or finish speaking, the robot narrated a fact the model actually took, or a
# reply completed. An OPEN MICROPHONE IS NOT A CONVERSATION — an armed mic in an
# empty room streams audio frames (and bills for them) forever, which is the
# most expensive way there is to be idle, so frames alone will not hold the lane
# open. You will not be cut off mid-sentence: speech detection resets the clock
# at the first syllable, and a turn that is still waiting for its answer is
# never idle at all.
#
# ZERO MEANS NEVER (card P0-B). It used to be refused, on the reasoning that a
# silent off switch on a billing session is worse than a loud number — and the
# reasoning was right about silence and wrong about zero, which is neither
# silent nor a default: you write it by hand, /api/state echoes it, and the two
# bounds that keep an unattended session finite are untouched (`session_max_s`
# still rolls the socket over, `monthly_budget_usd` still stops the robot
# starting billed exchanges, and both still refuse zero). What it buys is a
# prototype companion that is still awake when you come back into the room.
#
# Negatives, non-numbers and `.inf` are still refusals.
idle_close_after_s: 600.0

# ---------------------------------------------------------------------------
# THE COMPANION UNLOCKS (card P0-B). All three are OFF in the values shown, and
# a config that omits them behaves exactly as it did before the card.

# MAY THE ROBOT MOVE WHEN NOBODY SPOKE TO IT?
#
# The lane tags every response with who asked for it. A response the ROBOT
# started — it narrated its own battery level and asked the model for a reply —
# may not move the body, because the 2026-08-19 bench fired a spurious
# `navigate_to("picnic spot by the big oak")` off a single injected telemetry
# item in 2 of 3 forced-response trials. That gate is why the dog cannot drive
# off because it told itself something. It is also why the companion cannot tilt
# its head when you walk in.
#
# This list is the hole in that gate, and only these two names may go in it:
#
#   play_gesture   a bounded emote from the catalog
#   set_pose       a bounded pose from the catalog
#
# `navigate_to`, `circle_owner` and `follow_owner` are REFUSED AT LOAD if you
# write them here — they commit the robot to travel, and a trip you never asked
# for is the defect the gate exists to prevent. Everything admitted still goes
# through the safety supervisor, the emergency latch and the activity
# coordinator's cooldown, exactly as your own request does.
#
# Empty (the default) means no proactive motion at all.
proactive_motion_tools: []

# WHAT `navigate_to` DOES WITH A PLACE THE MAP HAS NEVER HEARD OF.
#
#   refuse  the shipped behaviour: the name is handed to the router exactly as
#           a typed "go to narnia" is, and the refusal comes from grounding.
#           Authority parity with the keyboard, and a robot that sets off and
#           then gives up.
#   ask     the broker answers the model with a structured `unknown_place`
#           result naming the places it DOES know, touches no door and starts
#           no motion — so the companion asks you where that is, or offers to
#           go and look, instead of walking at a name it cannot ground.
#
# A place the robot knows is unaffected either way, and so is a junk directive
# ("with owner"), which is refused before this key is consulted.
unknown_place: refuse

# DOES "I'M FEELING SAD" REACH THE BODY ON THIS LANE?
#
# The deterministic explicit-affect grammar — the one that turns an explicit
# first-person feeling into your personality's `affect_actions` gesture, a
# comfort bow for sad and a paw wave for happy — has only ever been wired to the
# LEGACY voice path. Everything you say to the hosted companion arrives
# somewhere else, so on the lane that actually ships the sentence has always
# done nothing: the model says something kind and the body stays still.
#
# With this true, a hosted sentence that no local command claimed is checked for
# an explicit affect. Above `agent.affect.minimum_confidence` in
# configs/robot.yaml it writes an `[affect <label>]` note into the conversation
# ledger (a `system` row, so it does not appear in the chat pane as something
# the robot said) and PROPOSES the gesture to the activity coordinator, which
# keeps the timing, the cooldown and the e-stop. It never replies, never speaks
# and never touches postural recovery.
hosted_affect: false

# The arming gate refuses to open a session once this month's estimated spend
# reaches this number. Estimated at ASSUMED rates (realtime/cost.py) — it is a
# guard rail, not an invoice.
#
# Card R25 made that sentence TRUE. It was documented here from R1 and the
# arming gate never read it: the gate's comparison existed, nothing passed it a
# figure, and the ceiling compared 0.00 against this number every time for
# twenty-four cards. What changed:
#
#   * "this month" is UTC, measured from a durable append-only ledger at
#     <capture.dir>/spend.jsonl (default: recordings/spend.jsonl) — one priced
#     row per hosted response, so the ceiling survives a restart. Delete the
#     file to reset the month; move it with PARCEL_REALTIME_SPEND_LEDGER.
#   * The refusal names the figure, the period and this key. Raise the number
#     (or wait for the 1st, UTC) to open a session again.
#   * A session ALREADY OPEN is never hung up mid-sentence by the ceiling; what
#     the ceiling does to it is stop the ROBOT from starting billed exchanges
#     you did not ask for. Your own turns are never gated.
#   * SAFETY-class facts — the emergency latch and its clear, a refusal of your
#     own command, a mission terminal — are spoken PAST the ceiling, exactly as
#     they already bypass the whisperer's max_updates_per_minute below. A robot
#     that will not say "I have stopped" because of a money knob is the failure
#     the knob exists to prevent. A voice rejection is deliberately NOT in that
#     set: it is a fact about somebody else's request, and a talkative
#     television must not be able to spend past your ceiling.
#   * If the ledger cannot be READ, the lane arms anyway and says so loudly in
#     the panel. That is the one deliberately fail-OPEN control in this file: a
#     broken spend file must not brick the robot. Everything else here still
#     fails closed — including this key itself, which now refuses .inf as well
#     as zero, negatives and non-numbers.
#
# The figure is a LOWER BOUND: a response the provider billed whose completion
# frame never arrived is spend the ledger never saw.
monthly_budget_usd: 25.0

# ---------------------------------------------------------------------------
# THE SITUATIONAL WHISPERER (card R11) — the owner's cost knob.
#
# The robot notices things about itself: it arrived, it is waiting behind
# someone, its battery went low, you asked to run and you are walking. Each of
# those it decides to SAY becomes a `response.create` on the hosted session, and
# the 2026-08-19 bench measured gpt-realtime-2.1-mini speaking on essentially
# every injected state item (0-2 silences in ~20). So one forwarded fact is one
# spoken line and one billed response, and `max_updates_per_minute` is the cost
# control and the politeness control at the same time.
#
# THIS BLOCK ONLY GOVERNS WHAT THE ROBOT STARTS. Talking to it — the panel's
# text box, the microphone, every voice command — is a different door and is
# not affected by anything here.
#
# Delete the whole block to accept the defaults shown.
whisperer:
  # false = no state updates at all. The robot still answers you; it just never
  # starts a conversation about itself.
  enabled: true

  # THE KNOB. A hard cap on robot-initiated (billed) queries per rolling
  # minute. Anything held back is not thrown away — it is folded into the next
  # item that does go out as "+N more", so you can always see the knob working
  # (/api/state shows `updates_this_minute` and `folded`).
  #
  # Four facts BYPASS this cap, because delaying them failed the bench
  # disqualifyingly (judge-everything delayed an emergency stop by 9.8 s and
  # lost the resume-clear outright): an emergency stop and its release, a
  # mission terminal, a reroute, and a refusal of something YOU asked for.
  max_updates_per_minute: 2

  # Spacing inside the budget, in seconds. Terminal-like events are exempt —
  # the bench caught a reroute silently swallowed by a min-gap that a
  # "way is clear again" was holding.
  min_gap_s: 15.0

  # THE OTHER HALF OF THE CAP (card P0-B). The window `max_updates_per_minute`
  # is counted over, in seconds. It was a literal 60 inside the code, which made
  # the narration rate half a knob: you could set the number and not the minute,
  # so the only rates you could buy were whole multiples of one-per-minute. 60.0
  # is the same minute the cap has always meant.
  #
  # Halve it for a chattier prototype (2 per 30 s is 4/min at the same spacing);
  # raise it for a quieter robot without dropping the cap to 1. Zero is refused:
  # a window of nothing counts nothing and would remove the cap silently, and
  # `enabled: false` is the off switch that can be seen.
  window_s: 60.0

  # -------------------------------------------------------------------------
  # WHEN THE DOG NOTICES *YOU* (card P2-B) — DEFAULT OFF.
  #
  # Everything above this line is a fact about the ROBOT: it arrived, it is
  # waiting behind someone, its battery is low. That is why the dog never says
  # hello — there was no class of fact whose subject was you. These four are
  # that class:
  #
  #   owner_appeared        you came into view after being away
  #   owner_returned        the same thing after a LONG absence, said differently
  #   greeting_due          you are here and neither of you has said anything
  #   question_of_the_day   once a day, it asks you one thing about your day
  #
  # THEY BUY NOTHING. Every one of them is banded, deduplicated, min-gapped and
  # counted against `max_updates_per_minute` exactly like a mission terminal,
  # and none of them is in the critical set that may spend past your monthly
  # ceiling. This block decides when a greeting is DUE; the knobs above decide
  # whether a due greeting is affordable, and they win.
  #
  # It is default-off for one reason only: a forward is a billed hosted
  # response, and a config written before this card must keep costing what it
  # cost. Turn it on in configs/realtime.prototype.yaml.example.
  #
  # Delete the whole block to accept the defaults shown (i.e. off).
  owner_events:
    # The opt-in. While it is false the four classes are never PRODUCED — not
    # produced and suppressed, not produced and deduped. Never produced.
    enabled: false

    # How sure the owner track has to be before a sighting counts. The mocap /
    # UWB track reports 1.0; the camera track (card P1-C) reports a measured
    # similarity, and this is the number that stops a 0.2-confidence stranger
    # being greeted as you.
    min_confidence: 0.3

    # How long you must stay in view before a greeting fires. The anti-flicker
    # guard: a tracker that drops a frame must not buy a hello.
    appear_debounce_s: 2.0

    # How long you must have been AWAY for a new sighting to count as an
    # arrival at all. Below this it is the same visit and you have already been
    # greeted — the most important number here for anyone who has met a dog.
    absence_s: 60.0

    # Above this, an arrival is a RETURN and gets the other sentence.
    long_absence_h: 3.0

    # Silence, in seconds, after which a present owner is owed a greeting.
    # Counts both sides of the conversation, so the robot never interrupts a
    # conversation it is already having. 0 disables this class alone.
    greeting_interval_s: 900.0

    # The one question a day, asked while you are actually there. This is the
    # only thing the robot says whose purpose is to LEARN about you rather than
    # to report on itself. false disables this class alone.
    question_of_the_day: true

# ---------------------------------------------------------------------------
# SESSION AUDIO CAPTURE (card R17) — DEFAULT OFF, and off is the shipped answer.
#
# On 2026-08-20 the owner spoke a 52-query corpus to the live robot. It cost
# real money and produced the most informative eval this project has. The
# gateway moved 3,605 microphone frames up and 296 speech frames down and wrote
# NONE of it to disk, so every ASR failure in that run — a Korean TV sign-off
# attributed to the owner, the spoken emergency phrase transcribed as "Dice
# out!" and never matched, a code-switched sentence normalised away — is
# permanently unreproducible. The transcripts survived.
# The sound did not.
#
# With this block on, each gateway run writes:
#
#     <dir>/<session_id>/owner.wav   exactly the microphone audio the provider
#                                    heard (mono PCM16 at the session rate)
#     <dir>/<session_id>/robot.wav   exactly the hosted speech the robot was
#                                    asked to play, unwrapped from its
#                                    per-chunk RIFF into one continuous stream
#     <dir>/<session_id>/index.json  the tiling of both files into segments —
#                                    owner turns cut on silence, robot audio cut
#                                    on each hosted utterance — so a single turn
#                                    can be sliced out by byte range and dropped
#                                    into a corpus as a fixture. Times are
#                                    DERIVED from byte offsets, never measured,
#                                    so the index cannot drift from the audio.
#
# THIS RECORDS A HOUSEHOLD MICROPHONE TO DISK. That is why it is off unless you
# write it down here yourself, and why the folder is created lazily — a stack
# that boots with capture on and is never spoken to leaves nothing behind.
#
# The tee can never slow the conversation down or break it: frames are handed
# to a bounded queue and written by a background thread, a full queue drops and
# counts rather than waiting for the disk, and a tee that fails disables itself
# instead of raising into the audio relay.
#
# Delete the whole block for the defaults shown (i.e. off).
capture:
  # The opt-in. Nothing below has any effect while this is false.
  enabled: false

  # Where per-session folders are created. Relative paths resolve against the
  # repo root and NEVER against the current directory, so `recordings` means one
  # place regardless of where the stack was launched from.
  #
  # A directory inside `evals/` is REFUSED at config load: those are reviewed,
  # frozen fixtures that runs are scored against, and a live tee appending into
  # that tree could rewrite the record while it is being graded.
  dir: recordings

  # Hard bound on ONE session's recording, per stream. Reaching it stops
  # CAPTURE — it logs once, finalizes both WAVs and writes the index — and
  # leaves the conversation completely untouched. An audio tee is a
  # convenience; it is never a reason to end a session. Zero is refused, the
  # same way a whisperer cap of zero is: `enabled: false` is the off switch,
  # and it is visible.
  #
  # 30 minutes of 24 kHz mono PCM16 is ~86 MB per stream.
  max_minutes: 30.0

  # Silence between microphone frames that cuts a new OWNER segment in the
  # index. The robot half is delimited by the lane's own utterance boundaries;
  # the owner half has no such marker, so the index cuts on a gap. The default
  # is longer than any capture buffer and shorter than the pause between two
  # spoken corpus queries.
  owner_gap_s: 0.75

# ---------------------------------------------------------------------------
# SPEAKER IDENTITY FOR COMMAND ARMING (card F1-SI)
#
# THE DEFECT THIS BLOCK EXISTS BECAUSE OF. On 2026-08-20 a television commanded
# the robot, twice, across two owner sessions. Acoustic echo cancellation does
# not defend against that (it cancels the robot's OWN speaker; a TV is an
# independent source in the room) and the transcript-level Unicode-script check
# only catches the cross-LANGUAGE case. A same-language TV defeats every
# transcript-level layer there is, so the defence has to be about who is
# speaking.
#
# THE SAFETY ASYMMETRY, AND IT IS NOT CONFIGURABLE. **The emergency stop is
# never identity-gated.** Anyone in the room may stop the dog — a visitor, a
# child, a voice the robot has never heard. There is no key in this block that
# reaches the latch, by construction. What identity gates is ARMING: turning a
# sentence into motion.
#
# NOTHING HERE DOES ANYTHING UNTIL YOU ENROLL. With no enrolled profile the gate
# reports `verify_disabled`, says so in /api/state and in the event log, and the
# robot behaves exactly as it did before this card. Enrollment is one command,
# once, with your own voice:
#
#     tools/enroll_owner_voice.py --wav a.wav b.wav c.wav d.wav e.wav
#     tools/enroll_owner_voice.py --show      # where is it, is it sane
#
# It writes ONE json file at mode 0600 beside this config — outside the
# repository, like the credential. It holds an averaged voice embedding, not
# audio, and re-running it overwrites cleanly.
voice_identity:
  # The feature switch. `true` is the default and is inert without a profile.
  enabled: true

  # Cosine similarity at or above which a turn is you. Measured on THIS host's
  # own microphone array (scrum/20260820/research/bench_doa.md, Bench B): the
  # worst impostor pair scored 0.431 and the weakest genuine pair 0.640, with
  # zero overlap across 378 pairs, so 0.55 is the midpoint of the measured gap.
  #
  # Raise it to be stricter (more "I didn't recognise that voice" for you);
  # lower it to be more permissive. A value outside (0, 1] is REFUSED rather
  # than clamped — `threshold: 55` is the percentage somebody meant, and it
  # would lock you out of your own robot forever.
  threshold: 0.55

  # Where the enrolled profile lives. Empty = beside this file. Never put it in
  # the repository; the enroller refuses to.
  # profile: /home/you/.config/parcel/owner_voice_profile.json

  # The embedding model. Empty = the vendored models/speaker_id/ copy named by
  # models/speaker_id/models.lock.json.
  # model: /path/to/nemo_en_titanet_small.onnx

  # How much speech is buffered before the first (provisional) check of a turn.
  # Raised from 0.6 to 1.2 by measurement: at 0.6 s the false-reject rate over
  # this host's gold set was 38.5 %, because a fragment of a sentence is not the
  # sentence. The whole turn is checked again when you stop speaking.
  min_utterance_s: 1.2

  # Added-latency budget in milliseconds, MEASURED and reported in /api/state,
  # not enforced as a timeout — half an embedding is worse than a slow one.
  # Measured end to end on this host: median 18 ms, p95 33 ms per turn.
  budget_ms: 50.0

  # How often a refusal may become a SPOKEN sentence ("someone who isn't you
  # asked me to…"). Every refusal is counted and logged regardless; this only
  # stops a talkative television from becoming a talkative robot.
  narration_interval_s: 60.0

  # THE DIRECTION-OF-ARRIVAL PREFILTER, and it needs a udev rule you must grant.
  # The reSpeaker XVF3800 reports the azimuth a sound came from on a vendor USB
  # interface that is separate from the audio streams, so reading it cannot
  # disturb capture. Until the rule below exists every read fails with a
  # permission error and the prefilter contributes nothing (it never refuses on
  # its own absence).
  #
  #   sudo tee /etc/udev/rules.d/99-respeaker-xvf3800.rules <<< 'SUBSYSTEM=="usb", ATTRS{idVendor}=="2886", ATTRS{idProduct}=="001a", MODE="0660", GROUP="plugdev", TAG+="uaccess"'
  #   sudo udevadm control --reload && sudo udevadm trigger --action=change /sys/bus/usb/devices/3-1
  #
  # (A udev trigger re-applies node permissions; it does not reset the device.)
  #
  # --- AIR-1 device notes (card task_25) — BEGIN --------------------------
  # Measured on this host 2026-08-22 with `tools/xvf3800_probe.py`, which
  # prints all of the following and exits non-zero when any of it stops being
  # true:
  #
  # * `/dev/bus/usb/003/008` is root:root 0664 and this user is in `plugdev`,
  #   so the rule above is the whole difference between a working DoA
  #   prefilter and 100 % Errno 13. The sysfs path in the trigger line is this
  #   host's real one (`/sys/bus/usb/devices/3-1` = bus 3, device 8).
  # * The array is **16 kHz only, in both directions** — one rate in
  #   `/proc/asound/card2/stream0`, and PortAudio answers PaErrorCode -9997
  #   (Invalid sample rate) for 22 050, 24 000, 44 100 and 48 000 Hz on
  #   `hw:2,0`. The hosted lane produces 24 kHz and Piper 22.05 kHz, so the
  #   path to this device MUST resample. `tests/test_air1_rate_pin.py` holds
  #   the pin and reddens if a producer's rate moves without one.
  # * Capture is two channels and they are two different beams: ch0
  #   Conference, **ch1 ASR**. An ear that asks for one channel gets
  #   PipeWire's average of the two, which is neither. The browser ear must
  #   select ch1 explicitly (card MARK-1).
  # * The array's hardware AEC references **its own DAC**, so the speaker has
  #   to hang off the array's amplifier. Playback routed anywhere else is echo
  #   the AEC has never heard of, and it comes back as false barge-ins.
  #   `tools/measure_erle.py` measures what survives; the gate is >= 20 dB.
  # * Each USB capture channel is a runtime-selectable MUX
  #   (`AUDIO_MGR_OP_L`/`_R`, vendor control interface, same one as DoA above):
  #   raw mic, amplified mic as the canceller receives it, far-end reference,
  #   AEC residual, or the processed beam. So the echo measurement can be taken
  #   the same-instant way on ONE recording — `measure_erle.py --method mux` —
  #   and needs no second loudspeaker. It needs the udev rule above. Writing
  #   that mux changes what a LIVE capture stream carries, so the tool reads the
  #   routing first and restores it in a `finally`; nothing is saved to flash.
  # --- AIR-1 device notes — END -------------------------------------------
  doa: false

  # The television's azimuth, in degrees, as [start, end]. Wrap is allowed:
  # [350, 10] is the twenty degrees around due north. A turn arriving from
  # inside this sector is refused UNLESS the voice check passes — belt and
  # suspenders, in that order, and the voice check is always the authority.
  # Setting this while `doa: false` is REFUSED: nothing would ever read an
  # azimuth to compare it against.
  # rejected_sector: [80, 110]

# WHEN YOUR TURN ENDS (card TURN-1). ------------------------------------------
#
# THIS WHOLE BLOCK IS COMMENTED OUT AND THAT IS THE SHIPPED DEFAULT. With no
# `turn_detection:` key the lane sends exactly `{"type": "server_vad"}` — the
# frame it has sent since 2026-08-18 — so copying this file changes nothing
# about how the robot listens. Uncomment a block below to change it.
#
# Until this card, endpointing was not a setting at all: `"server_vad"` was a
# string literal inside realtime/protocol.py, and the provider's ~500 ms silence
# tail was therefore a property of the source code. Every pause longer than it
# ended your turn — including the pause in the middle of an ordinary sentence.
# (`robot.yaml speech.endpointing: semantic` is a DIFFERENT endpointer on the
# `--legacy` local loop and has never applied to the hosted lane.)
#
# TWO ENDPOINTERS, AND THEY TAKE DIFFERENT KNOBS.
#
#   server_vad    an energy VAD. Ends your turn after a fixed silence tail.
#                 Reads: threshold, prefix_padding_ms, silence_duration_ms.
#   semantic_vad  asks the model whether the sentence SOUNDED finished.
#                 Reads: eagerness.
#
# Sending a knob to the endpointer that does not read it is REFUSED here, not
# ignored. The provider would accept the frame and drop the key — which is
# precisely how every session before 2026-08-18 ran on the wrong voice and the
# wrong VAD while this repo believed it had set both.
#
# turn_detection:
#   # An energy VAD that waits longer than the default before deciding you are
#   # done. 200-800 ms is the accepted band; outside it is a refusal, because
#   # below 200 ms the tail is shorter than the pause inside an ordinary
#   # two-clause sentence and above 800 ms the robot reads as deaf.
#   type: server_vad
#   silence_duration_ms: 700
#   # Activation probability, 0-1. Higher is harder to trigger — raise it in a
#   # room with a television in it.
#   threshold: 0.5
#   # Milliseconds of audio kept from BEFORE the trigger, so a turn does not
#   # start with a clipped first syllable. 0-2000.
#   prefix_padding_ms: 300
#
# turn_detection:
#   # The one this card exists for: let the model decide whether you finished
#   # the sentence, and trade eagerness for patience.
#   type: semantic_vad
#   # low | medium | high | auto. `low` waits — it is the setting for a person
#   # who thinks mid-sentence. `high` jumps in. `auto` is the provider's balance.
#   eagerness: low
#   # Whether the PROVIDER cancels its own reply when it hears you start
#   # talking. The lane also barges in locally the moment it hears you, so this
#   # is the other half of the same behaviour and you almost certainly want it.
#   interrupt_response: true
#
# create_response: false is accepted on both and is a FOOTGUN worth stating
# out loud: it means the provider commits your turn and then answers nothing,
# leaving the lane's watchdog as the only thing that will ever speak. It exists
# for a build that drives responses itself. Leave it alone otherwise.
#
# Which one to run is a MEASUREMENT, not a taste: record twenty two-clause
# utterances and replay them through each arm —
#
#   .parcel/bin/python tools/replay_turn_detection.py --arms
#   .parcel/bin/python tools/replay_turn_detection.py --plan --out <dir>
#   .parcel/bin/python tools/replay_turn_detection.py --replay --live \
#       --recording <dir> --arm semantic_low
