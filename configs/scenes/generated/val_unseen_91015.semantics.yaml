# GENERATED by evals/nav_instruct/scene_gen.py — do not hand-edit.
# The vocabulary below is re-emitted verbatim from
# configs/scenes/city_block.semantics.yaml; only `scene` differs. Geometry
# is never carried here — it is read from the MJCF at extraction time.
classes:
  bench:
    affordances:
    - near
    - next_to
    - towards
    aliases:
    - seat
    - park bench
    - bench seat
    kind: object
    landmark_roles:
    - waypoint
    - seating
    - meeting_point
    metadata: {}
    size:
      note: One bench instance; its parts (seat/back/legs) merge into `bench_1` and
        the radius is the max over parts. Derived radius 0.733757 m. The surface-anchored
        next_to band scales with that radius, so the size no longer decides the affordance.
      source: geom_radius_m
  building:
    affordances:
    - near
    - towards
    aliases:
    - bldg
    - storefront
    - building face
    kind: object
    landmark_roles:
    - boundary
    - obstacle
    metadata: {}
    size:
      note: "Facade boxes; derived radii run 1.84-2.41 m. Since the next_to band is\
        \ surface-anchored the radius no longer decides the affordance \u2014 see\
        \ the note above for why the exclusion stands anyway."
      source: geom_radius_m
  crosswalk:
    affordances:
    - inside
    aliases:
    - crossing
    - zebra crossing
    - cross walk
    kind: region
    landmark_roles:
    - traversable
    - boundary
    metadata: {}
    size:
      note: Four `xw*` stripes merge into one crosswalk region at extraction.
      source: polygon
  lamppost:
    affordances:
    - near
    - next_to
    - towards
    aliases:
    - lamp post
    - streetlight
    - street light
    - lamp
    kind: object
    landmark_roles:
    - waypoint
    - meeting_point
    metadata: {}
    size:
      note: Both lampposts are 0.06 m poles. `object_near_envelope_m` gives them the
        point-anchor stand-off rather than a radius-derived one.
      source: geom_radius_m
  planter:
    affordances:
    - near
    - next_to
    - towards
    aliases:
    - plant pot
    - flower box
    - pot
    kind: object
    landmark_roles:
    - obstacle
    metadata: {}
    size:
      note: Both planters are 0.45 m and co-located with a tree.
      source: geom_radius_m
  sidewalk:
    affordances:
    - inside
    aliases:
    - pavement
    - safe region
    kind: region
    landmark_roles:
    - traversable
    - waypoint
    metadata: {}
    size:
      note: The scene has a north and a south sidewalk; both carry this class.
      source: polygon
  tree:
    affordances:
    - near
    - next_to
    - towards
    aliases:
    - trees
    - street tree
    kind: object
    landmark_roles:
    - waypoint
    - obstacle
    metadata: {}
    size:
      note: "tree_1 and tree_2 are GEOMETRICALLY IDENTICAL in this scene (both 0.58\
        \ m derived radius, canopy included), so no size attribute can distinguish\
        \ them and \"the big tree\" honestly keeps both \u2014 see navigation/attributes.py,\
        \ which compares against the per-class median inclusively for exactly this\
        \ reason. Each tree is also co-located with a planter, which is what defeats\
        \ approach-pose search for every tree directive (backlog N-SUP-4)."
      source: geom_radius_m
geom_prefixes:
- class: lamppost
  prefix: lamp_post_
- class: bench
  prefix: bench_
- class: tree
  prefix: tree_top_
- class: tree
  prefix: tree_
- class: planter
  prefix: planter_
- class: building
  prefix: bldg_
region_prefixes:
- class: sidewalk
  prefix: sidewalk
- class: crosswalk
  prefix: xw
- class: crosswalk
  prefix: crosswalk
scene: configs/scenes/generated/val_unseen_91015.xml
schema_version: 1
