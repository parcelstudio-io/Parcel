# Task board · designed 2026-08-21 (evening) · Fable

The forward plan assembled from every open register, audit carry-forward,
and research recommendation. One card one tree; Opus executes, Fable
audits; dispatch gates as marked.

## Wave A — DONE: the perception cutover chain (audited, AUDIT_CHAIN_FABLE.md)

| Card | State |
|---|---|
| W-1 world worth looking at | **ACCEPT_CLOSE** — scene/manifest/scene-truth reconciled via owner-authorized re-pin ("Re-pin.", 2026-08-21); gate fully green, sweep 15/15/15 (AUDIT_W1_INCIDENT_FABLE.md) |
| C-1 attach the eye | **ACCEPT_CLOSE** — call site exists, live proof both arms, CollisionGate p99 +0.735 ms ≤ +5 ms; two pre-registered MISSES honest (freshness 562 ms vs 300 ms TTL ⇒ stream is diagnostic-only, not grounding authority; whole-loop +28.2 ms). Audit correction: 68 leased inferences, not 69 |
| C-2 online semantic map | **ACCEPT_CLOSE + AU-C2-1 carded** — package real, R27-clean; 0/5 live-corpus MISS honest; BLOCKING finding: ranking_margin ≡ 0.0 under label-primary retrieval. AU-C2-1: REVISION §6 source crop silently dropped on store persist (round-trip proven) |
| C-3 grounding cutover | **ACCEPT_PARTIAL** — semantic_source axis, POI disable proven, taxonomy landed; shadow agreement 0.0/18 (reproduces C-2's finding independently), admission_flip 0/18 clean; tail NOT REACHED (VLM veto, F2 re-derivation, G1 voice, G2 missions) — central claim UNPROVEN by its own record |
| E-2 held-out generalization | **ACCEPT_HALT (exemplary)** — Rule E honored, held-out exposure UNSPENT (sha verified twice), zero source edits, seven defects filed E2-D1..D7 (E2_STATUS §4). Audit granted the two doc allowlist seats its record required |
| Fable audit of the chain | **DONE** — 4 adversarial verifiers; 1 refutation (AU-C2-1), 2 count corrections, contract v2 lessons (E2-D7 + doc catch-22) |

## Defects from the chain (owner-visible, dependency-ordered)

| ID | What | Card |
|---|---|---|
| E2-D2 | 160/160 motions accepted, ~3.35 cm displacement — undiagnosed; blocks ALL patrol/mission measurement | **task_20 MOVE-1** (dispatching) |
| E2-D4 + E2-D5 | ranking_margin structurally 0.0 (measured twice); all learned-map thresholds fitted on invalidated distribution; masked only by `abstention.enabled: false` | **task_21 PG-4** (OWNER-GATED: touches PG-3 internals) |
| E2-D1 | red-team decoys have no home | **task_22 RT-1** (owner choice inside: variant scene vs re-pin) |
| AU-C2-1 | map-entry source crop dropped on persist ⇒ no re-embedding evidence after reload | folds into task_21 or its own micro-card |
| E2-D3 | T1 patrol query vocabulary unspecified — open design question | part of task_20 |
| E2-D6 | E-2 harness needs a held-out allowlist seat before re-dispatch | granted at re-dispatch, per contract v2 |
| E2-D7 | entry gate pollutes successor quiescence (mtime) | fixed in contract v2 (git status + content hash) |

## Wave B — NEXT (cards written, dispatch-gated as marked)

| Card | What | Gate |
|---|---|---|
| **task_15 M-1** measurement pack | pass^k (k=3) over the 52-query corpus incl. the e-stop's first real reliability bound + q34; SLURP-style intent+slot; FDB-v3 latency defs | after Wave A audits |
| **task_16 M-2** adversarial sets | BFCL-style irrelevance set (the fabrication weakness); held-out phrasings authored by a gold-blind agent (the in-domain→held-out drop) | after M-1 |
| **task_17 R28** arrival every class | 1-of-5 verified arrival → all classes, measured per-class in the textured world | after Wave A |
| **task_18 R30** evals hygiene | vacuous latency ratchet, acoustic sentinel, E1 seal, requirements-lock, Gemma provenance, the model-seat fixture gate | after M-1 |
| **task_19 SI-v3** prompt catches up | tool surface named, map-fact honesty, latch language; A/B with per-pair gold | after C-3 |

## Wave C — owner experience (cheap, high signal)

* **Voice-tier experiment:** one session on full-size gpt-realtime vs the
  mini — answers how much remaining conversational friction is substrate
  ceiling (Voice Showdown places our mini near the bottom of the frontier).
  One config line + one session; I package the comparison.
* **Owner session 3** on the post-cutover build: scene answers from the
  dog's own map, memory with provenance, latch narration, "Dye. Stop." —
  the acceptance test only a human can run.
* **R29 owner-doc re-audit** (docs/CONVERSATIONAL_AUTONOMY_HIGH_LEVEL_
  DESIGN.md is stale since 08-15; regenerate against the shipped system).

## Owner actions (standing, none urgent)

1. ~~Land the wave~~ — **DONE** (`e5d4956`, `71b39a1`).
2. The 256-row memory cleanup: quarantine / delete / leave (tooling ready,
   dry-run verified).
3. The udev rule (unlocks DoA on the mic array).
4. One-minute voice enrollment (F1-SI's live half).
5. 25-thread corpus human-review sign-off.

## Registered debt (scheduled when it hurts, not before)

runtime.py decomposition (8,900+ lines) · panel JS test harness · barge-in
mark integrity · Ministral judge seam (trigger not met) · local hotword
hardware e-stop (Space remains the guarantee) · Habitat as the phase-2
held-out venue · inherited flake `test_runtime_streaming_text_...` ·
R22's double-ledger panel-turn open risk · R25 durable-spend restart gap.

## Standing rules

Pre-registration before measurement · null controls beside every
localization claim · seeds RED with cache purges + post-write sweeps ·
store isolation mechanical · deviations declared · failures recorded as
failures. The evidence bar does not relax when nobody is watching — that
is the register's whole point.
