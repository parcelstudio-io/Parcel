# A9 MEMORY-FIXES + NON-TRAVEL LIFE · acceptance VERDICT (Fable) · 2026-08-24

Verification: my guard run (label `fable-a9-verify`) — both A9 suites + the
four A-card suites + all three ratchets/r24 + the memory, abstention/map,
drives/body, runtime, and prototype-profile suites = **1091 passed, 0
failed**; ruff clean on all ten touched files; scope = six modified product
files (+373/−21) + two new leaves (body_lane.py 286, initiative.py 684) +
two suites; `config.py` byte-unchanged at 1000 lines; the owner-store rule
verified independently (no `parcel_memory` reference, no
`PARCEL_MEMORY_PURPOSE` set — only a docstring stating the rule); every diff
and both leaves read line-by-line. The register's noqa count (0 added) is
CORRECT this time — the distiller's BLE001 is a pre-existing context line.

## Disposition: **ACCEPTED — Lane A is complete**

- **The four H5 defects are fixed at the named sites, honestly layered**:
  the tombstone-aware upsert is a statement about the table's shape (one row
  per key, revived in place) while the model-resurrection policy correctly
  lives one layer up on the write path (`respect_revocations=True` covers
  EVERY caller, not just the wrapped proposer); `session_id` now exists in
  the reader AND is writable at the legacy door; `complete_json` is a second
  constrained mode that deliberately does NOT ride `_request_structured` —
  a background distillation pass cannot cancel a live spoken turn; the
  ranking-margin fix replaces only the estimator an UNCONFIGURED map selects
  (explicit policies, including robust_z, win unchanged — P0-D's seeded-red
  stays red). Each defect has a pre-A9 arm in the suite that still
  reproduces the measured failure.
- **The body lane's safety argument is structural**: it reads `_last_sent`
  (the OUTPUT of finalize_command, assigned post-acceptance), contains no
  actuator route (token-scanned with docstrings stripped, with an
  anti-vacuity control), holds no lock, and drives nothing — both body
  adapters remain uninstalled. HOLD is a command (`locomotion is HOLD`,
  ttl > 0), e-stop is same-tick with epoch +1 exactly, and H4's 0.88-tick
  reference is asserted as the bound's mechanism.
- **The zero-translation lease is enforced by vocabulary, construction, and
  arbiter**: `BodyOffer` cannot express translation; the lease refuses a
  nonzero radius at construction and a travelling proposal at admission BY
  EXCEPTION (non-vacuous — H3's own proposer at radius 6 forms the refused
  proposals); no drive source exists in `SOURCE_PRIORITIES`. The yield is
  same-tick by tick-order (preemptions read before budget, terminal applied
  before the offer returns). `Terminal(returned=True)` raises with the
  refuted stop-and-return numbers in the message — the ratified amendment
  made unforgeable. Rate 6/h = the measured ceiling inside D1's band;
  initiative SHIPS OFF; quiet-hours stay the product's own door.
- **Composition held everywhere**: e-stop reads `arbiter.emergency_stopped`
  (no second flag), follow keeps motion authority over a drive, the opener
  asks A7's governor exactly once as routine (never critical) and degrades
  to seeded local phrases; no-governor = local, correctly ("a build without
  a budget is not a build with an unlimited one").

Accepted judgment calls: the per-key revocation gap (sister/sister_name)
recorded as its own row rather than widened — right, value-level suppression
needs its own measurement; the lane rides the expression tick at the
runtime's real cadence rather than a new thread — right for an inert
descriptor; `_background_policy` chooses at the site where the background is
BUILT — the correct home for the estimator decision.

Undone, correctly named: the runtime never BEGINS a behavior (no digest
source wired — initiative is product code with no product trigger, per M1's
OFF default); no config key turns initiative on (the DEC-0 ceiling; the
enabling card owns the whitelist edit); the remark line is carried, not
spoken (the second governor call site decision is A7-firewall work);
no body adapter installed; H4's jerk/P99 rows not re-measured (limiter
byte-unchanged); the receding-horizon planner is unreachable at radius 0
and belongs to the card that earns a positive radius; the soak + blinded
human nuisance bar are eval/box-day; defect 2's 0.96 precision awaits a
live local-GPU re-run ($0 rule held); `distil_session`/scheduler still have
no product caller — Gate 8's other half. Does not prove: anything physical;
no behavior has ever moved a real body, and nothing here can make one.
