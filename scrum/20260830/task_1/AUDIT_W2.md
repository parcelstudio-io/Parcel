# AUDIT · W2 INSTALLS-1 (C4 hook + C5 speech acts on the product path) — verifier: Fable (parcel-0e), 09:5x 08-30

**Disposition: ACCEPT (pending parcel-6c's lens).** Worktree `~/.cache/parcel-0e/wb/w2` at `c96ac34` (`parcel_robot.__file__` from the worktree); 11 tracked files +844/−47 plus the new `tests/test_realtime_speech_act_install.py` (1,241 lines, 25 tests).

| row | executor | verifier |
|---|---|---|
| MB-1 corpus through the PRODUCT path (fake executive → receipts → `_accept_plan` hook → whisperer → real `RealtimeLane` over `FakeRealtimeServer`, flag ON): b1 75/75 from `KIND_PLAN_ACCEPTED` (65 forwarded, 5 `plan_not_admitted`, 5 `plan_reissue`); grounding 1.000 over 80 turns, invented 0, keys 15/15 | GREEN | accepted from the register (the install test exercises it) |
| flags OFF: C4 `4e5e2e47…` and C5 `edaa32ed…` unchanged, re-derived at HEAD before editing and at close | GREEN | tests cover both; my guarded run below includes them |
| poisoned `goal` / claim-bearing `ask_clarify` REJECT → template, never speech; swapped destination `foreign_place_name:bench`; empty `places` → error event + `SpeechActInstallError` | GREEN | in the install tests |
| rule-4 rows: activation-time firing + dropped-before-activation; mission = executive `task_id`; queue-policy re-issue fires beside same-task that does not; hourly ceiling N = 6/h priced; `as_dict` renders `speech_acts` and TURN-1 re-pinned "+2 keys, 0 changed" with cause; item-only + local TTS with 0 `response.create`; matcher docstring delta | GREEN (25 tests) | **guarded re-run in the worktree: 258 passed** (`test_realtime_speech_act_install` + `test_turn1_endpointing` + `test_runtime_whisperer_wiring` + `test_realtime_lane*` + `test_speech_acts` + `test_whisperer_plan_accepted` + `test_realtime_spend_budget`) |
| hunk adjacency (`runtime.py`) | 19 hunks adjacent to the import block / `EMERGENCY_STOP_TERMINAL_REASONS` / `_accept_plan` / `_narrate_mission` / `_whisper` / `_narrate_mission_terminal` / `_whisper_curiosity` / `_whisper_refusal`; root's 23 dirty hunks avoided | **verifier's intersection check: 19 W2 hunks vs 27 root dirty hunks (U0) → 0 overlapping** |
| hygiene | 0 noqa added; `config.py` 1000; ruff adds 0 | **noqa added in the diff: 0; in the new test file: 0; ruff All checks passed; config.py 1000** |

## Three items the executor flagged, my reading
1. **Voicing boundary:** "never a `response.create` on a fact" is enforced on the CONTRACT path; kinds MB-2 has no act for (block clear, battery, pace, curiosity) still narrate their model-facing template through the model path — over the MB-1 replay the count is 0 because every fact driven is contract-bearing. Both halves pinned. Accepted; the remaining kinds are a follow-up (extend the contract), not a defect.
2. **Clarifications:** claim-free now (0 claims / 0 invented), but 4/5 still REJECT on `foreign_place_name` alone because `ask_clarify` has no `goal` slot in MB-2's frozen contract — measured and reported on the "or" branch, not fixed by widening frozen evidence. Accepted; contract extension is a new card.
3. `_accept_plan` now narrates, so four pace rows in `test_runtime_whisperer_wiring.py` read `_paced(lane)` (same list minus acknowledgements) rather than a re-pinned count — no pace number moved. Accepted. No `KIND_REROUTE` runtime install (not on the build list; no decision object on the social-progress path) — door, cap and ceiling live at the whisperer.

## Second lens (parcel-6c, read-only in the W2 worktree, 10:0x; `~/.cache/parcel-verify/w2-lens/NOTE.md`) — CONCUR / ACCEPT
Every binding row implemented AND pinned by a named test: the C4 hook refuses `DEFERRED_ADMISSION_DISPOSITIONS {"defer","deferred"}` (`runtime.py:620`) and fires on the activation call (tests :740/:782); `task_id` is the executive's PlanIR id (:624), `plan_digest = ValidatedPlan.plan_sha256`, lineage from the `_accept_plan` branch (REVISE :3566 / NEW :3577; `queue` accepted from W1, never derived) with the same-task / queue-re-issue pair (:651/:677/:722) and undeliver on a failed narration. C5: render and check are one call (`voiced_sentence`); a REJECT is counted with reasons and the CALLER's template is narrated — the rejected render never reaches the wire or the speaker (:844 poisoned goal, :879 poisoned question, :897 inseparable, :910 swapped destination); empty `places` raises `SpeechActInstallError` out of the lane to the runtime door → error event + template (:941/:979); `places` is the learned map's `known_places`. Voicing: contract path = item without `response.create` (`ResponseCreate` only when `spoken is None`, :2064-2068) + `_speak_system_utterance(lane.last_narration)`; corpus-wide 0 response_creates, voiced == narrated (:573), voiced sentence == the one that passed check (:590); flag OFF = model path, nothing voiced locally, OFF digests unchanged (:607/:1223). Reroute: task_id-keyed mission cap + `REROUTE_PER_HOUR_CEILING 6` (:663/:1619) with re-issue-chain, in-hour and floor-refund tests. Clarifications claim-free (:1019); the `foreign_place_name` residual is an MB-3 contract change (a `candidates` slot on `ask_clarify`), not W2's. TURN-1 "+2 keys, 0 changed" with the second key named and the cause stated.
**Two non-blocking nits → W2 follow-up F1 (dispatched):** (1) the hook's empty-lineage fallback (`REVISE if frame.speech_act == "correction" else NEW`) is a string-guessed lineage no current caller reaches — make `lineage` required / assert non-empty; (2) `_narrate_mission` reads `lane.last_narration(_from_contract)` back after `narrate_event` returns, outside the lane lock — same-thread today; return the spoken sentence from `narrate_event` to remove the read-back dependency.

## Follow-up F1 — verifier, 11:2x — **W2 disposition: ACCEPT**
(1) The string-guessed lineage is deleted (and was wrong: a correction with nothing active is a NEW goal); `_whisper_plan_accepted(..., *, lineage: str)` keyword-only without default (TypeError if omitted, ValueError if blank); both `_accept_plan` call sites pass it explicitly; the receipt itself does not raise (its totality is a C4 invariant; three frozen C4 rows construct it without a lineage to test the door's refusal) — deviation recorded and accepted. (2) `RealtimeLane.narrate_event_outcome() -> NarrationOutcome` decided inside the lock; `_narrate_mission` voices `outcome.voice_locally`; `last_narration_from_contract` deleted; `narrate_event` keeps its bool contract as a facade (changing it would move 24 assertions across 6 files and C5's digest recipe) — deviation recorded and accepted; all new function lengths under the DEC-0 ceiling. Executor: 28 install tests, 1,685 passed across 20 files, digests unchanged, MB-1 corpus identical (75/75, 1.000, 0, 15/15, 0 response.create), R24/nominal-stop zero re-pins. **Verifier's guarded re-run after F1: 227 passed on the six-file subset (install + turn1 + wiring + lane + speech_acts + plan_accepted; the earlier 258 included spend_budget); noqa added 0; ruff clean; `NarrationOutcome` at lane.py:506, `NARRATION_REFUSED` :550, `narrate_event_outcome` :2056, `lineage: str` keyword-only at runtime.py:17385.**
