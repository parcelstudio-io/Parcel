# AUDIT · W5 REFREEZE-1 — verifier notes (Fable / parcel-0e), started 07:1x 08-30

## Verifier's own record of Sol's v4 panel remediation (clean worktree at HEAD c96ac34 + Sol's three in-tree files copied at ~06:3x; PYTHONPATH pinned; MUJOCO_GL=egl; through the guard)

| check (board rule 3) | result |
|---|---|
| (a) provenance keeps C0's sentences | present: "disabled as a kill channel" ×1, "re-armed when D-15 agrees" ×1, `a379bf4` ×1; `reactive_gate_exercised` ×3; panel `generated_at 20260830T050043Z`, 9 episodes, 5 clean checks incl. `reactive_gate_exercised`; `reactive_gate_disabled` killed via `['reactive_gate_exercised', 'final_poses_within_tolerance']`. Per-episode intervention counts: no `reactive_gate_coverage` field at the top level of the copy I took (`coverage None`) — the per-row justification is to be confirmed by the W5 executor against the field names Sol used |
| (b) live reproduction | not run separately by me (the freshness test's live row covers it — see (c)) |
| (c) freshness test green | **FAIL at copy time: 2 failed / 9 passed in 1254 s** — `test_mutation_panel_runs_on_the_current_frozen_set_live` and one more (the run's tail lists one name; the executor's re-run will name both) |
| (d) hashes at verification time | recorded above in the shell log (copy vs tree-now hashes); Sol may still be editing |

**Reading:** Sol's design certificate is incomplete as of my copy — the live row did not reproduce in a clean tree at HEAD. Under Amendment A1 this is moot for landing: Sol's v4 panel cannot land unchanged once W4 introduces v5; W5 regenerates a v5 panel (C0's lineage carried; Sol's four witness rows re-selected on v5 with counts; Sol attributed for the design and the check) and check (v) applies. The W5 executor re-records the four checks with its own hashes; if its copy also fails (c), the board line for Sol says so.

## Final pass on the merged gate tree — verifier, 15:4x — **W5 disposition: ACCEPT**
- **Artifacts generated ON the merged tree, bit-identical to the preview** (so W1+W2+W3+W6 move the nav evidence by zero — a measured result): v5 MINIVAL baseline `nav-instruct-v1-baseline-v5-20260830T135548Z.json` report digest `53eec205…`, episode digest `2822ebfd…`; v5 MATRIX artifact `nav-instruct-v1-matrix-v5-20260830T135527Z.json` report digest `041d9bdd…`, episode digest `5ea2cd93…` (= Sol's matrix digest lineage); matrix sentinel `5979b8e8…`.
- **Ledger `+1` exactly** (numstat `1 0`; prefix `cmp`-identical to HEAD): `frozen_baseline true`, minival, n 25, `scaled-path-v1 / 200 / 20260804`, SR 0.24, SPL 0.18509069, collisions 0, authority {agreement 21, disagreement 4, false_arrival 0}, episode_digest `2822ebfd…`, a 3,288-char `refreeze_provenance` carrying A5 in full (A2 ×4 incl. A-00 as a LOSS with the §1.4 citation; a379bf4 ×1; W4 ×2; SR 0.24 = W4's D-15; the band change moves SR/SPL by zero).
- **v5 mutation panel (LAST, by A2 measurement on that tree):** passed, survivors [], equivalent [], **8 mutants / 8 killed**, clean authority **{agreement 9}**, collisions 0, all five clean checks green — the declared disable is **withdrawn by regeneration** (D-15 agrees; `inverted_relation` now kills through `no_authority_disagreement`); C0's sentences remain a strict prefix of the provenance. Witness rows selected by measurement with per-row counts: `region_goal-C-11` chg_nz 96, `object_relative-C-11` 31, `region_goal-B-09` 20, `region_goal-D-17` 17 (Sol's C-11/D-17 re-selected on v5 by measurement — attributed; `object_goal-D-18` dropped, `B-09` added).
- **Eighth mutant — accepted with the reasoning recorded:** the four-suite run first read 2 failed on "no_false_arrival is green on the clean run but no mutant exercises it"; a controlled experiment (the three v4 candidates on the old five rows under v5 code) showed it is the CODE axis — W4's `receipt_says_arrived` is now the single arrival predicate and no existing mutant can produce a false arrival through it. `arrival_receipt_bypassed` (keeps `receipt.arrived`, drops `receipt.is_for(...)` — exactly the B-09 defect F2 fixed; monkeypatch only) is killed through `no_false_arrival` (mutant authority {agreement 8, false_arrival 1}). A design change the card did not request; without it the freshness rows are red. Verifier's ruling: this is the E3 rule applied correctly — the mutant ships in the same commit as the check it exercises and the panel that proves it.
- **Honest negative:** the §10.3 prediction that D-15 would bring a hard-stop witness is falsified — `translation_zeroed` is 0 across all 125 v5 rows (D-15 now arrives instead of grinding into the gate); hard-stop coverage remains unproven and is written into the artifact's provenance.
- Proof: four suites 141 passed / 0 failed (26 m 44 s, guard); receipt-authority + receipt 14 passed; **M1's expected red closed** (sentinel literal 4 → 6 green); ruff clean on the six W5 files; tier partition 4/6 commit, 2/6 slow; the seven transferred files `cmp`-identical to the worktree-proven versions; no `src/` edit; T1's folder untouched. Diff on top of M1: 8 files +2456/−157 + 3 new (matrix freshness test, two v5 artifacts). Whole gate tree vs HEAD: **48 files, +5686 / −431** (+ new files).

## Integrator ruling on the eighth mutant (parcel-fb, 15:5x) — CONCUR with four conditions → W5-F1 (dispatched; edits after the running close gate reports; one more gate run follows)
(a) the mutant patches the PRODUCT predicate at its definition (`parcel_robot.instructnav.arrival_receipt.receipt_says_arrived` / `ArrivalReceipt.is_for`), never the runner's call site; (b) killed through `no_false_arrival` on a witness row where the receipt refuses by IDENTITY (B-09's wrong_instance), named in the provenance with the controlled experiment ("code axis, not row selection"); (c) `_MINIMUM_KILLED` stays a FLOOR (6) — the count grows to 8, the floor does not chase it — and the mutant's `checks_reddened` is recorded like the others so a later row change that stops exercising it reddens the "no_false_arrival exercised" assertion; (d) the provenance states that `phantom_view_consistent` no longer reddens `no_false_arrival` BECAUSE the receipt refuses phantom arrivals (a product improvement) and lists the channels it is still killed through. Plus: the hard-stop sentence names WHERE the hard stop IS certified (`core/hard_stop.py`'s tests, the A3 latch tests, the A6 stop path) so the fifth check reads as certifying the gate's SLOWING branch (changed_nonzero 96/31/20/17), not full gate coverage. Precedent for a growing panel: VS-6's seventh mutant.

## F1 — verifier, 18:2x — ACCEPT (with one coverage limit recorded for the integrator's ruling)
- **(a) the mutant's patch site:** as first built it patched the eval runner's own binding (`runner_module.receipt_says_arrived` — one call site of four); moved to `ArrivalReceipt.is_for`, the gate panel reported **`Equivalent: arrival_receipt_bypassed`** because W4-F4/F5 made `receipt_says_arrived` call `receipt_refusal` directly (Sol's `passed = not survivors and not equivalent` rule is what stopped a dead mutant shipping green); re-targeted to **`arrival_receipt.receipt_refusal`** — where identity is decided — with its three identity guards (`STALE_RECEIPT`, `OTHER_PLACE`, `SUPERSEDED`) deleted; a missing receipt is still refused and an honest reason survives; verified to flip the definition, the runner binding, `runtime.py`'s binding and `is_for` False→True and restore. **Coverage limit (in the provenance):** `runtime.py:13245` and `brain/runtime_adapter.py:428` import `receipt_refusal` directly and hold their own binding — this mutant does not reach those two product consumers (the panel drives the eval runner's path); reaching them would mean patching call sites, which (a) forbids.
- **(b)** killed through `no_false_arrival` on `nav-region_goal-B-09-3ee156e4` — the only row that moves (`planning_error → false_arrival` at an unchanged pose, dtg 4.7739); row + the controlled experiment named in the provenance. **(c)** `_MINIMUM_KILLED = 6` untouched; 8 ≥ 6; the assertion reads recorded `checks_reddened`, not names. **(d)** written: phantom no longer reddens `no_false_arrival` because the receipt refuses phantom arrivals (a product improvement), the four channels it is still killed through listed; the hard-stop sentence names `tests/test_core_hard_stop.py`, `tests/test_a3_discontinuity_latch.py`, `tests/test_a6_stop_local.py`, so `reactive_gate_exercised` reads as the SLOWING branch (185 changed requests: 96/31/20/17); C0's lineage a verbatim prefix.
- **The three panel reds:** `ADDED_DEFECTS["arrival_receipt_bypassed"] = "W5"` (test not relaxed); the registration rule's intent is append-only — renamed `test_mutants_are_registered_append_only` with the dated reason (neither name a gate node id); the committed-panel pin re-pinned as a channel handover (`FALSE_ARRIVAL_CHANNEL_MUTANT = "arrival_receipt_bypassed"`, phantom still killed but not through `no_false_arrival`).
- **M6:** `scripts/mutation_panel.py` 1,142 → 851; new leaf `scripts/mutation_panel_mutations.py` (373 lines: the eight `mutate_*`, `_patched`, `_reflected_phantom`, `MUTATIONS`, re-exported); `run_panel` / `run_once` / `harness_checks` / `clean_safety_fields` / `live_clean_safety_fields` / `PANEL_EPISODE_IDS` / witnesses / pins / provenance stay by name; no BASELINE edit; panel byte-identical excluding `generated_at` + provenance (clean_run identical, 8/8). **M7:** the CARD marker pair dissolved into prose; count back to HEAD's 19.
- Proof: freshness + digest-recipe + ci_gate + matrix-freshness + v4s + scene_gen + DEC-0 → **206 passed / 0 failed** (27 m); panel PASSED 8/8; ruff clean on nine files; `DIGEST_SENTINELS` 6 checked, no re-pin (only `mutation_panel.json` regenerated — not sentinel-pinned; the v5 minival/matrix artifacts untouched). F1 moved no measurement: the four digests, the ledger `+1` and the per-row witness counts unchanged from §12. W5-on-gate diff: 10 files +2543/−440 + 4 new; whole gate tree vs HEAD **50 files +5884/−715** + new files.

**Integrator's ruling on (a) (18:4x):** targeting `receipt_refusal` satisfies (a); NO product edit to make the mutant reach the two direct-import consumers (the tail wagging the dog) — those two are certified by `tests/test_arrival_receipt_wiring.py`, the right instrument for consumers; the mutation panel proves the EVAL EVIDENCE is sensitive to the defect class. → **W5-F2** (after gate #2 reports): two provenance sentences ("the two direct-import consumers (runtime.py:13245, brain/runtime_adapter.py:428) are certified by tests/test_arrival_receipt_wiring.py, not by this mutant"; the structural guard `test_no_consumer_reads_the_expected_identity_off_the_receipt` must move with any refactor of identity decisions) → panel regeneration (same selection) → freshness/ci_gate/v4s/scene_gen → close gate #3. W5-F1 accepted for the candidate as described.
