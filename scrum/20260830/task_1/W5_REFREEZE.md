# W5 · REFREEZE-1 — the v4 minival baseline at HEAD, and a mutation panel whose episodes exercise the gate by design

**Executor:** Opus · **Verifier:** Fable · **Lens:** parcel-fb · Sources: `scrum/20260829/task_2/README.md` close-out items 1, 2, 7 and the LIVE COLLISION rule; `AUDIT_C3.md` §4.1; `research/20260824/nav-quality/RESULTS.md` §1.4.

## Part A — v4 minival baseline
The committed `nav-instruct-v1-baseline-v4-20260811T070536Z` (`c172da37…`) no longer reproduces: at HEAD the minival reads `021b67ab…`, SR 0.24 → 0.20, 5/25 rows moved — four by card A2's clearance cost (≤ 08-24, NAV-QUALITY §1.4: B-05/D-15 object_goal and D-15/A-00 object_relative, incl. A-00's lost success) and D-15 region_goal's `_inside_goal` suffix by a379bf4. `evaluate_hard_safety` reads only the ledger's pinned `collision_total` / `false_arrival`, so no gate saw it. **Sequence with W4:** W4's band change moves rows too — re-freeze ONCE, after W4 lands (or produce two attributed rows if W4 slips), never freezing the known arrival defect as acceptable (N45/N46's rule).
Build: regenerate the baseline row in a clean worktree at the result (PYTHONPATH pinned, MUJOCO_GL=egl), keep the superseded artifact, bridge file with every moved episode (before/after reason/authority/dtg, cause: A2 | a379bf4 | W4), update `scripts/ci_gate.py`'s pinned numbers + digest constants + `tests/test_nav_instruct_digest_recipe.py` published digests, and the README table row.

## Part B — mutation panel with gate-binding episodes (Sol's remediation)
Sol's in-tree edits (`scripts/mutation_panel.py`, the panel JSON, `tests/test_mutation_panel_freshness.py`: four intervention-witness rows, `reactive_gate_coverage`, fifth undisableable check `reactive_gate_exercised`) are the second half of B32/item 7. **Verify, do not rewrite:** in a clean worktree at HEAD + those three files, (i) the freshness test green; (ii) the panel provenance keeps C0's re-run sentence, the a379bf4 attribution and the declared-disable clause until D-15 agrees; (iii) each new row justified as "the gate binds by design" with per-episode intervention counts in the JSON; (iv) `reactive_gate_disabled` killed through the fifth check on every new row. If all four hold, land the three files with Sol's attribution in the wave commit; if not, write exactly which fails and land nothing of Sol's — the freshness test may assert the fifth check only in the commit that ships the check and its panel.

## Acceptance
`ci_gate --tier commit` in a clean worktree at the result: hard-safety PASS with no declared disable left that D-15 has satisfied; `tools/codebase_index.py --check` and `tests/test_nav_instruct_digest_recipe.py` green; every moved row attributed; superseded artifacts present.

## Amendment A1 (07:0x, parcel-fb lens — binding; re-sequences the card)

**Order: W4 first (v5 episode set + bridge + ArrivalReceipt), then W5 on top of W4's patch.** Part A becomes the **v5** minival/full baseline row; the v4 row `…-v4-20260811T070536Z` is kept superseded and its five moved episodes are recorded in the bridge as the v4 story (A2 ×4 incl. `object_relative-A-00` as a LOSS citing NAV-QUALITY §1.4; a379bf4 ×1), SR 0.24 → 0.20 stated as the cost of A2 and the v5 SR/SPL stated separately — never "SR improved to X" without the v4→v5 band change named as the reason; v4's `episode_digest` untouched; v5 gets its own; `tests/test_nav_instruct_digest_recipe.py` published digests updated for v5 only. Part B: Sol's three files cannot land unchanged once v5 exists (`_CURRENT_FROZEN_EPISODE_SET = max(vN)` reddens a v4 panel); Part B becomes **"Sol's design, re-run on v5"** — C0's five rows re-derived on v5 + Sol's four intervention-witness rows re-selected on v5 with per-episode counts re-measured, Sol attributed for the design and the `reactive_gate_exercised` check; the four checks stand plus **(v) the new panel's `episode_set_version` equals `_CURRENT_FROZEN_EPISODE_SET` in the tree it lands in, verified by running the freshness test in the gated worktree**. "No declared disable left that D-15 has satisfied" is verified by the freshness test's greener-direction message on a REGENERATED panel — never by editing provenance by hand. Hashes at verification time; a board line for Sol either way.

## Amendment A2 (07:2x, parcel-fb — binding for the v5 panel)
Do NOT inherit Sol's four witness episode ids. Re-select witness rows on v5 BY MEASUREMENT: run the clean pass over the v5 full matrix with the gate-coverage counter; choose rows where the per-episode coverage `changed_nonzero > 0` on the clean run (the gate binds by design); confirm `reactive_gate_disabled` reddens `reactive_gate_exercised` on each. If Sol's four are among them, keep them and attribute the design; otherwise the list changes and the provenance says why. Coverage counts are stored PER ROW in the panel JSON under a named field, and the freshness assertion reads the per-row field, not an aggregate, so a future row that stops binding is visible by name. Verifier's record of Sol's v4 design (AUDIT_W5.md): live freshness row red at HEAD (2/9); hashes stable since 06:3x.
