# Task 1 · FIX-SUBSTRATE-2 (wave B) — finish the unfinished and the frozen work

**Date:** 2026-08-30 (America/New_York), opened 06:4x EDT · **Integrator / verifier:** Fable (parcel-0e) · **Executors:** Opus, one per card · **Second lens:** parcel-fb (E3 / re-freeze cards W4, W5), parcel-6c (W2)

**Owner instruction (verbatim):** "please finish the unifinished or fozen work"

**Reading of the instruction (stated assumption):** (a) wave B of `scrum/20260829/task_2` — the installs and the plan queue that were parked behind the owner's uncommitted product diff; (b) the E3-frozen decisions parked with the owner's name in the task_2 close-out, now executed **under the repository's attribution/re-freeze policy** (E8 precedent: `scrum/20260809/task_15/E8_V4_REFREEZE_STATUS.md`, `evals/nav_instruct/bridge_v3_v4.py`) with every moved digest attributed and the superseded artifact kept. NOT read into it: enabling C3's release door or raising the planner's demand on the shipped profile (value changes — measured and presented, not flipped), and committing the owner's/Sol's 28-file product diff itself. Physical motion: **NO-GO**, unchanged.

## Board

| card | file | executor touches | acceptance instrument | status |
|---|---|---|---|---|
| **W1** C6 plan queue with lineage (queue / revise / keep; resume-parent offers; cue-robust amendment admission) | `W1_PLAN_QUEUE.md` | `brain/executive.py` (region-confined hunks), `runtime.py` `_apply_goal_amend`, new leaf `brain/plan_queue.py` | NAV-INT-1 tier: admission ≥ 0.9, amended success ≥ 0.8, resume path ratio ≤ 1.1×, the two live defects green | OPEN |
| **W2** C4 + C5 installs (plan-acceptance hook at `_accept_plan`; speech acts in `narrate_event`) with every wave-B row from task_2 | `W2_INSTALLS.md` | `runtime.py` (`_accept_plan`, `_narrate_mission`), `realtime/lane.py`, `realtime/config.py` `as_dict`, `voice/amendment.py` (claim-free clarifications), tests | MB-1 corpus through the PRODUCT path: b1 75/75, grounding ≥ 0.98; off-path digests unchanged with flags OFF; TURN-1 re-pinned | OPEN |
| **W3** C1 per-navigator world identity (explicit, from the composition root) | `W3_WORLD_IDENTITY.md` | `navigation/pipeline.py` (`DirectiveNavigator` ctor/`from_config`), composition roots (`runtime.py` sim adapter, `sim.py`, `web_panel.py`, eval runners), `poi_admission.py` | NAV-GEN-1 A0 rows byte-identical; minival `021b67ab…`; panel {4, 1}; the process-scoped published scene becomes the fallback only | OPEN |
| **W4** One arrival authority: B32 — the `near` band ∩ the support polygon, composed into the runtime's receipt, with the E3 re-freeze | `W4_ARRIVAL_AUTHORITY.md` | `instructnav/scoring.py` `object_near_goal_region`, `evals/nav_instruct/generator.py`, the K0 seam, `runtime.py`/executive receipt; re-freeze of the affected v4 rows with attribution (bridge file + STATUS) | NAV-INT-1 authority disagreements ≤ 2/80, bench 0/29; `test_semantics_and_eval_object_goal_regions_agree` green; new frozen digest attributed row-by-row | OPEN |
| **W5** Frozen evidence re-freeze: v4 minival baseline at HEAD (A2 clearance + a379bf4 D-15 suffix) and the mutation panel with episodes on which the gate binds by design (Sol's remediation, verified and landed under the E3 rule) | `W5_REFREEZE.md` | `evals/nav_instruct/results/*baseline-v4*`, `scripts/ci_gate.py` pinned numbers, `scripts/mutation_panel.py` + panel JSON + freshness test (Sol's, verified) | `ci_gate --tier commit` hard-safety PASS in a clean worktree at the new HEAD with no declared-disable left that D-15 has satisfied; `--check` clean | OPEN |
| **W6** Measured, not flipped: the release door and the planner's full 1.12 m demand on the frozen corpora | `W6_VALUE_CHANGES_MEASURED.md` | research only (`nav-gen-attribution-1` harness + minival/panel runs in scratch) | a decision table for the owner: per value, strict/settled/unreachable/stalls, frozen-row moves, collisions, < 0.65 m exposure | OPEN |

## Standing constraints (unchanged from task_2 — verbatim there; the additions for wave B)

- All task_2 constraints apply (safety floors untouchable; `config.py` at the 1000-line ceiling; `pipeline.py` net-negative or leaf; zero `noqa`; every pytest through the guard; no `ci_gate.py` from executors; `TMPDIR` unset; owner facts; foreign folders never edited).
- **Owner-diff files (`runtime.py`, `brain/executive.py`, `gateway/*`, `bridge/*`, `control/*`, `navigation/grid_planner.py`, docs/, prompts/, `configs/robot.go2_edu_plus.yaml`):** a wave-B hunk may land there ONLY in a region the dirty diff does not touch (`git diff HEAD -- <file>` first; record the hunk headers you avoid); keep hunks small and self-contained; never reformat. At commit the integrator extracts wave-B hunks into a patch, `git apply --check`s it on a clean HEAD worktree, gates there, and stages with `git apply --cached` — the owner's/Sol's hunks never enter the index. If your change cannot be expressed as such a hunk, stop and write it up.
- **Re-freeze discipline (W4, W5):** the superseded artifact is kept beside the new one; the new artifact's provenance names the commit and the cause per moved row; a bridge file lists every episode whose digest moved with before/after fields; the pinned numbers in `ci_gate.py` are updated in the same commit; `tests/test_mutation_panel_freshness.py` may assert a new check only in the commit that ships the check and its panel (task_2 LIVE COLLISION rule).
- **Index:** regenerated in a clean worktree at HEAD after the wave commit (never at the dirty root; the tool reads working-tree content).

## Working agreement
As task_2: card → cited registers → code lines; `<CARD>_STATUS.md` incremental with RED/GREEN rows quoting the bar; no criterion moves; the verifier re-runs headline rows through the product caller and writes `AUDIT_<CARD>.md`; integrator gates once at close in a clean worktree with `PYTHONPATH` pinned.

## Integrator rules adopted before dispatch (parcel-fb, parcel-6c — 06:4x; binding, verbatim in substance)

1. **Develop in a worktree at HEAD, never in the dirty root.** Every executor works in its own `git worktree add --detach ~/.cache/parcel-0e/wb/<card> HEAD` with `ln -s <repo>/.parcel`, `PYTHONPATH=<wt>/src:<wt>`, `MUJOCO_GL=egl`, `TMPDIR` unset, and prints `parcel_robot.__file__` from the worktree in its STATUS pre-flight (drop `__pycache__` if it seeds). Its `git diff` IS the pure patch: `runtime.py` / `executive.py` hunks apply to HEAD by construction; the owner's/Sol's hunks are never in the file the executor edits. **If a hook cannot be written against HEAD because it needs Sol's `executive.py` hunks, the card is BLOCKED on the 28-file diff — record the exact symbol needed; do not work around it in the dirty tree.** At commit: `git diff --cached --stat` == the wave-B list exactly; for any path also dirty in the root, the staged blob is hash-compared to the gated worktree's blob.
2. **Re-freezes (W4 band, W5 minival, W5 panel) — E8 precedent, three conditions each:** (i) not a re-baseline of a red SAFETY result — collisions and false arrivals bit-identical or better, any check carried red DECLARED (the C0 mechanism); (ii) every moved row attributed to a named commit/card in the provenance string — the minival's four A2-clearance rows incl. the lost `object_relative-A-00` success recorded as a LOSS citing NAV-QUALITY §1.4, plus a379bf4's D-15 suffix, and SR 0.24 → 0.20 stated as the cost; (iii) the frozen `episode_digest` (the episode SET) never moves — only the report digest; **B32's `band_m` change touches 45/125 episode definitions = an episode-SET change ⇒ a new version label (v5) with bridge rows in `bridge_v3_v4.py`'s shape, not a v4 rewrite.**
3. **Sol's panel remediation rides only under four checks** in a clean worktree at HEAD + exactly its three files: (a) provenance keeps C0's sentences verbatim and each new row is justified with per-episode intervention counts showing the gate BINDS on it (`changed_nonzero > 0` per row); (b) the panel reproduces LIVE there (`run_panel` passed True, clean fields equal, `reactive_gate_disabled` killed through the new check); (c) the freshness test green incl. the widened undisableable list and the declared-disable ratchet; (d) `git hash-object` of the three files at verification time equals what is staged. Land with Sol's attribution in the message AND a board line Sol reads: "landed at <sha>, do not re-land; further changes are a new card".
4. **Register rows every card carries:** "tested at candidate sha X in worktree Y" (the committed state must be the tested state — the integrator re-runs each card's suite in the gated worktree at the commit candidate before pushing); for hooks in `runtime.py`/`executive.py`: "adjacent to <function>; the dirty root's hunks in this file are at <headers>, none overlap". W2 additionally: the R24 lock roster / nominal-stop digests green with zero re-pins; `narrate_event`'s OFF-path byte-identity survives the lane hunk; a queue-policy re-issue (new task, same digest → fires) tested beside the same-task re-issue (does not fire).
5. **Lenses:** parcel-fb — W4, W5 and the runtime arrival-authority composition (U32 territory); parcel-6c — W1, W2.

## Foreign edits seen in the root during wave B (07:4x)
`scripts/ci_gate.py` (+253/−5 at 07:00 — "checkout provenance: HEAD, index digest, manifest digest over tracked + non-ignored untracked bytes") and `evals/nav_instruct/results/ledger.jsonl` (+1 row at 03:15) are Sol's, not wave B's (every W-executor's worktree is clean of root edits). The close gate for this wave runs the worktree's HEAD `ci_gate.py`; Sol's gate change is not in the wave commit and is listed for the owner beside the panel remediation. **W6 landed (ACCEPT as measurement, `AUDIT_W6.md`): V1 door ON — frozen corpora structurally blind (watchdog never fires within the 200-step budget), do not enable until a corpus can exercise it; V2 1.12 m demand — NO (< 0.65 m exposure 1 → 4, worst 0.5837 m below the stop band; 34/80 frozen demo rows move).**

## W5 Part B verdict + two findings (07:5x)
- **Sol's v4 panel remediation: NOT LANDED.** (a) provenance additive, C0's sentences intact; per-episode counts at `clean_run.reactive_gate_coverage.per_episode` (Sol's four rows: changed_nonzero 78/18/14/31); (b) live reproduction at HEAD FAILS on one episode — `nav-region_goal-D-15`: Sol 88 calls / 0 zeroed vs HEAD 200 calls / **105 zeroed** (= C0's own provenance number), so **Sol's panel was generated on the dirty tree (`grid_planner.py`)**; its largest witness `region_goal-C-11` measures `changed_nonzero = 0` at HEAD (vindicates Amendment A2); (c) freshness 2 failed / 9 passed (RED direction, "not a licence to regenerate"); (d) hashes stable 06:3x → 07:2x. A fourth tracked file belongs to the remediation (`scrum/20260829/task_2/C0_SOL_REMEDIATION.md`, +37, cited by the provenance). **Board line for Sol: the v4 design did not reproduce live at HEAD; its check (`reactive_gate_exercised`), per-episode counters and `passed = not survivors and not equivalent` are adopted for the v5 panel with Sol's attribution; the artifact and the row selection are not.**
- **Finding — budget policy:** the committed v4 minival row is `budget_policy: scaled-path-v1` while the CLI default is `fixed`; `021b67ab…` was measured under `fixed`, so a naive diff reports 15 rows. Under the committed policy the code-axis digest at HEAD is **`a47b27bf…`**: 7 rows with any delta — 5 outcome moves (A2 ×4 incl. A-00 as a LOSS; a379bf4 ×1; `object_relative-D-15`'s terminal is budget-dependent) + 2 trace-length-only. All wave-B minival comparisons use the committed policy from here on.
- **Rule — one baseline row:** `run_nav_instruct_v1.py:514` marks any `--mode baseline` run `frozen_baseline: True` and `ci_gate._latest_frozen_baseline_row` takes the LAST one — a re-freeze appends exactly ONE full-matrix baseline row (never a minival row) or the hard gate silently retargets.
- 14 artifacts a v5 re-freeze must touch are listed in `W5_STATUS.md`; the `ci_gate.py` pin regions do not overlap the root's foreign hunks.
- Integrator additions (08:0x): the fourth file `scrum/20260829/task_2/C0_SOL_REMEDIATION.md` is Sol's and stays unlanded with the rest. **Budget-policy rules:** the recipe of record is `--budget-policy scaled-path-v1 --max-steps 200 --seed 20260804` (research/20260824/nav-quality/RESULTS.md reproduction section — the precedent; cite it in the bridge); (i) every wave-B minival/matrix run states the policy explicitly on the CLI, never the default, and the STATUS quotes the exact command; (ii) `tests/test_nav_instruct_digest_recipe.py` must encode the policy in the recipe it pins — if it cannot tell `fixed` from `scaled-path-v1`, that is the defect that produced two digests tonight: W5 fixes it (a test); (iii) the bridge records BOTH v4-at-HEAD digests (`021b67ab…` fixed / `a47b27bf…` scaled-path-v1) with the 7-row table; object_relative-D-15's budget-dependent terminal is recorded as budget-dependent, not attributed to a commit. **Ledger rules:** every wave-B run is `--no-ledger` except the ONE v5 re-freeze run; at commit the ledger diff shows exactly one appended row whose digest equals the bridge's v5 digest, and the STATUS names the run that wrote it. Backlog (owner): freezing must require an explicit `--freeze` flag and `_latest_frozen_baseline_row` should select by version, not recency. `scripts/ci_gate.py` is hash-compared at commit (a gate file; Sol's foreign +253 hunk lives only in the root).

## W5 §6.2 finding and the integrator decision (08:2x)
- **Finding:** the v4 FULL matrix at HEAD under the recipe of record (125 rows, `--no-ledger`) reads `collision_total 0` but **`false_arrival 1`** — `nav-region_goal-B-09-3ee156e4` "walk onto the sidewalk": `arrived_verified`, system True / scorer False, `claim_without_predicate` at dtg 4.77 m (the E7/v2 class), **outside the 25-row minival** — so the frozen slice reads 0 and no gate has ever seen it. Every `frozen_baseline` row ever appended is a minival (8/8); a minival→matrix switch would move the frozen `episode_digest` (`4113607b…` → `e7c302dd…`) without any episode definition changing — a re-scoping rule 2(iii) forbids — and would redden `PINNED_FROZEN_FALSE_ARRIVAL = 0` (rule 2 never relaxes it). Cross-check for free: the 125-row `episode_digest` equals Sol's `PANEL_MATRIX_DIGEST` — the panel's substrate pin is right even though its artifact is not.
- **Decision (verifier, rules intact):** the gated frozen row stays a **minival v5** (exactly one ledger row); the v5 full matrix is landed `--no-ledger` as a reported artifact in the bridge/STATUS; **B-09 is a NAMED FINDING** on this board and in the bridge, and W4 reports it under v5 by name (region_goal is the family the band ∩ support change touches). Keeping the matrix as the gated row would require moving ruling 6 — a value change the task reading excludes.
- **Recipe test fixed (W5, `tests/test_nav_instruct_digest_recipe.py`, +78/−0, 9 passed):** the defect was identity, not discrimination — `budget_policy` was already inside the hashed body, but nothing named which policy the published digests belong to; now `FROZEN_ROW_BUDGET_POLICY / MAX_STEPS / SEED` constants, a policy-identity cell that reddens on a silent-collapse refactor, and a per-episode-budget cell; published digests byte-identical.

## Integrator ruling on the gated row + three additions (08:4x) — adopted
- Gated frozen row stays a **minival v5**; the v5 full matrix is a `--no-ledger` reported artifact in the bridge.
- **B-09 is not new — dated:** `nav-region_goal-B-09-3ee156e4` is `research/20260824/nav-quality/RESULTS.md` §5.1 (08-24: "the never-run 125-episode matrix holds a false arrival 4.78 m outside the sidewalk, invisible to the 25-episode pin") and is already the owner's PRE-MOUNT-CLOSE-1 item 7 ("false-arrival pin over the full matrix"); two independent instruments now agree on the row. W5's pin of its full v4 state: polygon goal `[[−8,2.2],[8,2.2],[8,4.2],[−8,4.2]]`, `band_m: null`, body tracking cleanly (`err=0.0`, `status=planned|clear`, 369 steps of budget left) then `semantic_stop_requested` at 4.77 m and the terminal contract VERIFIES the claim — an arrival-authority failure on a polygon goal with no band (close-out item 3), not a navigation failure.
- **B-09 is a W4 ACCEPTANCE row by principle, not special-casing:** its label is the card — `claim_without_predicate`; under `ArrivalReceipt` a claim with no predicate cannot produce a receipt and missing receipt = not arrived. Row: "v5 full matrix under W4: `nav-region_goal-B-09` false_arrival = 0 by receipt absence; the matrix false_arrival total reported". If it still false-arrives, `ArrivalReceipt` has a second author and W4 is not done. No per-episode exception anywhere in code. Note: B-09 is a `region_goal` polygon, outside `object_near_goal_region`'s band — it is in scope through the receipt rule, so W4's receipt must cover polygon goals too.
- **The gate must never be blind to the matrix again — ADDITIVE pin (W5):** a second, matrix-scoped pinned artifact (the v5 full-matrix `--no-ledger` result, its own digest recipe, its own `PINNED_MATRIX_FALSE_ARRIVAL`) read by `evaluate_hard_safety` beside the minival row; value 0 if W4 clears B-09 (tightening), else 1 with the C0 DECLARED mechanism (provenance names the row and the check; a test asserts the declaration) — never silent; the minival pin stays exactly where it is.
- W5's v5 runbook (recipe of record stated once): (1) minival v5 `--no-ledger` dry run; (2) full matrix v5 `--per-family 25 --no-ledger` (reported artifact incl. B-09 by name); (3) minival v5 `--refreeze-provenance` — the ONE gated row (ledger `+1`); (4) panel on v5 from run 2's coverage scan — LAST. ~50 min after W4's patch.
- **A4 refined (W5 proposal, adopted 09:0x, integrator concurrence requested):** (1) the matrix declared mechanism is ROW-identified — `evaluate_hard_safety` compares the false-arrival episode-id SET against the set named in the artifact's provenance and reddens on any undeclared id; `PINNED_MATRIX_FALSE_ARRIVAL` is an upper bound only (a bare count would stay green if B-09 cleared and another row started claiming). (2) Freshness: a pinned matrix artifact read from disk is a derived artifact with no freshness check — the E7 class Part B just re-demonstrated; live re-derivation costs 238 s (panel ~4 s), so the commit tier reads the tracked artifact for the pin under a `DIGEST_SENTINELS` entry and prints "matrix: committed artifact, NOT re-derived (238 s; nightly)" (PRE-MOUNT-CLOSE-1 item 7(a) verbatim), and the nightly tier re-derives via `MATRIX_FRESHNESS_NODE_IDS` → `tests/test_nav_instruct_matrix_freshness.py` (collisions, false-arrival id set, authority histogram). Artifact `nav-instruct-v1-matrix-v5-<ts>.json`, tracked, carries no `frozen_baseline` key (can never become the gated row); its `episode_digest` `e7c302dd…` equals Sol's `PANEL_MATRIX_DIGEST`. B-09 verified stable across a379bf4 and wave A (dtg 4.782 → 4.7739 m; §5.1 confirmed line 695).
- **A4 final (integrator concurred, 09:1x) — two commit-tier additions:** (a) re-run LIVE only the DECLARED false-arrival rows at commit tier (today B-09, ~2–4 s) and assert each still produces the declared outcome — a flip to another reason, or ceasing to be a false arrival while still declared, reddens immediately (greener direction ⇒ withdraw the declaration); (b) assert the matrix artifact's `episode_digest` equals the v5 generator's `_matrix_digest` over the episode definitions in the tree (no run) so the artifact cannot drift from the set it certifies. Net: commit tier certifies identity + the declared set live and prints "NOT re-derived" for the rest; nightly certifies the whole matrix live; nothing is read from disk without being named as such.
- **A4 checks measured before build (W5, 09:3x):** (a) declared-row live re-run of B-09 is BIT-IDENTICAL solo vs in-matrix (all twelve `EpisodeScore` fields incl. dtg 4.77391552924085) in 1.30 s — the HeadlessCityWorld scan-RNG order-dependence Sol recorded affects gate call counts, not this row's outcome; any future declared row is re-verified the same way before it is trusted at commit tier; (b) tree `_matrix_digest` == artifact `episode_digest` `e7c302dd…` in 0.001 s. Commit-tier cost of the matrix block ≈ 1.3 s; the 238 s full re-derivation is nightly only. Wiring trap recorded: `_pytest_gate` takes `tier` only as a label and node-id selections run with no `-m` filter, so `MATRIX_FRESHNESS_NODE_IDS` goes ONLY in `run_nightly_tier()` (the commit-tier checks live inside `evaluate_hard_safety` next to `_panel_safety_fields_live`), timeout 1200 s; every matrix-re-running test cell is `@pytest.mark.slow`.

## Next cards surfaced by wave B (not built here)
- **MB-3 contract extension:** a `candidates` slot on `ask_clarify` (4/5 claim-free clarifications still REJECT on `foreign_place_name` because the frozen contract has no goal slot for them), and acts for the kinds MB-2 has none for (block clear, battery, pace, curiosity) so those stop taking the model path — a contract change under MB-1's scorer, re-measured on the MB-1/MB-2 corpora, never a widening of frozen evidence.
- **W2 follow-up F1 (in flight):** `lineage` required/non-empty at the plan-acceptance hook; `narrate_event` returns the spoken sentence (no lock-external read-back).

## M1 — merge card (12:1x)
Each wave-B patch applies to HEAD by construction (rule 1), but they overlap with EACH OTHER: dry run on the gate worktree (HEAD + W2 + W3) — W1's patch fails in `runtime.py:3547` (`_accept_plan`: W1 binds the queue lineage where W2 installs the plan-acceptance hook that consumes it); W4's patch fails in `pipeline.py:1156`, `poi_admission.py:411`, `perception/city_semantics.py:21`, `simulation/headless_city.py:1071` (W3 threads `world_identity` through the same seams W4 extends with the settle/receipt fields). A dedicated MERGE executor (M1, Opus) integrates W1 + W4 onto the gate worktree preserving both sides' semantics, proves it with every card's suites plus one integration test (a queue steering decision yields a `plan_accepted` receipt with lineage QUEUE at the whisperer), and writes `M1_STATUS.md` with each conflict and resolution. W5's patch (which already carries W4) is layered afterwards as "W5 minus W4". The close gate runs on that merged tree; the committed state is the tested state.
- **M1 sequencing rules (integrator, 12:2x, adopted):** (1) W4-F4 is part of what M1 merges — it lands as the first patch on the merged tree and M1's FULL proof (every card's suite + ratchets + the integration row) runs after it; M1_STATUS carries "merge (pre-F4)" and "merge + F4 (final)" tables. (2) W5's frozen artifacts are generated LAST on the merged gate worktree — the v5 baseline ledger row, the v5 panel (witness rows selected by measurement ON THAT TREE), the matrix artifact + declared set, the digest-recipe published digests, the ci_gate pins; anything W5 generates in its own worktree is a preview (b87c079's and Sol's lesson: a panel generated on a tree that isn't the committed one). The bridge derivation is tree-independent and stands. The integrator's lens on the merged tree at the commit candidate: live `run_panel`, minival + matrix under the recipe of record, the receipt suites + F4's wiring test, both ratchets, `--check`.

## W1 landed (13:0x) — rulings
Admission 31/32 = 0.969 GREEN; owner-referring amendments 6/6; cue-stripped re-issues 8/8; return 13/13; refused-and-continued 7 → 0; terminal false arrivals 3 → 0; classifier port exact. **Bar 2 (amended success ≥ 0.8) = 21/28 = 0.75, RED exactly at the pre-registered ceiling** — `bench 0/7`, every other goal 21/21; the bench legs are W4's arrival-authority class → **re-measured on the MERGED tree (W1 + W4 + F4) at the commit candidate: a NAV-INT-1 tier run on the gate worktree is the wave's headline instrument and is scheduled after M1 + F4 are green (≈ 2 h, single sim, concurrent with W5's artifact generation).** **Bar 3 (resume path ratio ≤ 1.1× vs the straight-line oracle) = 1.73, RED as written / MET on intent:** the metric pays for refusal (a refused interruption never detours); the resume/from-rest quotient on the three hold rows (zero queue work) is 0.980 — the plan queue adds no measurable path overhead; the bar is re-specified for the next tier as `hold-row quotient ≤ 1.1`. Harness correction in `nav-interrupt-1/run.py` (`_false_arrival_in_window` matches task id AND owning revision; window false arrivals 3 → 0 on re-score).
- **W1 lens (parcel-6c, 13:2x):** CONCUR / ACCEPT + required **W1-F1** — `_resume_queued_parent` restores the channel before the executive re-bind; on refusal a running channel sits over a non-running record and a second start follows (re-bind first or re-pause on refusal; refusal-after-restore test). M1's merged-tree integration test is REQUIRED (neither worktree holds both sides of the lineage contract). Next-tier bar 3: hold-row quotient ≤ 1.1 (parking is free) AND resume rows ≤ 1.2 × the two-leg oracle (goal1 → goal2 → goal1).
- **W1 closed (14:1x):** final patch `8fe1a7a4…` (+407/−11 + plan_queue.py 845 + tests 955); F1 (resume refusal path) in with a test proven to fail without it; e2e's one red pre-existing in all four cells; executor's 442/2/0 on pristine HEAD; verifier's 183 passed. Standing merge hazard for the OWNER: all three `executive.py` hunks sit inside Sol's uncommitted regions — if that diff lands, `_notify_plan_activated` must move into `_activate_replacement_impl` or deferred replacements activate with no `plan_accepted` receipt. **W4 closed (14:0x):** F4 `LegIdentity` + wiring tests; latch defect fixed; tier 0/80 disagreements, bench 0/29. **M1 fresh pass signalled** with W1.final + W4.final.

## M1 landed (14:5x) — merged gate tree proven
`~/.cache/parcel-0e/wb/gate` = HEAD c96ac34 + W1 (final) + W2 + W3 + W4 (final incl. F4/F5) + W6; 43 files +3326/−283 + 44 new. Nine conflicts resolved with both sides preserved (`M1_STATUS.md` table): the semantic one — W1's `_bind_plan_lineage` now RETURNS the queue policy's lineage and `_accept_plan` overrides W2's door lineage when the policy answered (queue answer wins; the door cannot see `queue`); `pipeline.py:1169` metadata = exact union of W3's `identity_source` and W4's `arrival_goal_region`; `city_semantics` union minus the symbol W3 moved; a MERGE-CREATED DEC-0 red (`extract_city_semantics` 97 → 103 lines, ceiling 100) fixed by extracting `_object_track` (no baseline edit); five `_narrate_mission_terminal(state="arrived")` test sites now hand over a real receipt + `LegIdentity` (two were red, three had silently passed on the failure branch). Proof: 671 passed / 1 xpassed / 0 failed across 26 files incl. `test_wave_b_integration` (queue admission → exactly one `plan_accepted` with the QUEUE rendering "Okay, I'll check bench after that."; parent resume → `resume_offer`/`plan_resumed`; KEEP fires nothing), ratchets green, ruff clean on 55 files, 0 `noqa`, `config.py` 1000, `pipeline.py` 7221 (+10 comment only — recorded), the single expected red = W5's sentinel literal. Process fault disclosed by M1: a measurement script ran `git stash` in pass A (popped within one command; pass B rebuilt from patches). **Now on that tree: T1 (the merged-tree NAV-INT-1 tier for W1's bar 2) and W5's final pass (artifacts generated there).**
- **Process rule (integrator's audit of the main repo, 15:1x):** no `git stash` anywhere by executors — a measurement that needs a clean state builds a worktree. For the record: the main repo's only reflog event during the wave, `704ba5c HEAD@{01:04:15}: reset: moving to HEAD`, is the verifier's mixed `git reset -q` (index clear before hash-object staging for b87c079; working files untouched); `git stash list` is empty; the foreign diff is intact (116 files, anchors' mtimes unchanged). M1's disclosed stash/pop location is being confirmed from M1 directly and recorded verbatim.
- **M1 stash — contained (facts from M1, verified from evidence):** directory `~/.cache/parcel-0e/wb/gate` (the stash commit `69624bb1…` reads `WIP on (no branch)`; the main repo on `main` would read `WIP on main`; the foreign diff intact at 116 files); 2026-08-30 08:31:58 EDT, popped within the next command (`git stash pop` dropped the entry; `refs/stash` gone); pass B rebuilt the tree from patches so nothing of it survives.

## W5 landed on the merged tree (15:4x) — ACCEPT
v5 minival baseline (report `53eec205…`, episode `2822ebfd…`; ONE ledger row, SR 0.24 = W4's D-15, A5 history in the provenance) and the v5 matrix artifact (report `041d9bdd…`, episode `5ea2cd93…`; `PINNED_MATRIX_FALSE_ARRIVAL = 0`, empty declared set; commit tier = identity + declared rows live, "NOT re-derived (238 s; nightly)"; nightly = full re-derivation) generated ON the gate tree, bit-identical to W5's preview — W1/W2/W3/W6 move the nav evidence by zero. v5 mutation panel by A2 measurement: 8/8 killed, {agreement 9}, the C0 declared disable withdrawn by regeneration; witness rows by per-row counts (C-11 96, obj_rel C-11 31, B-09 20, D-17 17). An **eighth mutant** `arrival_receipt_bypassed` (the B-09 class) added because W4's receipt became the single arrival predicate and no existing mutant exercised `no_false_arrival` — accepted under the E3 rule (shipped with the check and the panel that prove it). Hard-stop coverage remains unproven (`translation_zeroed` 0 on all 125 v5 rows) — in the provenance, not claimed. Sentinel literal 4 → 6; four suites 141/0; whole gate tree vs HEAD 48 files +5686/−431 + new. **Close gate running on the gate worktree; T1 tier running.**
- **W5-F1 (16:0x):** the integrator concurs on the eighth mutant with four conditions (product-predicate patch site; killed by identity refusal on a named witness row; `_MINIMUM_KILLED` stays a floor of 6; `phantom_view_consistent`'s channel change explained as a product improvement) and one provenance addition (where the hard stop IS certified: `core/hard_stop.py` tests, A3 latch, A6 stop path). F1 edits the provenance constant → regenerates the v5 panel on the gate tree → freshness → re-pin; then a second close-gate run. The committed state stays the tested state.

## Close gate #1 on the merged tree (16:2x; `ci_gate --tier commit`, gate worktree, PYTHONPATH pinned, guard)
**PASS:** hard-safety (frozen baseline `nav-instruct-v1-baseline-v5-20260830T135548Z`: collisions 0, false_arrival 0; mutation panel clean + fresh), release-parity (106 assets), assertion-evals, tier-coverage (11,261 = 11,172 commit + 89 nightly, no orphans/overlap), owner-store-isolation (5/1 skip), model-off, unitree-assets. **RED, attributed:** ruff 73 new — all committed research files (pre-existing at HEAD); default-suite **11,077 passed / 8 failed** + release-parity-integrity 1: (1–3) `test_nav_instruct_scene_gen::…defects_its_cards_declare`, `test_v4s_search_cells::test_phantom_mutant_is_registered_last`, `…committed_panel_gained_exactly_one_row_and_kills_through_false_arrival` — the eighth mutant's declarations/order/pin → **W5-F1**; (4) `test_dec0_debt_ratchet::test_no_new_oversized_module` — `scripts/mutation_panel.py` 1,095 lines → **W5-F1** extracts a leaf (≤ 600, M6); (5) `…test_no_new_card_markers` 176 → 177 — the `# ---- CARD W5 / amendment A4` marker at `ci_gate.py:2596` → **W5-F1** dissolves it (M7); (6–7) `test_companion_brain_eval::test_full_frozen_brain_boundary_suite…` and `…positive_cases_reach_only_bounded_semantic_callbacks` — `run_suite()` 15/15 at HEAD, 12/15 merged (`sidewalk_inside_boundary`, `lamppost_near_boundary`, `correction_at_checkpoint_boundary` no longer reach `succeeded`) — the brain eval's venue never cuts an ArrivalReceipt: the second-author class on the companion-brain path → **W4-F6** (fix at the eval's venue by principle); (8) `test_release_parity::test_effective_config_is_equal_under_source_and_packaged_roots` — the prompts leak by construction (owner's dirty prompts via the editable install; not wave B's). **Close gate #2 runs after W5-F1 + W4-F6; the committed state is the tested state.**
- **Rules for the two fixes (integrator, 16:4x, adopted):** W4-F6 — the brain eval's venue cuts its receipt THROUGH the product function (`arrival_receipt(...)` from its own terminal observations with a LegIdentity built from the leg it issued), never a venue-local "succeeded" from status, never a product relaxation; the three cases are SPLIT and reported by name — (i) reach `succeeded` once the venue cuts a receipt = harness second-author defect, fixed; (ii) the receipt legitimately refuses = a finding — the case's expectation is updated with the refusal token quoted, nothing widened; a pure-stub venue cannot claim arrival (no exception for evals). W5-F1 — the leaf extraction moves mutants and/or reporting, never `run_panel` / `harness_checks` / `clean_safety_fields` (the freshness tests import them by name).
- **W4-F6 landed (16:5x):** all three companion-brain cases refused on `no_arrival_leg` — (i) ×3, (ii) none; `run_brain_v1.py`'s venue now cuts the receipt through `arrival_receipt()` against the leg it issued; `run_suite()` 15/15; per-case metrics byte-identical to pristine HEAD; frozen artifacts untouched. Waiting: W5-F1, T1 → close gate #2.

## T1 landed (17:1x) — the merged-tree tier found a W4-F4 defect
Admission 0.969, both live defects green, return 5/5, refused-and-continued 0, false arrivals 0 (terminal and switch-window, live revision-aware), collisions 0, hold-row quotient 0.969, resume rows vs the two-leg oracle **1.079 ≤ 1.2** (GREEN on the refined bar), bench `system_failed_but_arrived` 0/29. **But amended success fell to 16/28 and authority disagreements read 7/85: F-T1-1** — F4 froze the leg's `region_id` at the first commit and perception's lock-on refines the instance mid-leg, so 24 legitimate terminal receipts were refused as "another place". W4's own tier never ran F4 (imported before it landed). **W4-F7 dispatched** (commitment chain per generation; eval runners keep the answer-key chain so B-09 stays `wrong_instance`); **T1 re-runs after F7; close gate #2 after F1 + F7 + T1.** Expected after F7: bar 2 = W1's 21/28 (bench 0/7 = the pre-registered ceiling, confirmed).
- **U32 ruling on F-T1-1 (integrator, 17:3x, adopted → W4-F7 tightened):** identity = the leg's own COMMITMENTS, never the label (label-level would re-admit B-09); accept only the CURRENT commitment — the leg carries the chain (`committed_region_ids`) and a `commitment_index` incremented on every refinement, the receipt is cut under the index current at the cut, `is_for` requires same generation AND equal commitment index (a receipt cut before a refinement is refused `arrival_receipt_superseded` — the stale class inside one generation); refinements are PRODUCT-driven only (perception lock-on through the K0 seam; nothing scorer/harness-side may append); eval runners keep a one-element answer-key chain so B-09 cannot drift green. Six test rows through every consumer. **Rule for tier rows:** every tier row is stamped with the patch sha it ran under, or it isn't a row (W4's F1 tier imported modules before F4 landed — token 0× there, 24× in T1). After F7, AUDIT_T1 re-scores the 24 refused receipts and the 7/85 by name (expected 21/28; residual = B32's honest bench strip, not a new class).
- **W4-F7 landed (17:5x)** under the U32 rule (current commitment only; `arrival_receipt_superseded`; product-driven refinements; six consumer rows; B-09 still `wrong_instance`; minival/matrix unchanged). **T1 re-run dispatched, stamped `bfc72ae269a2cce5`** (tracked diff + untracked contents of the gate worktree at dispatch), outputs `m1-merged-f7-*`, with the by-name re-score of the 24 refused receipts and the 7/85. Close gate #2 after W5-F1 + the T1 re-run.
- **W5-F1 landed (18:2x):** the eighth mutant re-targeted to `arrival_receipt.receipt_refusal` (the `is_for` target had become EQUIVALENT under F4/F5 — caught by the `not equivalent` rule); coverage limit recorded (the two product consumers that import `receipt_refusal` directly are not reached by a module-global patch); conditions (b)(c)(d) + the hard-stop certification sentence done; three panel tests closed by declaration / append-only rule / channel handover; M6 split (`mutation_panel_mutations.py` 373 lines) and M7 marker dissolved; 206 passed. **Close gate #2 running** (stamp in `~/.cache/parcel-0e/wb/gate2.stamp.txt`; T1's in-progress `m1-merged-f7-*` outputs excluded from the stamp); **T1 re-run in progress.**
- **(a) ruling (18:4x):** no product edit for the mutant's sake; coverage split by instrument (panel = eval evidence; wiring suite = product consumers) written into the provenance as **W5-F2** after gate #2 reports; **close gate #3** after F2 + T1's stamped re-run; then the candidate.

## Close gate #2 on the merged tree (19:0x; stamp `0524a7229c194b09` excl. T1's in-progress outputs)
**Every hard row PASS:** hard-safety (frozen baseline `…-v5-20260830T135548Z` collisions 0 / false_arrival 0; mutation panel clean + fresh at commit tier), release-parity (106), assertion-evals, tier-coverage (11,268 = 11,179 commit + 89 nightly, no orphans/overlap), owner-store-isolation, model-off, unitree-assets. **default-suite 11,091 passed / 1 failed** — the single red is `test_release_parity::test_effective_config_is_equal_under_source_and_packaged_roots`, the prompts leak by construction (owner's dirty prompts via the editable install; not wave B's); ruff RED = 73 in committed research files (pre-existing at HEAD). All eight of gate #1's reds closed (W5-F1 ×5, W4-F6 ×2, the eighth mutant's tests). **W5-F2 (provenance sentences → panel regeneration) now running; T1's stamped re-run in its episodes; close gate #3 after both, then the candidate.**

## T1 re-run after F7 (19:2x, stamped `bfc72ae2…`) — the wave's headline instrument
Admission 0.969 · **amended success 21/28 exactly at the pre-registered ceiling (bench 0/7; every other goal 21/21; authorities agree leg for leg)** · **authority disagreements 0/85** (strict and incl. tolerated) · false arrivals 0 (terminal and switch-window) · collisions 0 · owner-referring 6/6 · cue-stripped 8/8 · return 13/13 · refused-and-continued 0 · the 24 receipts F-T1-1 refused → all verified, `superseded` 0 (the U32 tightening removed 24 false refusals and added none) · hold quotient 0.98, median two-leg 1.06, all-rows two-leg 1.09 (the 8-row mean 1.37 is two pre-existing outliers). Waiting: W5-F2 → close gate #3 → candidate.

## W5-F2 verified in the gate tree + close gate #3 launched (17:09Z, verifier)
F2: `mutation_panel.json` `generated_at 20260830T164423Z`, `passed True`, 8/8 killed, survivors `[]`, equivalent `[]`, both capitalised provenance sentences present verbatim (episode_set_provenance 13,063 chars; the "not a choice of patch site" clause corrected), ledger `+1/−0`, DIGEST_SENTINELS 6/6 byte-identical, the four v5 digests unchanged (executor's 177 passed on freshness/ci_gate/v4s/scene_gen accepted with the executor's stamp). Gate #3 stamp of the final bytes (`git diff HEAD` + every untracked file's contents, T1's nine `m1-merged-*` outputs and the F2 panel included, `.parcel` symlink excluded): **`5a656e51310a3b5a`** (`~/.cache/parcel-0e/wb/gate3.stamp.txt`); invocation `PYTHONPATH=$G/src:$G MUJOCO_GL=egl PARCEL_XDIST_WORKERS=8 PARCEL_MEMORY_PATH=~/.cache/parcel-0e/gate3-mem.sqlite3`, `TMPDIR` unset, through `pytest_guard.sh --label parcel-0e-gate-waveB3`, log `~/.cache/parcel-0e/gate-waveB3.log`. In parallel: a read-only adversarial review workflow (7 dimensions × 2 refuters per finding) over the candidate diff — no pytest, no edits; its confirmed findings, if any, are folded into the close-out before the commit. T1's nine `m1-merged-*` outputs (~1.2 MB) are committed like wave A's `nav-interrupt-1` jsonl (same size class; no ignore/pin).

## Close gate #3 — RED BY THE VERIFIER'S OWN ENVIRONMENT, withdrawn; gate #3b launched (17:2x)
Gate #3 (stamp `5a656e51310a3b5a`) reported 4 hard reds: ruff (committed research, known), release-parity-integrity (the prompts leak, known), **owner-store-isolation 1 failed** and **default-suite 214 failed** — the new two are self-inflicted: the verifier exported `PARCEL_MEMORY_PATH=~/.cache/parcel-0e/gate3-mem.sqlite3`, so eight xdist workers shared one live sqlite store (409,600 B written at 17:15Z) and every store/replay/quarantine test that reads that variable (`test_conversation_store`, `test_realtime_corpus_replay`, `test_owner_store_isolation::test_quarantine_defaults_to_dry_run_and_never_deletes`) failed. Gate #2 on the same bytes (minus F2's panel and T1's outputs) ran without the variable: 11,091/1. Not a tree defect; the log is kept as `gate-waveB3-memorypath-red.log`. **Gate #3b** re-runs the identical bytes with gate #2's exact environment (`PYTHONPATH` pinned, `MUJOCO_GL=egl`, `PARCEL_XDIST_WORKERS=8`, `TMPDIR`/`PARCEL_MEMORY_*` unset), log `gate-waveB3b.log`; its rows are the close rows.

## Close gate #3b — the CLOSE ROWS (17:16–17:21Z, gate worktree, PYTHONPATH pinned, stamp `5a656e51310a3b5a`, TMPDIR/PARCEL_MEMORY_* unset, 8 workers through pytest_guard.sh, 279 s)
- hard-safety **PASS** — nav frozen baseline `nav-instruct-v1-baseline-v5-20260830T135548Z` collisions=0 false_arrival=0 · mutation panel clean collisions=0 no_false_arrival=True · **panel freshness: committed fields reproduce live = True** · nav matrix v5 n=125 collisions=0 false_arrival=0 declared=[] · matrix episode-set digest reproduces from the tree · follow-bench 7 rows all 0 · walk_with_me all 0
- unitree-assets PASS · release-parity PASS (106 byte-identical) · assertion-evals PASS · tier-coverage PASS (11268 = 11179 + 89, no orphans) · model-off-non-inferiority PASS 23/23 · **owner-store-isolation PASS 5/5** · default-suite **11,091 passed / 1 failed** (`test_effective_config_is_equal_under_source_and_packaged_roots` — the prompts leak, attributed to the root's uncommitted `prompts/system/planner*.md`; not the wave's) · serial 12 passed
- ruff FAIL — 73 new, all under `research/20260828–29` committed scripts; baseline 7 unchanged; zero noqa · release-parity-integrity FAIL — the same prompts-leak probe
- Identical shape to gate #2 (11,091/1). Gate #3's extra reds are confirmed environmental (withdrawn above). **The candidate is closed on these rows**; it goes to parcel-fb for the lens, then the commit stages the gate bytes.

## Owner request 18:xx — assessment of Sol's 2026-08-30 recommendation (verifier)
Owner asked for an assessment of `research/20260829/SOL_ULTRA_POSTFIX_AUDIT.md` + `scrum/20260829/task_1/README.md` against the prototype goal. Verification workflow `wf_db7d8e90-bad` (31 read-only agents; targeted guarded runs only): every systemd / Model-B / revision-lock claim HOLDS as worded; 14 and 91 are stale counts (21 and 99 today, all green); 19/19 and the 11,417 = 11,330 + 87 collection reproduce; **the audit's "ruff 72/72/0 new" is green only because `scripts/ci_ruff_baseline.json` was regenerated 7→72 inside the same uncommitted change (68 research/20260829 entries) — ratchet rule broken (HIGH)**; upheld MEDIUM findings: narration lane latches after 256 tasks/process (`execution_narrative.py:420`), terminal task-id resubmission latches the lane (`:418`), `--profile` precedence has no consumer and no `parcel-runtime` entry point exists (`config.py:775`), the change is 116 tracked files + 21 new modules not "~28". **Root ∩ wave-B overlap = 12 paths** (listed in the commit message), including a COMPETING frozen ledger row (root: agreement 19/disagreement 5; wave: v5 21/4) — the gate takes the last row (N47) → owner/Sol must drop it or the gate must select by version. Assessment delivered as the artifact "The Next Rung"; recommendation: adopt safety core + receipt contract + three fixes/service graph (after baseline restore + rebase) + acoustics; park steps 2/3/6; decline step 5 and HIL-as-next-rung (no hardware; BOM mismatch AGX 64 GB vs Orin NX 16 GB); next rung = exemplar transaction end-to-end in sim with voice through the array + perception/owner-model track + panel hard-stop witness.

## Candidate review workflow (wf_bd309b9c-13e, read-only, 7 finders × 2 refuters) — interim, 19:xx — COMMIT HELD FOR A FIX PASS
23 findings; 13/46 verdicts in. Confirmed by both refuters so far (introduced by the candidate): **[HIGH] `navigation/poi_admission.py:570` two-worlds — `poi_goal_metadata` reads `active_scene_instances()` with no identity, so a navigator with explicit identity A commits world B's crosswalk polygon as `arrival_goal_region` (M1 composition of W3 + W4)**; **[HIGH] `tests/test_plan_queue.py:748` — the only product caller of `_resume_queued_parent` (`_step_brain`'s poll loop, runtime.py:5244) is exercised by no test (every test hand-calls the method)**; **[MEDIUM] `tests/test_nav_instruct_matrix_freshness.py:122/139` — writes a 3.3 MB report into un-ignored `REPO/.pytest-matrix-freshness/` per run and reads the newest file, not its own; two 3,344,398-byte residue files sit in the candidate's untracked set and the staging script would have committed them (now excluded; the test must write to scratch and the dir must be ignored)**; [LOW] `scripts/mutation_panel.py:775` provenance text says "pinned 125-row v4 matrix" for a v5 panel; [LOW] runtime.py:7436 `_behavior_generation` vs `_generation` stamp on the already-there receipt. Pending verdicts on: runtime.py:13029 (product-path LegIdentity built from the receipt's own inputs → F4/F7 refusals unreachable on the product path), runtime.py:3593 (revise during a queued child binds to the parked parent), runtime.py:5244 (child failed inside `tick()` never resumes the parent), runtime.py:17220 (owner redirect voiced as "something stayed in the way"), runtime.py:17936 (deferred correction never acknowledged — no activation caller), test_runtime.py:1235 (honest arrival at an object-class POI without `arrival_goal_region` becomes a narrated failure), run_brain_v1.py:145 (`inside_region=True` bypasses NO_REGION). **Decision: per the owner's "fix the issues first", confirmed HIGHs and any regression against HEAD are fixed in the gate worktree by an Opus executor (card F-M2), then gate #4 + the integrator's delta lens, then the commit; confirmed mediums/lows that are not regressions become attributed follow-up cards.** F-M2 dispatched on the three confirmed items now; the pending HIGHs are appended when their verdicts land.

## W4-F8 (integrator's confirmation of review finding runtime.py:13029) — dispatched 19:xx; GO WITHDRAWN
parcel-fb read the finding at source: `_navigation_leg` is assigned at ONE site — the receipt cut (~:12981) — from the same three inputs the receipt is stamped with, and `_navigation_receipt` only there; the seven `_navigation_directive` set/clear sites reset neither. So on the product path leg and receipt agree by construction (`stale_arrival_receipt` / `arrival_receipt_for_another_place` / `arrival_receipt_superseded` unreachable — the wiring suite seeds `state.arrival_leg` by hand and never sees it), and after leg A's receipt the owner's directive B leaves `_brain_runtime_state` snapshotting A's receipt with A's leg; only the adapter's success-state STRING gate (runtime_adapter.py ~:406-416) — the thing the receipt was meant to replace — prevents a wrong-leg arrival today. HIGH: fix-and-regate. Fix shape (integrator's, implemented verbatim by the F8 executor in the gate worktree, Edit-only on runtime.py alongside F-M2): the leg is MAINTAINED by the runtime at every directive set/clear/generation site; the cut extends the existing leg's chain and refuses to cut with no leg; runtime-level tests (A → arrive → B ⇒ not-arrived before B terminates; commit r1 → cut → refine r2 ⇒ held receipt refused `superseded` by the runtime's own consumers; positive twin; no-leg refusal) with a teeth check; wiring seeds + structural guard kept; v5 artifacts byte-identical (digest-recipe test). Then gate #4 (new stamp), the integrator's delta lens (wiring + new runtime tests + live panel/minival), then GO. AUDIT_W4 will carry: "the adapter reads the lane's success-state string AND the receipt; the receipt must be able to refuse on its own — F8 makes it so."

## F-M2 — verifier, 20:2x — **ACCEPT** · review workflow stopped · F-M3a / F-M3b dispatched
F-M2 (register `FM2_STATUS.md`): two-worlds region — `scene_for_identity(scene, world_identity)` (poi_admission.py:520) resolves through W3's seam and `poi_goal_metadata(..., world_identity)` (:600) commits NO polygon on a foreign publication, writing `goal_region_refused`; pipeline.py:1183 passes `self.world_identity`; proof through the pipeline's own metadata assembly (test_poi_admission.py:786 + control :850; teeth: reverting the pipeline line fails the row); `_resume_queued_parent`'s product caller now tested through `_step_brain` (test_plan_queue.py:956-1105; teeth: runtime.py:5249 commented → 2 failed, restored byte-identical `bebd2505…`); matrix-freshness cell writes to a per-invocation scratch dir and reads back the runner-printed path, residue deleted, `.pytest-matrix-freshness/` ignored. Verifier re-run through the guard: poi_admission + plan_queue + wave_b_integration + matrix_freshness (not slow) = **112 passed**; residue dir gone; frozen v5 evidence intact (26 passed / 1351 s on digest-recipe + panel freshness, executor's run). Follow-up noted: `admit_poi`'s geometry diagnostic still reads the raw publication (→ F-M3b item H).
The review workflow (wf_bd309b9c-13e) was dead (no agent activity after 17:24Z; 13/46 verdicts); the verifier judged the remaining findings at source: **confirmed** runtime.py:3593 (revise during a queued child binds to the parked parent — `_build_brain_snapshot` takes the first non-terminal row of `_ordered_records`, sorted by sequence), runtime.py:5249 (child failed inside `tick()` never resumes the parent), runtime.py:17981 (deferred correction never acknowledged — `on_activated` has no runtime reader), runtime.py:17343 (every non-e-stop terminal → FACT_FAILED/klass obstacle, so an owner redirect is voiced as "something stayed in the way"), arrival_receipt.py:426 (`inside_region=True` bypasses NO_REGION; run_brain_v1's simulated controller uses it), and the region-less-POI narrated-failure question (to be MEASURED by probe first). **F-M3a** (plan-queue lineage A/B/C + narration facts D) and **F-M3b** (receipt API E, region-less POI F, README G, diagnostic H) dispatched in parallel on disjoint files (Edit-only on runtime.py). Lows deferred to backlog with attribution: runtime.py:4532 phantom PlanRecord on `keep` with no task id; :3600 activation observer before `_bind_plan_lineage`; realtime/lane.py:2078 counters mutate outside the lock; scripts/mutation_panel.py:775 provenance text says "v4 matrix" for the v5 panel (regeneration cost); scripts/ci_gate.py:1876 last-row selection = N47. Then gate #4 (new stamp) → integrator delta lens → commit.

## Board honesty (integrator condition 5) + gate #4 conditions — 20:3x
**The review workflow (wf_bd309b9c-13e) produced 23 findings and only 13 of 46 refuter verdicts before it died. The remaining findings — including every runtime.py finding fixed by F-M3a/F-M3b and every "low" deferred to backlog — were judged by ONE reader at source (the verifier, parcel-0e), with the adversarial refuters NEVER run.** Nothing below may later be described as "adversarially refuted"; the confirmed items were confirmed by reading the cited lines (and, for runtime.py:13029, independently by parcel-fb at source), and the deferred lows carry the label "single-reader, unrefuted".
Gate #4 conditions (parcel-fb, accepted verbatim): (1) teeth per fix — A, B, C, D, E, F each carry a test that fails with the fix reverted, recorded with the revert sha; (2) the FULL merge proof on the final bytes — M1's 26-file set + wave_b_integration + the new runtime suites, the companion-brain eval (run_suite 15/15), the live panel, and the v5 minival + matrix under the recipe of record — all four v5 digests reproduce or the movement is attributed by name (E may legitimately move a receipt-based row from arrived to refused: a finding to record, not a regression to hide); (3) a STAMPED T1 re-run on the final bytes — D and E change what NAV-INT-1 measures (authority histogram, refusal tokens, the 21/28), so the certifying tier row is the one run on the committed bytes; the earlier T1 (bfc72ae2…) becomes a pre-M3 row; (4) E is a tightening only — no new way to claim arrival without a committed region; F, if the probe shows a narrated failure, is parity with the eval's `object_near_goal_region`, never a bypass; the eighth mutant must still be killed through `no_false_arrival`; (5) this honesty note. Sequence after F-M3a/b land: stamp S4 of all bytes → gate #4 ∥ stamped T1 re-run on S4 → merge-proof set + companion eval + live minival/matrix (verifier) → delta to parcel-fb → GO or not → stage from gate bytes → commit → index in a clean worktree → push.

## F-M3b — verifier, 21:0x — **ACCEPT** (register `FM3B_STATUS.md`)
E: `arrival_receipt.py:436` `elif region is None and inside is None:` → `elif region is None:` — a caller-supplied `inside_region` can only refine a verdict with a committed region (tightening only; the frozen eval runner and `_cut_navigation_receipt` never pass the flag; headless_city passes None without a committed region); new row pins `inside_region=True, metadata={} → no_committed_arrival_region`; `run_brain_v1._SimulatedController` now declares its own scene (`region_inside_goal_region` sidewalk; `object_near_goal_region` band∩support 0.32 m lamppost), commits it onto the leg, passes a real pose; `_ARRIVED_STATES` and the false docstring claims gone; **zero dispositions changed (15/15)**; teeth: guard reverted → the row fails `arrived_verified`; poses moved outside the regions → 3 cases red (old code would have passed all). F: **measured, no product change** — probe reproduces `Navigation failed for bookstore: no_committed_arrival_region` at an honest arrival, BUT of the four shipped demo POIs only `crosswalk_a` has any instance of its class in the demo block; `bookstore_main`/`coffee_42nd`/`park_entrance` are coordinates with NO geometry — the card's explicit `NO_REGION` keep (building a region around a surveyed point is the C0/D-15 defect W4 refuses). Real residual = an object-class POI the scene DOES declare needs `SceneInstance` support surfaces + `region_instance_for_poi` changes — follow-up card (out of OWNS; the in-OWNS alternative would restore the fourth authority and re-open the two-worlds hole). Behaviour pinned by three product-path rows in `test_arrival_leg_runtime.py` (crosswalk arrival verified; 1.5 m short refused `outside_arrival_region`; bookstore `NO_REGION`); teeth: removing `poi_goal_metadata`'s commitment reddens both crosswalk rows. G: README v5 table names the committed artifact files. H: `admit_poi` diagnostic scoped through `scene_for_identity`; two-worlds row asserts the un-scoped diagnostic named the foreign crosswalk and the scoped one names nothing. Executor proof 110 passed + frozen evidence 19 passed; **verifier re-run through the guard: 62 passed** (arrival_receipt, arrival_leg_runtime, companion_brain_eval, poi_admission, receipt_authority); runtime.py carries no F-M3b hunk (zero overlap with F-M3a); ruff clean, zero noqa. Waiting on F-M3a → S4 stamp → gate #4 ∥ stamped T1 → proofs → integrator delta.

## F-M3a — verifier, 21:2x — **ACCEPT** (register `FM3A_STATUS.md`)
A: single authority `_amendable_active_row` (runtime.py:3687) answers "which task is this correction about" for BOTH `_build_brain_snapshot` (:3741) and `_accept_plan` (:3818) — order: the open amendment's own `revise` target, then a row in `AMENDABLE_TASK_STATES`, then any non-terminal row; the amendment-first order is load-bearing (executor's finding: `_apply_goal_amend` suspends its target before the replacement reaches `_accept_plan`, so parent and child both read `suspended` and only the amendment decision remembers which one the owner corrected). Product-path three-utterance test; the lamppost revise binds to the child, the parent keeps its ResumeIntent and resumes on the child's terminal. B: executive buffers internal terminals (`_note_terminal_locked` from `_fail_or_retry`/`_cancel`/both succeeded sites) drained via `drain_terminal_notifications()` in `_step_brain` after `tick` — named `on_task_terminal` (PlanQueue already owns `on_terminal` with parent-popping semantics), drained not inline (inline = AB-BA against the voice thread and resumes a parent mid-cancel-loop); runtime hands back only when the queue still waits on that child (duplicate-call guard, caught by F-M2's rows). C: `_whisper_plan_accepted` parks its promise; `_ExecutivePlanObserver.on_activated` (unchanged signature) queues, `_step_brain` speaks; the :780 test rewritten off the FakeExecutive — real TaskExecutive, real defer, activation from `report` at a checkpoint, sentence from `runtime._step_brain()`. D: one classifier `mission_terminal_attribution` (runtime.py:866) feeds prose AND event rider — owner stops/redirects/e-stop → cancelled/`owner`; only `person_stop`/`obstacle_stop` may claim an obstruction; everything else incl. verification refusals → failed/`unverified`; speech_acts gains `CLASS_UNVERIFIED` + one claim-clean template ("I couldn't get to {goal}, and I couldn't confirm why."). Teeth: all four RED when reverted, runtime.py restored byte-identical (`1c52e618f5e272e5…`) every time. Executor suites 287 passed; **verifier re-run through the guard: 153 passed** (plan_queue, wave_b_integration, speech_act_install, whisperer_wiring, speech_acts, adapter). Referred out and dispatched to F-M3b as item I: the W4 terminal emit `{refusal or reason}` masks an unclaimed terminal's real reason behind `no_system_arrival_claim` (`test_voice_nav_e2e.py` fountain case — slow/nightly, never in the close gates; the lamppost e2e failure is pre-existing at HEAD per W1_STATUS §e2e). S4 stamp after item I lands.

## F-M3b item I — verifier, 21:4x — **ACCEPT** · stamp S4 · gate #4 + stamped T1 + live digests launched
Item I: runtime.py:13722 `detail = reason if refusal == NO_CLAIM else (refusal or reason)` — one panel string, `receipt_refusal` and all consumers untouched; rows pin both sides (unclaimed terminal names the navigator's reason; a refused claim still names its token, and that row stays green under the revert — the guard against printing reason unconditionally); e2e fountain case green (2 passed), teeth red/restored byte-identical (`e59e79b9…`). FINISH list 112 passed. All F-M3 bytes final; **stamp S4 computed below**; gate #4 (same env as #3b) and the stamped T1 re-run (prefix `m1-merged-m3`) launched concurrently; live v5 minival + matrix under the recipe of record running to scratch for the four-digest comparison.

## Stamp protocol lesson (verifier + integrator, 22:0x)
**Name the hash function beside every quoted digest.** The S4 confirmation exchange burned a round because one side quoted `git hash-object` (blob SHA-1: runtime.py `e59e79b9…`) and the other `sha256sum` of the same bytes (`e7732827…`); the earlier F8 sha (`9d1bdd4c…`) happened to be sha256 on both sides. Same bytes, two functions; the tracked-diff sha256 `7d5eac0c92d379b0` was the invariant that held on both sides throughout. S4 = `bbe7a19453bfaba5` stands; nothing restarted. From here: every digest quoted in a register or a peer message carries its function (`sha256:` or `git-sha1:`).

## Close gate #4 (S4 `bbe7a19453bfaba5`, 19:51–19:56Z) — 4 NEW reds, all attributed; fixes dispatched; T1-on-S4 demoted to diagnostic
Green: hard-safety PASS (v5 frozen baseline collisions=0 false_arrival=0; panel clean; **panel freshness reproduces live**; matrix digest reproduces from the tree), unitree-assets, release-parity, assertion-evals, tier-coverage **11291 = 11202 + 89**, model-off 23/23, owner-store 5/5. Known reds: ruff (committed research), release-parity-integrity + 1 default-suite (the prompts leak). NEW default-suite reds (5 failed total): `test_r24_lock_discipline::test_the_lock_roster_is_complete` — F-M3a's `_plan_activation_lock` not in the lock-order roster; `test_dec0_debt_ratchet::test_no_new_long_function` — `_narrate_mission_terminal` + `_whisper_plan_accepted` > 100 lines (F-M3a D/C); `test_dec0_debt_ratchet::test_no_new_oversized_module` — `brain/executive.py` > 1000 lines (F-M3a B); `test_arrival_settle::…[arrived-arrived_verified-True-True-True]` — harness verdict carries `committed_region_ids=()`, `arrived_verified` False (green after F8's runs ⇒ broken by F-M3b's later hunks; E's guard vs headless_city's settle receipt). Remediations dispatched to the owning executors: F-M3a = roster line + helper extraction (< 100) + an M6-style split of executive.py (leaf ≤ 600, re-exports, NO BASELINE entry); F-M3b = item J, settle seam fixed in the honest direction (the venue commits the real region; E's guard never weakened; parametrised expectations unchanged). **T1-on-S4 (in flight) is a DIAGNOSTIC row; the certifying tier row is a fresh stamped T1 on S5 after gate #5 is green** (integrator condition 3 honoured at full cost). Live v5 digests on S4: minival episode `2822ebfd…` MATCH, matrix episode `5ea2cd93…` MATCH; report-digest comparison pending the recipe's compact function (comparator key bug, being redone).

## Live v5 digests on S4 — ALL FOUR MATCH (verifier, 22:2x)
Recipe of record into scratch, `--no-ledger`, computed with the digest-recipe test's own `report_digest(report, drop_aggregate_scene=True, compact=True)`: minival compact report `53eec205…` MATCH / episode `2822ebfd…` MATCH; matrix (n=125) compact report `041d9bdd…` MATCH / episode `5ea2cd93…` MATCH. The behavioural fixes (F8, F-M3a A–D, F-M3b E–I) moved NO v5 eval row — the four published digests reproduce live on the S4 bytes; the earlier "DIFFERS" was the verifier's comparator reading a nonexistent top-level key. Integrator condition 2's digest half is satisfied on S4; re-confirmed on S5 after the ratchet/settle remediations.

## F-M3b item J — verifier, 22:4x — **ACCEPT** (settle seam: the fixture was the defect)
Root cause: `test_arrival_settle.py`'s `_result_for` hand-built results with `mission_metadata={}` and leaned on `inside_arrival_region` alone — exactly the `no_committed_arrival_region` bypass item E closed; the row asserted a "committed K0 region" never committed (`committed_region_id({}) == ""` → the empty chain the gate printed). The venue is innocent: `headless_city._observe_settle` (:932-937) returns `inside_at_terminal=None` whenever `_arrival_region(mission)` is None, so the real settle path never reaches the receipt with a bare flag; the file's two slow end-to-end rows (real navigator metadata) stayed green throughout. F8's earlier green predates E — the open bypass hid the fixture's false claim. Fix per condition 4's honest direction: the fixture commits the region it claims, built with the product's own `scoring.object_near_goal_region` from the city block's published geometry (`lamp_post_1` at (0.2, 3.15), band (1.18, 1.38) reproduced by `object_near_envelope_m` at the 0.06 m footprint; no support polygon deliberately — module-scoped world, slow rows move the body — reason in the docstring). **All six parametrised tuples byte-identical; E's guard untouched (`arrival_receipt.py` git-sha1 `530a11bf…` unchanged).** Teeth as a SCRATCH PROBE on the product function instead of a tracked-file revert — deviation accepted: three sessions share the worktree and live gate jobs were reading it; the probe's OLD arm (`mission_metadata={}`) reproduces the gate's failure signature exactly (`arrived_verified=False`, `no_committed_arrival_region`, `committed_region_ids=()`) and the NEW arm verifies with chain `('lamp_post_1',)`. One file moved (`test_arrival_settle.py`, git-sha1 `637f0397…`, sha256 `73196725…`); guarded suite counts to follow when the guard frees (queued behind the integrator's panel and the merge-proof). `runtime.py` is now git-sha1 `c73477d1…` — F-M3a's narration split in progress; item I's hunk verified intact at :13780/:13786/:187.

## Merge-proof set (M1's 26 files + every new wave suite, −slow) — **718 passed / 0 failed** (109.9 s, label `parcel-0e-mergeproof-s4`)
Caveat recorded honestly: the run was QUEUED behind the integrator's panel job and executed after F-M3b's item-J fix landed, while F-M3a's ratchet remediation was still editing — so it certifies a mixed mid-queue byte state, not S4 and not S5. It is a diagnostic green (notably: the settle case passes post-J). The CERTIFYING merge proof re-runs on the S5 bytes per integrator condition 2.

## F-M3b item J — closed with counts (23:0x)
`fm3b-settle` guarded run: **65 passed** (settle incl. its slow end-to-end rows, receipt, arrival-leg runtime, K0, headless-city tasks, digest recipe — the frozen-digest evidence in the same invocation). Unmoved for the record: `arrival_receipt.py` git-sha1 `530a11bf…` (E's guard NOT weakened), `headless_city.py` git-sha1 `17e6d5c2…` (venue never edited). One file moved: `tests/test_arrival_settle.py` git-sha1 `637f0397…` / sha256 `73196725…`. Remaining before S5: F-M3a's ratchet remediation only (runtime.py mid-split at git-sha1 `c73477d1…`; item I's hunk re-verified intact at :13780/:13786/:187). T1-on-S4 diagnostic still running.

## F-M3a gate-#4 remediation — substantively done, guarded confirmation queued (23:1x)
Roster: `_plan_activation_lock` is a LEAF held across nothing; roster symmetric-difference `[]`, edges added/removed `[]`, cycle None; `PINNED_LOCK_ORDER` unchanged; 38 passed pre-label. Long functions: `_narrate_mission_terminal` 108→71, `_whisper_plan_accepted` 105→90 via three leaves (`mission_terminal_fact_text` :903, `require_plan_lineage` :1831, `plan_accepted_receipt` :1853); the receipt-consumption path byte-identical (extraction starts on the non-arrival path). Module split: `brain/executive.py` 1042→952 with `executive_notifications.py` (187, ExecutiveNotifier) + `executive_resources.py` (71, ResourceLocks) both re-exported; NO BASELINE entry. Ratchets measured directly at baseline. **Process deviation, reported by the executor and dispositioned: it ran `test_decig2_import_ratchet.py` (15 passed) OUTSIDE the guard — single process, no xdist, no OOM exposure; accepted as a recorded breach, not repeated; the every-pytest-through-the-guard rule stands unchanged.** Final `fm3a-ratchets`/`fm3a-287` counts + hash pairs pending behind the guard queue; then S5.
