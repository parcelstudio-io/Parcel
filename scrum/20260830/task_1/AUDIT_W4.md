# AUDIT · W4 ARRIVAL-AUTHORITY-1 (B32, v5, ArrivalReceipt) — verifier: Fable (parcel-0e), started 10:2x 08-30

**Disposition: HOLD → follow-ups F1 (NAV-INT-1 tier with the harness's K0 region) and F2 (the eval runner's second author) in flight; the v5 episode set, bridge and receipt are verified as landed.** Lens: parcel-fb (pending).

## Verified by me (worktree `~/.cache/parcel-0e/wb/w4` at `c96ac34`)
| row | result |
|---|---|
| ledger.jsonl byte-identical to HEAD (an accidental baseline row was reverted) | **yes** |
| v4 artifacts untouched (`episodes/v4`, `…baseline-v4-20260811T070536Z.json`) | **yes** |
| `noqa` added / ruff / `pipeline.py` / `config.py` | **0 / All checks passed / 7211 / 1000** |
| `brain/executive.py` untouched; nothing BLOCKED (terminal fact decided in `brain/runtime_adapter.py`, no owner hunks) | **yes** |
| `runtime.py` hunks: 14 (terminal-receipt path: `_step_navigation`, `start_navigation`, `_brain_runtime_state`, `_log_mission_terminal`, `_narrate_mission_terminal`) vs the root's 27 dirty hunks | **0 overlap** (U0 intersection) |
| guarded: `test_arrival_receipt` + `test_k0_arrival_authority` + `test_authority_half_scale_smoke` + `test_embodied_plan_eval` + `test_nav_instruct_digest_recipe` | **40 passed, 1 xpassed** (executor: 570 passed / 1 xpassed across 27 files) |

## Bars (register)
| bar | verdict |
|---|---|
| NAV-INT-1 authority disagreements ≤ 2/80; bench 0/29 | **not measured at full scale** (harness `_region_for` is a stale bare annulus → every bench leg `system_failed_but_arrived` by construction); 2/2 bench control legs with the K0 region read `agreement`, dtg 0.243 → **F1** runs the full tier with the harness fixed (my folder) |
| re-freeze record (bridge_v4_v5, provenance, `DIGEST_SENTINELS` `05c6a99a…`) | **GREEN** — v5 moves 1/25 minival (`object_goal-C-10`: band unchanged, support added, shortest_path 2.5 → 3.0) and 5/125 matrix; minival SR 0.24 = 0.24, SPL 0.185091 = 0.185091, collisions 0 = 0, false_arrival 0 = 0; matrix SR 0.232 = 0.232, SPL 0.161913 → 0.164864, false_arrival 1 = 1; v4 `episode_digest` unmoved; v1–v4 regenerate byte-identically |
| D-15 under v5 | **AGREES** (success, dtg 0.0 at (2.677, 0.8194) inside the crosswalk polygon) → the declared disable is withdrawable by regeneration (W5); consequence: `reactive_gate_disabled` SURVIVES on the old five rows — W5's A2 selection by measurement is load-bearing |
| A3: B-09 false_arrival = 0 by receipt absence | **not yet** — root cause measured: wrong INSTANCE (navigator committed `sidewalk_south`, ended inside it at (−0.056, −2.574); the key names the north `sidewalk` 4.77 m away — the C1/W3 region-instance family). The receipt refuses it by `region_id` (`ArrivalReceipt.is_for` gained that identity; pinned on B-09's geometry). **Second author: `evals/nav_instruct/runner.py` derives `system_arrival` from `system_arrival_claim(status, reason)`** — a status string → **F2** migrates the navigation families to the receipt (follow/circle keep the status path), re-runs minival + matrix; recorded meanwhile in `bridge_v4_v5.KNOWN_UNMOVED_FALSE_ARRIVAL` citing NAV-QUALITY §5.1 |
| floors / `config.py` / `pipeline.py` | **GREEN** |

## Reading so far
The band ∩ support-polygon change is small on the frozen corpora (6 episode definitions) and does exactly what B32 predicted where it applies (D-15, the bench control legs). B-09 was never a band problem — it is region-instance selection, and it exposes that the eval runner had its own arrival author. F2 closes that author by principle; whether B-09 then reads `wrong_instance` (refused claim) is the number to watch.

## Second lens (parcel-fb, own worktree = c96ac34 + W4's diff, 10:5x) — CONCUR on the build, one attribution required
Reproduced by the integrator: 113 passed / 1 xpassed across the receipt, K0, half-scale, embodied, digest-recipe, arrival-settle, runtime-adapter and scene-surface-truth suites (incl. the scorer-true/system-false consumer test and the re-issue/other-place receipt tests); `bridge_v4_v5.py` re-derived under the recipe of record — every leaf byte-identical except `generated_at`/`measured_bridge`; v4 `episode_digest` unmoved.
**Attribution gap (required before W5 layers):** §7's "v4 episodes × this tree" reads SR 0.24 / SPL 0.1851, but clean c96ac34 reads SR 0.20 / SPL 0.1533 (= W5's HEAD number). W4 moves TWO frozen-v4 rows on the CODE axis: `nav-region_goal-D-15-1b8b2361` `navigation_step_limit_inside_goal` / authority_disagreement → `agreement` (system True, scorer True) — the +1 success and the whole SPL rise (= "D-15 agrees"); `nav-object_relative-D-15-61f68ad6` `semantic_target_not_found` → `semantic_target_unreachable` (reason-only relabel). Both are W4's product changes acting on the frozen v4 SET (the v5 intersect never touches region_goal/object_relative). So the v4 row's history at W5 time is **A2 ×4 (≤ 08-24, incl. A-00 LOSS) + a379bf4 ×1 (D-15 suffix) + W4 ×2** — and the v5 minival's SR must never be read as "improved by the re-freeze": it improved by W4's arrival authority on one episode. Same sentence closes the loop: the product change that makes D-15 agree is the one that lets the declared disable be withdrawn AND blinds the gate mutant on the old five rows — W5's selection-by-measurement is load-bearing exactly because of W4. → W4-F3 (register wording) and a W5 provenance requirement, dispatched.

## F2 + F3 — verifier, 11:5x
- **F2 (eval runner's second author) — ACCEPT:** `evals/nav_instruct/runner.py` cuts an `ArrivalReceipt` from its own loop for `RECEIPT_ARRIVAL_FAMILIES = {object_goal, object_relative, region_goal}` and requires it for this leg and place (no receipt ⇒ False); `follow_owner`/`circle_owner` keep the status path (they commit no arrival region — docstring + test). **B-09 closed by principle:** `system_arrival` True → False, trace `arrival_receipt = wrong_instance`, authority `false_arrival → agreement`, failure `false_arrival → planning_error`; **matrix false_arrival total 1 → 0 in v4 and v5**; minival unmoved; SR/SPL/dtg/collisions unchanged by F2; the v4↔v5 delta is still the single row `object_goal-C-10`. 301 passed / 1 xpassed on the executor's 20 files; the one failure across the other runner consumers is the pre-existing `test_person_cell` veto row. Verifier: ledger still byte-identical, noqa added 0, guarded receipt-authority + receipt + K0 + digest-recipe run: **30 passed**.
- **F3 (attribution) — DONE:** §7 now reads "v4 × HEAD+W4" with the two W4-moved rows named; the v4 row's seven moves under three causes (A2 ×4, a379bf4 ×1, W4 ×2) live in code as `bridge_v4_v5.V4_ROW_CODE_AXIS_HISTORY`; the loop-closing sentence recorded.
- **F1 (tier):** harness `_region_for` → `support_surface_for(...)` with `TERMINAL_SUPPORT_CLEARANCE_M`; C7-F1's committed-instance rule untouched; `run.py` gains `NI1_WORKDIR` / `NI1_OUT_PREFIX` (writes `w4-b32-*`, never the recorded artifacts); tier running — bars 1–2 pending.
- **W5 note:** the matrix pin is now 0 with an EMPTY declared set (W5 built it at 1 pre-F2; corrected in the GO). W5 also found a fourth Sol consumer — `tests/test_ci_gate.py`'s `_panel_fields` (Sol widened `clean_safety_fields`'s contract without its other consumer) — a commit-tier self-test that would have gone red had Sol's files landed; the v5 panel build moves `_panel_fields` in the same commit.

## Lens finding → F4 (parcel-fb, 12:1x; dispatched)
At all three PRODUCT consumer sites the expected identity is taken from the receipt itself — `brain/runtime_adapter.py` (+50-54) `receipt_says_arrived(receipt, goal_id=getattr(receipt, "goal_id", ""), sequence=getattr(receipt, "sequence", -1))`; `runtime.py` +304-308 (mission-terminal record) and +346-350 (`_narrate_mission_terminal` → whisperer/speech acts) — so `is_for(goal_id, sequence, region_id)` is a tautology on the product path and `region_id` is never passed: (a) a receipt cut for generation N still says arrived when leg N+1 terminates; (b) B-09's other-place refusal exists only in the eval runner — the real dog would narrate arrival at `sidewalk_south` when asked for the north sidewalk. The unit tests prove the predicate with explicit ids and the eval loop; neither exercises the adapter/runtime wiring (why 113 + 30 passed). **F4:** each consumer passes the CURRENT leg's identity from the runtime's own state (the `directive` / `generation` the cut site uses; the committed region id from the K0 mission metadata) — never `getattr(receipt, …)` — plus `tests/test_arrival_receipt_wiring.py` (stale generation ⇒ not arrived; other place ⇒ not arrived; same generation + same region ⇒ arrived). "Consumers read nothing else" stays GREEN — they read nothing else; they asked the wrong question.

## F2 lens (parcel-fb, own worktree c96ac34 + W4 F1–F3, recipe of record, 12:5x) — REPRODUCED
Full matrix v4: n=125, SR 0.232, SPL 0.16191322, collisions 0, false_arrival rows [] (was [B-09]); v5: SR 0.232, SPL 0.16486450, collisions 0, false_arrival rows []; B-09 both versions: system False / scorer False / agreement / planning_error / `arrival_receipt = wrong_instance` — by receipt absence for the asked-for place, no per-episode exception. Suites 121 passed / 1 failed — the one red attributed: `tests/test_ci_gate.py::test_real_frozen_sentinels_match_the_current_tree` count literal `checked == 4` → 5 (the sentinels pass; W4's v5 manifest pin is byte-correct; the E8-idiom literal at :327 is W5's 4 → 6 bump on the gate worktree — recorded in M1's tables as the expected single red).

## F4 + F1 tier — verifier, 14:0x — **W4 disposition: ACCEPT (final patch `ed43554f…`, 22 files +1269/−105 + 32 new files)**
- **F4 (consumer identity):** a typed `LegIdentity(goal_id, sequence, region_id)` carried by each caller's own state — `runtime._navigation_leg` (built where the receipt is cut, from the same `directive`/`generation`; `region_id` snapped at the leg's FIRST committed region so a mid-leg re-commit terminates with a receipt for a place the leg wasn't issued for), `HeadlessTaskResult.arrival_leg`, `SemanticRuntimeState.arrival_leg`; `committed_region_id(metadata)` is the one derivation for receipt and leg; `receipt_refusal(...)` is the implementation with printable tokens (`no_arrival_leg`, `stale_arrival_receipt`, `arrival_receipt_for_another_place`, then the receipt's own); `leg is None` ⇒ not arrived. Wiring test `tests/test_arrival_receipt_wiring.py`: one good receipt against stale-generation / other-place / mirrored / no-leg legs through all three consumers (not arrived, right token) + the positive twin + an identity-distinctness property + the shared derivation pin. **A real latch defect found by F4's own sweep:** `mission.status` already `arrived` made `_inside_arrival_goal_region` refuse on its `status != "running"` guard and the earlier POI narrowing dropped the `cmd.stop or status == "arrived"` latch — a runtime that never saw a terminal; fixed (latch tested first; the D-15 narrowing untouched); `test_runtime.py`'s teleport landed on the surveyed POI point (3.5, −0.6), 0.2 m outside the crosswalk — moved to (3.1, 0.8). Re-measured after the fix: minival/matrix/panel bit-identical to F2 (fa 0 everywhere; D-15 agrees; the gate mutant still survives on the old rows).
- **F1 tier (harness `_region_for` → `support_surface_for`, 40 episodes, 3479 s, 0 errors, 60 sims launched / 0 orphans, recorded artifacts untouched, blind set unchanged):** authority disagreements **0/80** (bar ≤ 2), bench `system_failed_but_arrived` **0/29** (was 11/29) — the class is empty. Read honestly: bench legs agree at mean dtg 0.2435 m (B32's 0.244) and bench controls still `success_both` 0/2 — B32 stops the scorer certifying the road; it does not widen the strip. The honest failure is scored as a failure, not as an arrival.
- Verifier: ledger byte-identical; 0 `noqa` added; ruff clean on every touched/new file; `config.py` 1000; `pipeline.py` 7211; runtime hunks unchanged in shape (terminal-receipt path only; 0 overlap). Executor's suites: 47 (named five) + 123/1 xpassed (every receipt consumer) + 32 (runtime/POI/settle after the latch fix); a 29-file sweep queued behind the lock. My guarded re-run in the W4 worktree (wiring + receipt + adapter + receipt-authority + K0 + settle): **51 passed**.

## Final lens (parcel-fb, 14:2x) — CONCUR, conditional on F5 riding into M1's take
Part 1 (by read): 0 remaining `getattr(receipt, "goal_id"/"sequence")` sites; every consumer gates on `leg is not None and receipt_says_arrived(receipt, leg…)` / `receipt_refusal(...)`; `committed_region_id(metadata)` the one derivation; `tests/test_arrival_receipt_wiring.py` carries the four legs plus `test_no_consumer_reads_the_expected_identity_off_the_receipt` — a structural guard that makes the defect class un-reintroducible. The latch fix is correct in code but had no direct witness → **W4-F5** (dispatched): two rows in `tests/test_poi_admission.py` (status `arrived` + inside_region False + geometrically_arrived False ⇒ True; status not arrived + POI-with-region + inside_region False ⇒ False). F1's 0/80 and 0/29 accepted with the honest reading. Part 2 (own worktree = c96ac34 + `ed43554f…` + the 32 new files, guard): receipt wiring + receipt + receipt-authority + K0 + settle + runtime-adapter + poi_admission + semantic-navigation-regressions → **79 passed**; F2 matrices reproduced 1 → 0.

**F5 (14:4x) — landed:** `test_an_arrived_mission_is_never_un_arrived_by_the_narrowing` (status `arrived`, region committed, geom False, inside False ⇒ True) and `test_a_region_class_poi_outside_its_region_has_not_arrived` (status `running`, geom True, inside False ⇒ False; inside True ⇒ True — the anti-vacuity twin); seeded-red measured against the pre-F4 rule (latch row reddens the old code; twins pass both). `tests/test_poi_admission.py` +87 only; 21 passed; ruff clean. Final W4 patch re-saved (`w4.final.patch`, now incl. F5). **W4 FINAL: ACCEPT.**

## F6 (companion brain eval) — verifier, 16:5x — ACCEPT
Refusal token on all three cases: **`no_arrival_leg`** (receipt None, leg None) — never a band/support token. Root cause: the same second-author class F2 found in the NAV_INSTRUCT runner, on the brain-eval path — `run_brain_v1.py::_runtime_state` built `SemanticRuntimeState` from a frozen controller frame of status strings (a 14-field whitelist with neither receipt nor leg); the executive refused correctly. Fix: a `_SimulatedController` in `run_brain_v1.py` that cuts the receipt THROUGH `arrival_receipt()` from its own terminal observations against the leg it issued (`goal_id`/`sequence` from the `navigate` callback; `settled` from `navigation_enabled is False`; `inside_region=True` only on a terminal `arrived`, which the product sets only through `_step_terminal_verification` or the region-class arrival branch) — no venue-local "succeeded", no product change, no per-case exception; frozen artifacts (`integration_cases.jsonl`, `manifest.json`, whitelist) untouched; no expectation moved. Split: all three = (i) fixed; (ii) none. Stated limit (in code + STATUS): the venue is non-geometric — the receipt's geometric halves are vacuous there and are exercised over the same executive seam by `run_embodied_plan_v1.py`'s real `headless_city` venue. Result: `run_suite()` 15/15; both named tests green through the guard; widened sweep 75 passed; per-case metrics + aggregate byte-identical to a pristine `c96ac34` run; ruff clean; 0 `noqa`; one eval-side file +106/−5.

## F7 (current-commitment identity) — verifier, 17:5x — ACCEPT (my guarded run in the gate tree: wiring + receipt + adapter + receipt-authority + K0 + companion-brain-eval =  passed; ruff clean; 0 noqa)
`LegIdentity` carries `committed_region_ids` (every region THIS generation committed, in order) and a derived `commitment_index` (`len(chain) − 1`, so "which place" and "how many times it moved" cannot disagree); `ArrivalReceipt` carries the index it was cut under; `receipt_refusal` checks generation → `stale_arrival_receipt`, region ∉ chain → `arrival_receipt_for_another_place`, index ≠ current → **`arrival_receipt_superseded`** (new token). `LegIdentity.committed()` returns a new frozen object (idempotent on the current id); the chain is extended in ONE place (the runtime's K0 seam each tick); eval runners never hold a leg — the answer key normalises to a one-element chain with no index (a bare `str` is normalised deliberately, since it is iterable). `IDENTITY_REFUSALS` makes `is_for` exactly "no identity refusal", so an honest `outside_support_polygon` receipt is still for this leg. Six rows through every consumer: refine→terminal arrived; cut-before-refinement superseded; never-committed place another-place; other generation stale; B-09 via the answer-key chain `wrong_instance`; re-issue = fresh chain, prior receipt stale; plus `test_f7_refinements_are_product_driven_only`. Wiring suite 14; ten required suites 167 passed; v5 minival/matrix bit-identical to F2/F4 (fa 0; B-09 `wrong_instance`); ruff clean; 0 `noqa`; F7's four source edits sit inside the five regions W4 already owns (0 new hunks; 0 overlap with the owner's dirty hunks). F7-only delta: arrival_receipt.py +135/−21, runtime.py +27/−9, headless_city.py +21/−3, runtime_adapter.py +5/−1, wiring tests +168. Gate patch-stack stamp at T1's re-run dispatch: **`bfc72ae269a2cce5`**.

## F8 — runtime-maintained `LegIdentity` — verifier, 20:0x — **ACCEPT**
The integrator's confirmation of review finding runtime.py:13029 stands: before F8 the leg was assigned only at the receipt cut from the cut's own three inputs, so the identity half of the contract (`stale_arrival_receipt` / `arrival_receipt_for_another_place` / `arrival_receipt_superseded`) was unreachable on the product path and the wiring suite, seeding `state.arrival_leg` by hand, could not see it. **The adapter reads the lane's success-state string AND the receipt; the receipt must be able to refuse on its own — F8 makes it so.**
Read at source (gate worktree runtime.py): `_begin_navigation_leg` (:12997) at the cold start (:7432), the resume (:7354) and the channel resume bookkeeping (:7626); `_end_navigation_leg` (:13022) at the channel stop (:3347) and unreachable-at-start (:7483); `_observe_navigation_leg` (:13041) — the ONE chain mutation, from the navigator's own `arrival_goal_region`, called at the K0 seam before the still-current gate (:13208); `_cut_navigation_receipt` (:13077) reads the held leg, guards on `(directive, sequence)`, refuses with a warning when there is no leg, cuts under `leg.goal_id / leg.sequence / leg.commitment_index`; `_navigation_leg_identity` deleted. The executor's three deviations are correct and accepted: (A) terminal sites clear the directive only — the terminal fact is consumed by `_brain_runtime_state` → the adapter and by `_log_mission_terminal` after the clear, so nulling there would fail every navigation step closed; the next `_begin_navigation_leg` retires the pair, which is exactly where the defect lived; (B) the chain extension lives in the observe step, not the cut — the cut runs every tick, so an extension there would move leg and receipt together and `superseded` would stay dead; (C) one sequence (the navigation generation) at both cut sites — `_start_navigation_locked` had been cutting under `_behavior_generation`. (D) a hand-seeded `_navigation_directive` now yields no receipt — fail-closed; 370 passed across the eleven seeding suites and the venue/eval consumers.
Rows (new `tests/test_arrival_leg_runtime.py`, real `RobotRuntime`, no seeded leg or receipt, consumers = `_brain_runtime_state` → the real adapter and `_log_mission_terminal`): (iii) positive twin `arrived_verified` → "Arrived at the sidewalk." → adapter `succeeded`; (i) A arrives → owner asks for the bench → before B terminates `arrival_receipt is None`, leg B empty chain, `no_arrival_receipt`, adapter `failed`; (ii) commit `lamp_post_2`, cut, refine to `lamp_post_1` on a dropped tick → chain `("lamp_post_2","lamp_post_1")`, held index-0 receipt → `arrival_receipt_superseded` at the adapter AND at `_log_mission_terminal`; (iv) directive installed behind the runtime's back → no receipt, no leg, warning, terminal narrated as failure. **Teeth:** maintenance reverted (ruff-clean revert, sha 74244f00…) → 3 failed / 1 passed — (i) prints leg A's `arrived_verified` receipt standing during walk B, (ii) the chain never grew, (iv) a fabricated leg; (iii) passes in both states, so the fix is not "always refuse"; restored byte-identical (sha 9d1bdd4c…). Verifier re-run through the guard in the gate worktree: `test_arrival_leg_runtime` + wiring + adapter + K0 = 37 passed. Executor's must-stay-green at the final sha: 139 passed / 0 failed. Frozen v5 evidence untouched: `evals/nav_instruct/runner.py:1194-1219` cuts through `arrival_receipt(...)` with its own leg counter and never enters `RobotRuntime`; digest-recipe test green. ruff clean; noqa 69 → 69.
**Decision recorded, not taken:** an `arrived_verified` receipt cannot be superseded on the product path because `settled=True` is only passed on the terminal branch, which ends the leg. That is acceptable by design — an arrived-and-settled leg is over; supersession exists for receipts cut before a refinement on a leg still in flight — and making arrived receipts supersedable would mean cutting only at terminals with `_brain_runtime_state` reporting `None` mid-mission. Owner/integrator decision if ever wanted; nothing in W4's bar requires it.
