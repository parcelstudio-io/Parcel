# AUDIT · W1 PLAN-QUEUE-1 (C6) — verifier: Fable (parcel-0e), 13:0x 08-30

**Disposition: ACCEPT (wave B) — two bars RED as written, each with a measured cause outside the card; the e2e suite and parcel-6c's lens pending.** Worktree `~/.cache/parcel-0e/wb/w1` at `c96ac34`; patch `80d247a3…` (96 KB) applies cleanly to pristine HEAD; new leaf `brain/plan_queue.py` (845 lines) + `tests/test_plan_queue.py` (762); `executive.py` +36 (3 hunks), `runtime.py` +234/−6 (10 hunks), `voice/agent.py` +74/−6, `research/20260829/nav-interrupt-1/run.py` +40/−5 (one hunk inside `_false_arrival_in_window`).

## Bars (NAV-INT-1 tier, one build, 40 episodes, 0 harness errors, orphan check clean)
| # | bar | HEAD (08-29) | W1 | verdict |
|---|---|---|---|---|
| 1 | instruction admission ≥ 0.9 | 24/32 = 0.750 | **31/32 = 0.969** | GREEN |
| 2 | amended success ≥ 0.8 | 11/28 = 0.393 | **21/28 = 0.750** | RED — exactly the pre-registered ceiling: `bench 0/7` (every other goal 21/21: come_here 9/9, lamppost 3/3, sidewalk 5/5, towards_lamppost 4/4); scorer-only 24/28 = 0.857. The bench legs are W4's arrival-authority class (`outside_support_polygon` → band ∩ support, landed in W4). **Verifier's ruling: bar 2 is re-measured on the MERGED tree (W1 + W4) at the commit candidate — that is the honest number for this wave.** |
| 3 | resume path ratio ≤ 1.1× (vs the straight-line oracle, N8) | 1.4905 (n=8) | **1.7299 (n=12)** | RED as written — and the rise is the fix working: paired on the 8 common rows, 7 identical (±0.003); the one mover `ni1-25` 1.166 → 1.838 went `goal_1_continued → amended_goal` (it scored well by DISOBEYING the owner); the metric pays for refusal, which bar 1 exists to eliminate. The stack's uninterrupted from-rest floor is 1.280 (median; baseline 1.281). **Verifier's ruling: the bar's intent ("resume costs no more than a fresh run") is answered by the resume/from-rest quotient — the three `hold` rows (interruption with no second goal = zero queue work) read mean 0.980: the plan queue adds no measurable path overhead; rows at 1.6–2.3 drove to goal 2 and back, which is the task. Bar 3 is re-specified for the next tier as `quotient ≤ 1.1` on hold rows; recorded as RED-as-written / MET-on-intent here.** |
| 4a | owner-referring amendment admits | 0/6 | **6/6** | GREEN (rk=replace where a parent existed, rk=new_task where the interruption landed after goal 1 finished — the two lineages) |
| 4b | held queue utterance admits after cue-stripping | 8/8 | **8/8** | GREEN |
| 5 | `gold_blind.json` sha256 `c253df2f…` | — | unchanged | GREEN |
Also on the same instrument: return rate 8/9 → 13/13; refused-and-continued 7 → 0; terminal false arrival 3 → 0; blind classifier 0.8273 = 0.8273 (the port is exact: 110/110 label agreement).

## Harness correction (my folder, one function)
`_false_arrival_in_window` now matches a receipt on task id AND the revision that owns goal 1's polygon (derived in-function as the lowest revision each id produced — `replace()` refuses non-increasing revisions); re-scored offline from the recorded receipts: window claims 13 → 10, false arrivals **3 → 0** (the three were `plan_revision = 2` — goal 2's own verification on the reused id); every surviving revision-1 claim sits at `distance_to_goal_1_m = 0.0` on all 40 episodes; `episodes.jsonl` untouched (`episodes_rescored.jsonl` beside it); every other `h_ni1a` field byte-identical between `results.json` and `results_rescored.json`.

## Product path (register; verifier's guarded re-run at the merged candidate follows)
71/71 `test_plan_queue.py` (unit + live runtime); 439 passed / 2 skipped across 17 suites in a pristine HEAD + W1 worktree; debt + import ratchets green; 0 `noqa`; `config.py` untouched. A real queue-path bug found and fixed: `_accept_plan`'s interrupt loop was CANCELLING the parent a queue had just parked (`request_interrupt`'s correction branch cancels any non-running record; a queued parent is `suspended`) — a live-runtime test fails without the guard and passes with it; a second test drives park → admit child → child terminal → parent resumed with `resume_offer` + `plan_resumed`.

## Merge notes
All three `executive.py` hunks sit inside Sol's uncommitted regions with one semantic conflict (Sol rebinds `_activate_replacement` to a module-level `_activate_replacement_impl`; `self._notify_plan_activated(record)` must move with it) — for the owner when that diff lands; W1 needs none of Sol's symbols. W1 × W2 overlap in `_accept_plan` is the intended composition (W1's lineage feeds W2's hook) — M1 merges it.

## Second lens (parcel-6c, read-only in the W1 worktree, 13:2x; `~/.cache/parcel-verify/w1-lens/NOTE.md`) — CONCUR / ACCEPT with one required follow-up
- Receipt shapes ✓ (observer pair on the executive; `_notify_plan_activated` from `_activate_replacement`, not `replace()`'s defer; typed receipts, undeclared vocabulary refused, every transition files one). Lineage contract with W2's hook holds by VOCABULARY only until the merge ({new, revise, queue, keep} vs {new, revise, queue}; KEEP admits nothing so it never reaches the hook) — neither worktree contains both sides, so **M1's merged-tree integration test is REQUIRED** (one leading-cue admission → exactly ONE `plan_accepted`, lineage "queue", the "…after that" rendering; parent resume → `resume_offer`/`plan_resumed`; KEEP fires nothing). Cue table ✓ conservative. `ResumeIntent` ✓ consumed on the PARENT's resume (lives in `_resume_store`, not the txn; `_bind_plan_lineage` runs before `_close_amendment_window`; the interrupt loop skips `queued_behind` — the real bug, tested both ways).
- **THE FINDING → W1-F1 (dispatched, required before the merged commit):** `_resume_queued_parent` (~:4617) restores the channel from `_resume_store` FIRST, then calls `resume_task_running`; if the executive REFUSES (foreign lease / no request) it falls through to `semantic_tasks.cancel` + `resume_task` (fresh dispatch) while the channel is already resumed and driving — a running channel over a non-running executive record, then a second start. Narrow (foreign base lease at the child's terminal) but reachable. Fix: re-bind first, or re-pause into `_resume_store` on refusal; refusal-after-restore test + positive twin.
- Rulings: bar 2 — concur (re-measure on the merged W1+W4 candidate); bar 3 — concur (RED-as-written; ni1-25 rose by obeying); **next-tier metric refinement adopted:** the hold-row quotient (0.980) proves PARKING is free, not RESUME — resume rows' denominator should be the two-leg oracle (goal1 → goal2 → goal1); carry both: hold-row quotient ≤ 1.1 AND resume rows ≤ 1.2 × the two-leg oracle. The `_false_arrival_in_window` correction is a defensible oracle bug fix; pin in a comment that "lowest revision owns goal 1" depends on `replace()` refusing non-increasing revisions (in F1). Not reproduced by the lens: the tier numbers, 71 tests, 439/2.

## Final (13:4x) — F1 landed; e2e measured — **W1 disposition: ACCEPT (final patch `8fe1a7a4…`, 107,301 bytes)**
- **F1:** `_resume_queued_parent` re-pauses the channel on a refused re-bind (the shipped RESUME-cap idiom: `_pause_channel` back, then re-queue), so every intermediate state is consistent and exactly one start occurs; teeth measured (with the seven-line block removed the new test fails on `assert None is not None`); the register states plainly that a same-channel queue never reaches that branch (the child's mission takes the channel over) — the decisive test builds the restore-then-refuse state directly; the `replace()`-monotonic-revision dependency pinned in `_false_arrival_in_window`. F1-only delta: runtime.py +26 (two hunks inside W1's own added block; nearest dirty hunk 605 lines away — 0 overlap), run.py +8, tests +192.
- **e2e:** 16 passed / 1 xfailed / 1 failed — `test_sit_next_to_the_lamppost_settles_beside_it_in_a_sit` is PRE-EXISTING, measured in all four cells {patched, HEAD} × {contended, uncontended} with a byte-identical signature (`semantic_target_unreachable`, `blocked_route_gate: obstacle_stop`); no W1 path is reachable from that utterance (no amend cue).
- **Two self-corrections by the executor, recorded:** the e2e ran twice (an orphaned watcher + the detached one, both rc=1 with identical counts); its node-id enumeration used a lowercase-only regex (no earlier number affected — all 17 suites have no uppercase names) — the flaw surfaced `tests/test_runtime_whisperer_wiring.py`, which then ran 220 passed / 0 failed on the whisperer/lane set (the suites that would catch `_accept_plan` hunks disturbing W2's wiring). `tests/test_executive_preempt.py` (named on my dispatch list) does not exist.
- Verified on a second pristine-HEAD worktree by the executor: `git apply --check` clean, 442 passed / 2 skipped / 0 failed, ruff clean, 0 `noqa`, `config.py` untouched, `harness.py` untouched. Verifier's guarded re-run in the W1 worktree (`test_plan_queue` + `test_runtime_whisperer_wiring` + `test_realtime_whisperer`): see the count in the line appended below.
Verifier's guarded re-run in the W1 worktree: **183 passed** (`test_plan_queue` + `test_runtime_whisperer_wiring` + `test_realtime_whisperer`); ruff All checks passed; noqa added 0; config.py 1000.

## Merged-tree bar 2 (T1 re-run after W4-F7, stamped `bfc72ae2…`) — ruling closed
Bar 2 on the merged tree = **21/28 = 0.750**, exactly W1's number, with `bench 0/7` the only shortfall and every other goal 21/21; scorer-only = system-only (authorities agree leg for leg, 0/85 disagreements). RED against 0.8 by the pre-registered ceiling — B32's honest strip, an owner E3 item. Bar 3: RED as written; restated on the median (1.06), the hold rows (0.98) and the all-rows two-leg ratio (1.09), with the 8-row two-leg mean (1.37) carried by two pre-existing outliers — the next-tier bar keeps both statistics, per the second lens.
