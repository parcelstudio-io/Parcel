# W4 · ARRIVAL-AUTHORITY-1 (B32) — the certified band is the ground the contract will stand on; one typed receipt is the arrival authority

**Executor:** Opus · **Verifier:** Fable · **Lens:** parcel-fb · Source: `backlog/BLOCKED.md` B32 (both pairs), `scrum/20260829/task_2/AUDIT_C0_C2.md` §5, Sol's blocker 3.

## Defect (measured)
`bench_1`: the scorer certifies a bare annulus r ∈ [1.8538, 2.0538] while the terminal contract also requires the support polygon (north sidewalk y ∈ [2.2, 4.2] with `terminal_support_clearance_m` 0.32); the two are never intersected — **77.26 % of the certified band is unstandable**; every bench leg parks 0.244 m short of standable ground → `outside_support_polygon` → `system_failed_but_arrived` 11/29. D-15: a `known_poi` whose class is a region is judged by the 1.5 m point radius, not the region band. `_matrix_digest` hashes `ep.as_dict()`; 45/125 v4 episodes embed `band_m`, so the correct fix moves `e7c302dd…`.

## Build (under the re-freeze policy — the owner's "finish the frozen work")
1. `instructnav/scoring.py` `object_near_goal_region`: intersect the `near` band with the support polygon (with the same `terminal_support_clearance_m`); `evals/nav_instruct/generator.py` derives `band_m`/regions from the same function; `test_semantics_and_eval_object_goal_regions_agree` stays the pin (generator ≡ semantics ≡ pipeline). A region-class `known_poi` is judged by the class's region band, not the point radius.
2. **One arrival authority in the runtime:** the executive's terminal fact for a navigation step comes from ONE typed receipt (`ArrivalReceipt`: region id, settled, support-clearance ok, verified-by) produced by the K0 seam and consumed by the executive, the whisperer (C4) and speech acts (C5) — missing receipt = not arrived; no fourth opinion.
3. **Re-freeze:** regenerate the v4 baseline row(s) whose `band_m` moved, keep the superseded artifact, write `evals/nav_instruct/bridge_v4_v5.py` (Amendment A1: a NEW episode-set version v5, never a v4 rewrite) listing every moved episode with before/after (band, dtg, reason, authority), attribute each move to B32, update the pinned numbers in `scripts/ci_gate.py` in the same commit; the D-15 row's declared disable in the panel provenance is withdrawn if and only if D-15 now agrees (W5 coordinates).

## Acceptance (verbatim bars)
- NAV-INT-1 tier: authority disagreements **≤ 2/80**, bench `system_failed_but_arrived` **0/29**; NAV-GEN-1 A0 `arrived_verified` on the frozen block per target reported before/after; `test_k0_arrival_authority.py`, `test_authority_half_scale_smoke.py`, `test_embodied_plan_eval.py` green through the guard.
- Re-freeze record complete (bridge file, provenance, pinned numbers) and `ci_gate --tier commit` hard-safety PASS in a clean worktree at the result; the number of moved episodes and the SR/SPL before/after stated.
- No safety floor touched; `config.py` unchanged; `pipeline.py` net-negative or unchanged.

## Amendment A1 (07:0x, parcel-fb lens — binding; corrects the card's "bridge_v4_v4b" wording)

**Episode-set version v5, not a v4 rewrite.** 45/125 v4 episodes embed `band_m`; intersecting the band with the support polygon changes EPISODE DEFINITIONS. v4 stays byte-identical and superseded. W4 adds `EPISODE_SET_V5` to `generator.EPISODE_SETS` (`EpisodeSetSpec` with provenance naming B32 and the intersect rule), an `ARRIVAL_RULE_FOR_VERSION[v5]` entry, and `evals/nav_instruct/bridge_v4_v5.py` in `bridge_v3_v4.py`'s idiom (every moved episode: band before/after, dtg, reason, authority, cause=W4/B32).
**Sequencing consequence:** `tests/test_mutation_panel_freshness.py:44-45` computes `_CURRENT_FROZEN_EPISODE_SET = max(vN)` from `EPISODE_SETS`, so the moment v5 exists the committed v4 panel reddens the freshness test in the same tree. **Order: W4 (v5 + bridge + ArrivalReceipt) → W5 (v5 minival baseline; v5 mutation panel: C0's five rows re-derived on v5 with C0's provenance lineage carried forward — D-15 will likely AGREE under the region band, which is the declared-disable withdrawal by regeneration, never by hand; Sol's four intervention-witness rows re-selected on v5 with per-episode counts re-measured, Sol attributed for the design and the check).** W5 layers on W4's patch; both land in the one wave commit.
**ArrivalReceipt constraints (U32 territory):** produced ONLY by the system side (the K0 seam) from the product loop's own observations (C2's settle; support clearance from the same `terminal_support_clearance_m` the contract uses); never synthesized from scorer output, a harness field or a planner claim; `verified_by` names the instrument; the receipt carries the goal id/sequence — a missing or stale receipt (older than the current goal) = not arrived, so a re-issue cannot inherit the previous leg's receipt. Consumers (executive, whisperer, speech acts) read the receipt and nothing else — **test: seed a scorer-true/system-false leg and assert every consumer says "not arrived"**. No change to the A3 latch / A6 stop / `apply_reactive_safety` / `finalize_command`; the runtime hook = leaf + one install line in the HEAD worktree; needs Sol's `executive.py` hunks ⇒ BLOCKED with the symbol. `ci_gate.py` pins: digests re-pinned with the cause; `collision_total` / `false_arrival` pins stay or tighten, never relax; SR/SPL reported before/after with the number of moved episodes. The intersect lives in ONE function all three (generator, semantics, pipeline) import — copies refused.
