# GATE-0b — PREREGISTRATION · Claude Opus · 2026-08-23, before any gate run

Registered BEFORE the first `ci_gate.py --tier commit --json` of this card.
Rows are measured as written; a miss is reported as a miss. Design:
`DESIGN.md` (same folder). Instrument: the clean clone at
`~/.cache/parcel-gate0b/clean` — `git clone` of `e15e466` + all 60 dirty paths
of the working tree copied on, committed **in the cache directory only**,
re-cloned, fresh CPython 3.12.13 venv + `pip install -e '.[dev,voice]'`
(GATE-0 `task_20/GATE0_STATUS.md` §R9). Every gate run goes through
`~/.cache/parcel-guard/pytest_guard.sh --label gate0b` with `TMPDIR` unset;
**at most three gate runs in this card**: R0 baseline (pre-fix), R1 PASS
(post-fix), R2 optional confirmation.

## R0 — the baseline row (pre-fix, the re-count instrument)

`RESULT: FAIL` expected, with `default-suite` red. The run's FAILED list is
split by root and becomes the per-root table below. Registered prediction,
from `GATE0_STATUS.md` §C2 (the verifier's corrected table) — **the numbers
below are predictions; R0's own count is the measurement**:

| # | Root / premise | pred. n | Class of fix |
|---|---|---|---|
| A | `evals/external/results/{barn_ros2,threewe}` ignored while 55 siblings are tracked (`evals/external/.gitignore:1`) | ~5 | **carve-out**: negation-only un-ignore of the two subtrees (7 files, 52 KB) + the manifest comment |
| B | `.cache/external-evals/runtime/barn-parcel-bundles` (root `.gitignore:14`), generated by `barn_v{8,9}_policy_bundle.py` / `barn_profile_candidate_bundle.py`; 21 GB | ~17 | **skip-with-reason**, root declared in `tests/_external_roots.py`, printed by the `skip-list` gate row |
| C | V9 training-manifest mode bit (`split.json` tracked `100644`, `444` here, `644` in every clone) | ~7 | **decision executed**: drop the mode-bit requirement for the tracked manifest, keep it for generated roots |
| D | habitat 3 (provenance) | 3 | **diagnosed** in R0's output, then fixed OR skip-with-reason with the named root |
| E | BARN generator checkout | 1 | skip-with-reason (external checkout) |
| F | `evals/external/development/barn_frontier_detour_v4/results/.gitignore` (`runs/`) | 1 | carve-out or skip-with-reason, decided by R0's message |
| G | non-external 16: `test_capture_*`/`test_clockmap` 6, `test_prototype_profile` 5, `test_future_clock_guard` 4, `test_owner_store_isolation` 1 | 16 | fixed, or **declared** with the exact declaring line |
| | **total** | **51** | |

Registered in advance: **the total may not be 51.** R0 runs a tree that is
five batch-B cards ahead of the tree GATE-0 measured (FZ-1's nine frozen
snapshots, XD-1's two-phase runner, HY-1's sim guard, TRUTH-1's texts,
ROAM-2's coverage). Whatever R0 says is the baseline; the delta from 51 is
attributed by name in `GATE0B_STATUS.md`.

## R1 — the PASS row (definition of done)

Same recipe, working-tree fixes copied onto the stage, re-committed,
re-cloned, same venv build. Row is MET iff:

1. `RESULT: PASS — every hard gate green.` on stdout, exit 0;
2. the `skip-list` row is printed in the summary and names every absent root
   with the command that generates it;
3. `--json` is valid, names every stage in `COMMIT_TIER_STAGE_NAMES`, and no
   traceback appears in the human summary;
4. `default-suite` reports `0 failed`, and its skipped count equals the
   `skip-list` row's predicted module count for the absent roots;
5. no gate row goes from PASS to FAIL relative to R0 (8/10 were green).

## R2 — the hosted workflow file

`.github/workflows/ci.yml` passes an `act`-style dry validation. If `act` is
absent on this box (checked at the time, recorded), the substitute is
`python -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'`
plus a documented reason and a read of the two jobs' step lists. **B20 (the
Actions click) stays the owner's; nothing about a hosted run is claimed.**

## The V9 mode-bit decision (registered before it is executed)

Drop the mode-bit requirement for the **tracked** manifest
(`evals/external/training/barn_sampled_predictive_tracker_v9/split.json`);
keep `_require_immutable_regular_file`'s write-bit rejection for the
**generated** corpora under `.cache/external-evals/**` that
`_freeze_generated_tree` actually freezes. Rejected alternative: `chmod` in
test setup — it writes to the developer's checkout to satisfy an assertion
about the developer's checkout. Executed at
`evals/external/generate_sampled_predictive_tracker_v9_training.py:209`
(outside the README's OWNS — declared).

## Seeds (each RED before it is green; scratch copy, never the working tree)

* **S1 — the marker is load-bearing.** In a byte-identical scratch copy, delete
  the `pytestmark = skip_unless(...)` line from ONE external-root test module
  and run that module in the clone's environment with the root absent: it must
  go **RED** (the failure that was one of the 51). Restore by sha256, purge
  `__pycache__`, re-run: SKIPPED with the reason.
* **S2 — `--no-ledger` is load-bearing.** In a byte-identical scratch copy,
  restore the unconditional `LEDGER.open("a")` append and run
  `tests/test_nav_instruct_ledger_guard.py`: it must go **RED** naming the
  ledger that grew. Restore by sha256, purge `__pycache__`, re-run: green.
* Both seeds run with `PYTHONPATH=<scratch>:<scratch>/src` and assert the
  imported module's `__file__` is inside the scratch before seeding (the
  TRUTH-1 standing rule, `BATCHB_DISPATCH_FABLE_4a.md` 07:0x).

## Lint row

`.parcel/bin/ruff check .` on the working tree: exactly the 7 fingerprints of
`scripts/ci_ruff_baseline.json` attributable to this card's files — **zero new
in GATE-0b's OWNS**, no `noqa`, no re-pin.
