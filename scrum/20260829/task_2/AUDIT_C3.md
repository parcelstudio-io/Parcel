# AUDIT · C3 STALL-CLASS-1 — verifier: Fable (parcel-0e), 2026-08-30 00:0x EDT

**Disposition: HOLD → follow-up F1 (release door behind a default-OFF flag; off-path byte-identical on the frozen panel and minival). The attribution is ACCEPTED as research; the product change as landed moves a frozen hard-safety artifact (§4) and cannot ship ON in wave A.** One bar honestly MISSED and reported (`semantic_target_unreachable` rose 59 → 88 as held stalls were released through A2's door). Second lens: parcel-fb (pending: histogram + `status=planned` scan).

## 1. Re-run / re-read by the verifier

| row | executor | verifier |
|---|---|---|
| attribution BEFORE any product edit | histogram in STATUS §1 (A 17 / B1 5 / B2 4 of 26 non-POI stalls; 49 unreachable = U1 29 / U2 14 / U3 6) | STATUS §1 precedes the fix section; instrumented via a scratch driver, `headless_city.py` untouched (C2's file) — accepted as recorded |
| leaf module | `navigation/stall_attribution.py` 178 lines, 0 `noqa`, no safety value (a test greps for one) | **178 lines, 0 `noqa`**; the only floor names in it are docstring mentions; `HELD_RELEASE_AFTER: Final = 2` at `:88`, consumed at `pipeline.py:4637` |
| `pipeline.py` edit confined to the watchdog region + one import | 7200 → 7198 | hunks: two import lines, C1's `parse` (1138–1165), **the watchdog hunk `@@ -4618,33 +4616,30 @@`** — nothing else; 0 floor-name lines added/removed in that hunk; **7198** |
| `config.py` | byte-unchanged | **1000 lines** |
| no tick exempted from the watchdog (Amendment A1) | claimed; candidate (b) rejected | the hunk routes a *repeated* held stall through `_release_unreachable_candidate`; no exemption branch; **0 rows end `status=planned` without a terminal reason** (A1's GREEN row, executor's scan) |
| non-POI stalls halve at 0 collisions, no rise below 0.65 m | post-C1 43 → **10**; 0 collisions; 1 gen / 2 all below the band both before and after | accepted from the executor's 530-row runs (pre-C1 68/44 also recorded) |
| other reasons unchanged or improved | `semantic_target_unreachable` 59 → 88 | **MISSED on that reason — correctly reported.** Capability is flat-or-better (strict 335 → 335, any-instance 337 → 342, band entry 347 → 349, false arrivals 6 → 6, 486/530 rows byte-identical, +3.6 % steps): the release moved the *label* from a silent hold to an honest "unreachable", which is the direction the card wanted, but the bar as written is not met |
| frozen NAV_INSTRUCT digest | C3 moves it by **0** (`847ed260…` with and without C3, all 25 episodes) — but the tree is already off the committed `c172da37…` (15 episodes) | **E3 row: PASS for C3.** Isolated worktrees, `PYTHONPATH` pinned, no owner diff: **HEAD 704ba5c alone** reads `021b67ab…`, SR 0.20, **5/25 rows off the committed `c172da37…`** (D-15 `step_limit → step_limit_inside_goal`; object_goal B-05 / D-15 and object_relative D-15 `step_limit → semantic_target_unreachable`; **object_relative A-00 `arrived_verified → semantic_target_unreachable` — a frozen success already lost at HEAD**), and HEAD + the wave-A snapshot (C1+C2+C3, 23:3x) reads the **same `021b67ab…`** — wave A moved the frozen row by zero. **Attribution (corrected by the integrator, parcel-fb):** four of the five rows and the lost success are **card A2's clearance cost on the frozen corpus, on the record since ≤ 08-24** (`research/20260824/nav-quality/RESULTS.md` §1 lines 36-60, §1.4 lines 242-246, mechanism lines 258-290: target grounded, goal cell blocked at the commissioned 1.0223 m inflation, `_release_unreachable_candidate` fires, replan budget spent, `_target_missing_command` reports unreachable — clearance, not vocabulary; identical on the immutable v3 set); only D-15's `_inside_goal` suffix is a379bf4's. `evaluate_hard_safety` reads the ledger row's two pinned numbers and never re-derives the baseline, which is why no gate saw it. Owner re-freeze item: "frozen v4 minival digest `c172da37…` no longer reproduces: A2 clearance (≤ 08-24, NAV-QUALITY §1.4, four rows incl. one lost success) + a379bf4 (D-15 suffix); ci_gate cannot see either". C3's §2 residual 0.0977 m gap is the same 'directional inflation' successor question from the other side. (A later arm with the CURRENT tree files, C1-F1 in progress, moved D-15 further to `semantic_target_unreachable` — being checked against C1-F1: the frozen demo minival must keep D-15 at HEAD's behaviour.) |
| tests | 22 new + 144 + 168 passed | **guarded re-run (dirty tree): `test_stall_attribution` + `test_a2_navglue` + `test_grid_navigator` + `test_unroutable_goal_release` + `test_semantic_navigation_regressions` = 60 passed** |
| ruff | clean | **All checks passed** |
| `analyze.py` schema fix | A0 reads `LIVE_planner_inflation_radius_m 1.022296`; tracked `results.json` not regenerated (not OWNS) | accepted; regenerated at close by the integrator (one command; `tables.md` byte-identical per the mirrored run) |

## 2. What the attribution establishes (the finding that matters)
Three refusing authorities, one shape — route `planned` 199/200 ticks, body travel ≤ 0.02 m, `replan_count == 2`:
- **A (17/26):** the navigator's own brake holds the body at exactly 0.8000 m; `predictive_mode: projected_speed_cap` zeroes translation *at* the ring without crossing it, so the verdict is `obstacle_projected_speed_cap`, never `obstacle_stop` — and card A2's release witness is that one spelling, so `_steps_gate_blocked` stays 0 through the whole hold.
- **B1 (5/26):** `_control_observation` drops the target's own LiDAR returns; the brake is handed 1.60 m while the raw return is 0.71 m; the runtime gate returns `stopped` 194/200 ticks and the navigator has no channel to learn it was refused.
- **B2 (4/26):** the runtime yields to the OWNER (clearance 1.07–1.23 m vs a 1.30 m predictive person stop); the watchdog's person clause reads `nearest_person_m`, which the owner is not on.
The residual planner/gate disagreement is **0.0977 m** (A2's directional-cone discount `1.12·sin θ = 1.0223`, exact for a corridor centreline, loose off-axis). The honest deeper fix — the planner demanding the full 1.12 m — is a value change that raises an inflation and moves frozen routes: **written up, not taken; owner's re-freeze decision.**

## 3. Pre-existing reds flagged (not C3's)
`tests/test_voice_nav_e2e.py::test_sit_next_to_the_lamppost…` fails identically with C3 removed (shadow package); the NAV_INSTRUCT minival digest is off the committed row before C3 exists — attributed by the executor to the owner's uncommitted `grid_planner.py` (moves planner routes by construction) plus C1 shifting 3 failures `planning_error → grounding_error`. The C1 part is an E3 question (§1) and is being measured in isolation.

## 4. Frozen mutation panel — four isolated arms (HEAD 704ba5c worktrees, `PYTHONPATH` pinned, `MUJOCO_GL=egl`, no owner diff; `scripts/mutation_panel.py`)

| arm | passed | survivors | clean authority |
|---|---|---|---|
| HEAD | True | [] | {agreement 4, authority_disagreement 1} (= C0's committed panel) |
| HEAD + C3 only (watchdog hunk + leaf) | True | [] | **{agreement 4, tolerated_boundary 1}** — D-15's verdict moved |
| HEAD + wave-A files minus C3 (hunk reverted, leaf removed) | True | [] | {agreement 4, authority_disagreement 1} |
| HEAD + wave-A files with C3 (tree state 00:2x, C1-F1 in flight) | **False** | **['reactive_gate_disabled']** | **{agreement 5}** |

Reading (reconciled with the integrator, 01:1x): **the survivor in arm 4 is NOT C3** — it is C1-F1's in-flight "no published scene ⇒ refused" firing inside the panel runner (parcel-fb read it through the product caller: `parse("go to the crosswalk")` with no scene published → `goal_source=semantic_search, poi_refused=no_scene`; after `HeadlessCityWorld().reset(); observe()` → `known_poi`), so D-15 goes to the ladder, ends unreachable with both sides false ({agreement 5} = agreement on FAILURE), the body never drives at the POI point, and the gate mutant has nothing to bind on. **Refinement (integrator's counter-arm from the CURRENT tree, 01:2x):** with C1-F1's files as they now stand (incl. `demo_pois.yaml`'s declared scene), `run_panel()` reads passed True, {agreement 4, authority_disagreement 1}, survivors [] — inside the panel runner the world observes and the venue publishes, so D-15 grounds `known_poi` as at HEAD; my arm 4 captured F1 **mid-edit** (00:2x), not its final rule. What is still open for the final four-arm re-run: the reactive-gate mutant is killed through **2 channels (success set, failure histogram) vs 4 at HEAD** (mean-dtg and final-pose tolerances no longer differ under the mutant) — a thinning of the gate's evidence attributable to C3 and/or C2, to be read off the re-run. What remains C3's: **arm 2 — C3 alone moves D-15's label `authority_disagreement → tolerated_boundary`**, a flip inside the 5 cm epsilon on tick timing (the integrator's reading: D-15's final pose sits within floating point of both the polygon boundary and the 1.5 m radius). Epsilon or not, it is a frozen hard-safety artifact that no longer reproduces with C3 ON (`…still_reproduce` would go red), so the wave-A disposition stands on E3: flag default OFF. The executor's "C3 moves the digest by 0" was measured in a tree where the owner's `grid_planner.py` already stopped the gate from binding, so the held-stall class the door releases never occurred there. **E3: a wave-A card may not move a frozen hard-safety artifact.** F1 directive: flag default OFF, off-path byte-identical (panel {4, 1}; minival `021b67ab…`), measurement ON only in the NAV-GEN-1 harness; enabling on the shipped profile is the owner's re-freeze decision (the same directional-inflation question as the 0.0977 m residual).

## 5. Follow-up F1 (release door behind a default-OFF flag) — verifier, 02:1x

| row | executor | verifier |
|---|---|---|
| flag | `progress_watchdog.held_stall_release`, default False; kwarg `DirectiveNavigator(held_stall_release=…)` / `from_config(...)`; `configs/navigation/default.yaml` NOT edited (asserted by a test) | `pipeline.py:530/:734/:1027` plumbing; `stall_attribution.py:82` `HELD_RELEASE_FLAG`; `git status` clean on `default.yaml`; `config.py` 1000 |
| off-path is one line | `held_release_due()` gates on `enabled` first and short-circuits; metadata untouched flag-OFF | `stall_attribution.py:190` — accepted; tested on the mapping |
| panel, isolated worktree HEAD + C3 only, flag OFF | passed True, survivors [], {agreement 4, authority_disagreement 1}, byte-identical to the committed C0 panel except timestamps | **read from the executor's worktree JSON: passed True / [] / {4, 1}** |
| minival digest, flag OFF | `021b67ab73c4e7be…` (= HEAD) | accepted (my HEAD arm read the same) |
| flag ON, harness only | non-POI stalls 47 → 10 (C1-F1 now landed under both arms), strict 342 → 342, 0 collisions, 0 empty terminal reasons; `semantic_target_unreachable` 63 → 96 (the same missed bar) | accepted as the research finding |
| tests / ruff / noqa | 28 leaf cells; 324 passed across nav subsets; ruff clean; 0 noqa | **guarded re-run: 70 passed** (`test_stall_attribution` + `a2_navglue` + `grid_navigator` + `unroutable_goal_release` + `semantic_navigation_regressions` + `navcore_probe`) |
| cost stated | `pipeline.py` 7200 → 7211 (+11): the flag plumbing outweighs the watchdog simplification — a DEC-0 debt regression named as F1's price | accepted as stated; the ratchet's verdict is read at the close gate |

**Final C3 disposition: ACCEPT (wave A) — attribution + leaf + flag-OFF door.** The 43 → 10 (now 47 → 10) result is research evidence; enabling the door on the shipped profile is the owner's re-freeze decision (moves frozen panel/minival rows; the honest fix is the planner demanding the full 1.12 m).

### 4.1 Correction to the four-arm table (02:3x) — arms 2 and 3 were artifacts, not runs

Re-reading the panel files by `generated_at`: `scripts/mutation_panel.py` writes only to `--out` (default: the in-tree report), and my arms 2 (HEAD + C3 only) and 3 (wave A − C3) exited 1 before writing, so the "results" I read for them were the artifacts sitting in those worktrees — arm 2's `{agreement 4, tolerated_boundary 1}` with 4 kill channels is the **08-28 committed artifact** (a379bf4-era), and arm 3's `{4, 1}` is the **C0 panel I had copied in**. Only arms 1 and 4 were live (`--out`): **HEAD live = passed True, {agreement 4, authority_disagreement 1}, `reactive_gate_disabled` killed through 2 channels** (success set, failure histogram); arm 4 = the mid-edit C1-F1 capture. So: (a) "C3 alone moves D-15 → tolerated_boundary" is **withdrawn** (unmeasured); (b) the **4 → 2 kill-channel thinning predates wave A** — it is the difference between the 08-28 artifact and HEAD 704ba5c live, i.e. a379bf4's, not C2's or C3's; (c) the near-final run (HEAD + all current wave-A files, flag OFF) = HEAD live exactly ({4, 1}, 2 channels, minival `021b67ab…`) — **wave A moves the panel and the minival by zero**. Arms 2 and 3 are being re-run with `--out` for the record (C3 flag OFF in both, since the tree's C3 now defaults OFF). The C3 disposition (flag OFF; enabling = owner re-freeze) is unchanged — it rests on E3 caution, not on the withdrawn arm.

Re-run outcome (02:4x): both arms failed at import — the pre-F1 watchdog hunk applied on HEAD calls the leaf's old API, and the "minus C3" tree still carried C3-F1's flag plumbing after the leaf was removed — so neither is a measurement. **"C3 flag ON alone → frozen panel" is NOT MEASURED** and is left to the owner's re-freeze instrument (the C3 executor measured flag ON only on NAV-GEN-1's generated/frozen rows, where it changes 41/530 episodes). What IS measured and binding: HEAD live = near-final (flag OFF) = {agreement 4, authority_disagreement 1}, 2 kill channels, minival `021b67ab…` — wave A moves neither frozen artifact.

## 6. Second lens (parcel-fb, from the executor's raw artifacts, 02:5x) — CONCUR with ACCEPT

- **Histogram recomputed with independent discriminators** (`features.json`, 81 entries): A 17 / B1 5 / B2 4 — identical to §1.1; class A has `obst_min == obst_med == 0.800` exactly on all 17 (a hold at the ring, not a near-miss); B1 raw clearances 0.68–0.73 m with the gate zeroing 194–200 ticks; B2 clearances 1.8–3.5 m with the gate zeroing 196–199 (owner yield); all 26 route `planned` 199–200/200, displacement ≤ 0.022 m, replans 2.
- **F1 rows (flag OFF vs ON, 530 each):** generated `no_progress` 49 → 10, strict 342 = 342, collisions 0 = 0, < 0.65 m 1 = 1, false arrival 1 = 1 (the frozen block's), `status=planned`/no-terminal-reason 0 = 0 (A1 bar met both arms); the 49 flag-OFF stalls become 32 unreachable / 6 `arrived_verified` / 1 verification-failed / 10 still stalled. **Two register mismatches, minor, sent back to the executor:** rows changed OFF→ON = 48 by full-dict comparison (register: 41); the two strict regressions in the F1 rows are `gen:880007:crosswalk:1` and `gen:880016:crosswalk:0` (register names 880016/880025 from the pre-F1 tree). Gains 2, net 0.
- **Flag:** `pipeline.py:530/734/1027`, default False, `default.yaml` clean; `held_release_due` (`stall_attribution.py:190-210`) gates on `enabled` before any read/write — accepted.
- **DEC-0 at the close gate:** a clean HEAD 704ba5c worktree measures new oversized modules [] / new long functions [] (153 → 153) — HEAD GREEN; the working tree measures 4 new oversized modules and 9 new long functions (153 → 166) — all the OWNER's diff (`control/*`, `gateway/*`, `unitree_control`, `bridge/*`) plus Sol's in-flight DUPLEX-GEN-1 files (`audio/voice_loop.py` +450, `voice/execution_narrative.py`, `brain/execution_narrative_bridge.py`, `brain/executive.py`); **none is a wave-A file, and C3's `pipeline.py` +11 does not register** (the ratchet keys on new modules and new function names, not line deltas).
