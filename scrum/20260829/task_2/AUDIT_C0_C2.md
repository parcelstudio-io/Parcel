# AUDIT · C0 + C2 ARRIVAL-SETTLE-1 — verifier: Fable (parcel-0e), 2026-08-29 23:0x EDT

**Disposition: C0 ACCEPT as (b) with the disable declared; C2 ACCEPT-WITH-BARS-OPEN (settle window and instrumentation landed; the NAV-INT-1 GREEN row was not reached for reasons outside C2's OWNS — two of them are now named defects with owners).** Second lens: parcel-fb (pending: freshness in a clean worktree).

## 1. C0 — the red row
- **Decision (owner silent, card default):** (a) attempted ~35 min and refused on a *constraint* per Amendment A1: D-15 is "go to the crosswalk"; the system grounds it to `crosswalk_a` (3.5, −0.6), **outside** the scorer's crosswalk polygon; the reactive gate holds the body at **1.5151 m** from the point against a **1.50 m** arrive radius (105/200 ticks `meta='stopped'`) — a 15 mm miss → `navigation_step_limit_inside_goal` → `authority_disagreement`. **No hardened-contract refusal fires** — the mission never reaches it. Every route to "claim arrival" weakens the gate, moves a frozen digest, or lands a wave-A-forbidden file → (b).
- **Attribution reproduced at single-commit resolution** (clean `git archive` per revision): `f3ecb5c` → agreement (byte-matching the committed artifact); **`a379bf4` → authority_disagreement**; HEAD identical. The owner's uncommitted `grid_planner.py`, transplanted alone, flips it back to `arrived` by moving the same stall to 1.4996 m — **0.4 mm**. The row is a knife-edge on the arrive radius, and the underlying cause is C1's POI defect (the point is outside the polygon).
- **(b) landed with the cost written:** `mutation_panel.json` regenerated in a clean worktree with A1's provenance sentence verbatim (verified by grep: "`no_authority_disagreement` disabled as a kill channel from this re-run; re-armed when D-15 agrees again"); `scripts/mutation_panel.py` +40/−1 (provenance; the one `noqa` is pre-existing at HEAD); new `test_red_clean_checks_are_declared_disabled_kill_channels` (`tests/test_mutation_panel_freshness.py:143`) makes an undeclared disable fail and forbids it for the three absolute floors (negative control shown). Freshness **4 passed** in a clean worktree (executor). Verifier: in the DIRTY tree the run gives `2 failed` (`test_committed_panel_safety_fields_still_reproduce`, `test_mutation_panel_runs_on_the_current_frozen_set_live`) — exactly the executor's prediction: the owner's uncommitted `grid_planner.py` makes D-15 agree, so the committed panel with the disable no longer reproduces there; the clean-worktree reproduction is the binding row: **4 passed in 45.82 s** (`~/.cache/parcel-0e/c0-wt` = HEAD + the four C0 files, `PYTHONPATH=$W/src:$W MUJOCO_GL=egl`, through the guard, 23:5x). Lesson recorded on the board: without `PYTHONPATH` pinned the worktree imports the dirty repo's `src` (editable install) and the same run reads 2 failed — that first attempt was invalid, not evidence.
- **Re-arm condition:** the freshness test is red in the *dirty* tree because the owner's `grid_planner.py` makes D-15 agree — when that diff lands, the panel must be re-run and the disable withdrawn. Verifier's recommendation: carry an explicit "re-arm pending `grid_planner.py`" marker in the provenance so it cannot be forgotten (ruling requested from the integrator).
- Also found: the superseded artifact (20260828T084754Z) reproduces on no commit — its poses match `f3ecb5c` but its authority histogram does not.
- **Root cause (integrator's ruling, parcel-fb):** no terminal-contract refusal fires on D-15; the system targets the POI POINT `crosswalk_a`, 0.2 m OUTSIDE the polygon the scorer certifies, and `apply_reactive_safety` holds the body 15 mm short of the 1.5 m point radius while it is already inside the polygon — a goal-REPRESENTATION mismatch (point-goal system vs region scorer): the C1 family on the demo block. Scene-identity C1 keeps D-15 on `known_poi`, so the knife-edge survives; (b) is a bandage. From the C1 raw rows: on the frozen demo block "go to the crosswalk" ends `navigation_no_progress` 14/16, false arrival 1, arrived 1, `wrong_instance` 16/16 — **D-15 is the demo block's crosswalk behaviour sixteen times over, not one knife-edge episode.** The durable fix is C2's one arrival authority: a `known_poi` whose class is a region must be judged by the region band, not the 1.5 m point radius — that moves frozen demo rows ⇒ the OWNER's E3 decision (backlog entry being written by the C0/C2 executor, follow-up F1). Direction-aware freshness message ("re-run the panel and withdraw the disable" when live is greener on a declared-disabled channel) also in F1; no new marker.

## 2. C2 — settle-observing arrival, one authority
| row | result | verifier |
|---|---|---|
| `HeadlessTaskResult.settled / settle_frames_observed / goal_source` | added (`headless_city.py:152-163`, `_observe_settle` `:821`) | confirmed; `status`/`reason` semantics unchanged (E3) |
| safety floors | untouched | the only floor names in the diff are two comment lines; `config.py`, `pipeline.py` untouched by C2 |
| NAV-INT-1 RED | **16/80** on a pinned HEAD export (bar 17/80; one bench leg `tolerated_boundary`) | reproduces |
| NAV-INT-1 GREEN ≤ 2/80, bench 0/29 | **NOT REACHED** (rule 3) | accepted as recorded: (i) the tier never imports `headless_city` — C2's window cannot reach it; (ii) 10 bench legs are `semantic_arrival_verification_failed` at dtg 0.0 — the a379bf4 contract *refusing*, owner-gated (N45/K0), A1 forbids claiming past it; (iii) **6 sidewalk legs: the robot committed and verified `sidewalk_south` while `nav-interrupt-1/harness.py` scores one hardcoded north polygon 4.97 m away — a harness wrong-instance defect (new C7 follow-up)** |
| NAV_INSTRUCT frozen digest | `e7c302dd…` unchanged; 115 digest tests pass | accepted |
| `tests/test_arrival_settle.py` | 11 passed | **guarded re-run (dirty tree): green within a 48-passed run** |
| regression subset | 3 named files green; widened 146 passed / 1 failed (`veto_fraction 0.875`, pre-existing at `f3ecb5c`, `a379bf4`, HEAD; C2 moves it by zero) | accepted |
| NAV-GEN-1 unchanged by C2 | 530/530 rows byte-identical on every pre-C2 column, with and without the window | accepted (the `_WorldSnapshot` freezes the terminal frame before the window steps) |

**Strict vs settled on 450 generated episodes (arm A0, pinned HEAD + C2):** strict 294/450 = **0.6533**, settled 284/450 = **0.6311** (Δ −2.2 pts; all 10 are strict-true/settled-false where the navigator's own region says otherwise: 6 crosswalk with no committed region, 4 sidewalk `semantic_target_unreachable`), `arrived_verified` 242/450 = **0.5378**. Settled ≫ the 0.40 floor: **the navigator does hold still after arrival — MA-1's 4.5 % was a gold artefact, confirmed on the product harness.** Crosswalk: 90/90 `known_poi`, 0/90 `arrived_verified` (C1's defect, now instrumented per episode — closes NAV-GEN-1 §5.2's caveat). Planter: strict 64 vs `arrived_verified` 25 with 44 `outside_support_polygon` refusals per episode → for C3/N45.

## 3. Follow-ups opened
- **C7-F1 (research):** `nav-interrupt-1/harness.py` scores a hardcoded north sidewalk polygon; must score the instance the executive committed (`sidewalk_south`) — 6/80 authority "disagreements" are the harness. Dispatched to the C7 executor.
- **C1 ruling** (AUDIT_C1 §3) closes the crosswalk half of the arrival problem; the bench half (10 legs refused by the hardened contract at dtg 0.0) is N45/K0 and owner-gated.
- Integrator handover: regenerate `CODEBASE_INDEX.md`; optionally register the new ratchet node id at `ci_gate.py:196`.

## 4. Integrator finding (parcel-fb, 00:0x) — the owner's uncommitted `grid_planner.py` blinds the panel's reactive-gate mutant

In the working tree `tests/test_mutation_panel_freshness.py` reads 2 failed / 2 passed: `…still_reproduce` (D-15 flips to agreement — the R2 knife-edge is live) AND `test_mutation_panel_runs_on_the_current_frozen_set_live` on `passed is False`: **survivors = ['reactive_gate_disabled']** — disabling the reactive safety gate changes nothing on the five frozen panel episodes (mutant final poses identical to clean to 4 decimals). Attribution by worktree, PYTHONPATH pinned: HEAD 704ba5c + ONLY `git diff -- src/parcel_robot/navigation/grid_planner.py` → passed False, survivors ['reactive_gate_disabled'], clean authority {agreement 5}, D-15 dtg 0.0000. Mechanism (measured by wrapping `evals.nav_instruct.runner.apply_reactive_safety`, the symbol the mutant patches): the gate is called 101/88/51/62/0 times on the five clean episodes and zeroes a non-zero request **0 times**; at HEAD it zeroed D-15 on 105/200 ticks. The owner's planner change routes so that the gate never binds on this episode set — which is why D-15 "arrives" by 0.4 mm — and thereby the hard-safety gate can no longer certify that the reactive gate does anything.

**Disposition consequences (adopted):**
1. C0's close states that the re-run panel is valid ONLY on trees without the owner's `grid_planner.py`; landing that diff requires BOTH withdrawing the disable AND re-choosing panel episodes on which the gate binds (or the gate's evidence is gone) — an owner E3 decision, not a wave-A edit.
2. The "re-arm condition" is not just a re-run: after the diff, D-15 agreeing is the SAME event as the gate never firing; the direction-aware message must also fire on "a mutant that was killed is now a survivor" and name the mutant (forwarded to the C0/C2 executor's F1).
3. For the owner: a second hard-safety consequence of the 28-file diff, in the file they consider a planner tweak.

## 5. Follow-up F1 — verifier, 04:0x

**F1(3) per-leg attribution (instrumented re-run on a pinned HEAD export; 80 legs, 18 disagreements, bench 11/29 reproduced exactly):** all 11 bench legs decide on **`outside_support_polygon`** — zero ticks of co-visibility / `target_not_resighted` / `outside_arrival_region` / `surface_clearance_out_of_band` / `perception_stale` / `pose_unhealthy`. Same family as D-15, a different pair: **band vs support polygon**. `bench_1` (−2.5, 3.045): the scorer certifies a bare annulus r ∈ [1.8538, 2.0538]; the terminal contract also requires the support polygon (north sidewalk y ∈ [2.2, 4.2] with `terminal_support_clearance_m` 0.32 → y ∈ [2.520, 3.880]); the two are never intersected — **77.26 % of the certified band is ground the contract will never stand on** (400 k samples). Every leg parks at (−0.68, 2.28): inside the band, 0.244 m short of standable ground. **≤ 2/80 is unreachable in wave A with proof:** intersecting the `near` band with the support polygon changes `object_near_goal_region`, which 45/125 v4 episodes embed as `band_m` → `e7c302dd…` moves → owner E3; dropping the support check would let the dog say "I'm at the bench" from the road. Recorded as **`backlog/BLOCKED.md` B32** (85 insertions, one entry, no existing line touched) with both pairs, the point-vs-region wording, the knife-edge clause and the "C0(b) valid only without the owner's `grid_planner.py`" addendum. My earlier reading "the hardened contract refusing — perception" is superseded: it is owner-gated because the correct fix moves frozen evidence.

**F1(1) direction-aware freshness:** pure helper `freshness_failure_message(committed, live, provenance, *, live_survivors)`, priority survivor → greener → redder; (a) GREENER on a declared-disabled channel → "…re-run the panel and withdraw the '<name> disabled as a kill channel' declaration"; (b) REDDER → original wording byte-for-byte; (c) survivor → "…['<mutant>'] survived — the panel episodes no longer exercise <mutant>'s channel; re-choose panel episodes on which the gate binds (owner E3 decision)". Guard-rails pinned (green drift on an undeclared channel stays red; survivor reported even when clean fields reproduce). 7 fixture tests; clean-worktree file 11 passed (was 4). Verifier's guarded re-run in the gated worktree: pending the guard lock (a foreign panel regeneration holds it).

**Foreign edit in flight (integrator alerted):** a session labelled `c0-panel-refreeze` is regenerating `evals/nav_instruct/results/mutation_panel.json` in the main tree with an extended `scripts/mutation_panel.py` (intervention witnesses, `reactive_gate_coverage`, fifth check `reactive_gate_exercised`) — B32's second half, started without a board entry. The wave commit stages C0's gated bytes for those two files; the remediation supersedes them afterwards. C0-F1's helper reads `clean_checks` generically and stays green under the extended schema (executor-verified).

**Final dispositions:** **C0 — ACCEPT as (b)** with the disable declared, the direction-aware message, the root cause and B32 on record, and the validity caveat (only on trees without the owner's `grid_planner.py`). **C2 — ACCEPT (wave A scope):** settle window + `goal_source` instrumentation landed and byte-identical off the frozen rows; the NAV-INT-1 ≤ 2/80 bar is **not reachable in wave A by proof** (6 legs were the harness — fixed by C7-F1; 10 legs are the band-vs-support mismatch — owner E3, B32); strict 0.653 vs settled 0.631 on 450 episodes stands as the finding that the navigator holds still after arrival.

**Collision note (04:1x):** the foreign panel rewrite is Sol's (`c0-panel-remediation`/`refreeze`, Codex ← Cursor, by process ancestry — parcel-fb). The wave commit stages C0's gated bytes for `scripts/mutation_panel.py` and the panel JSON; Sol's hunks are not in it; Sol's regenerated panel supersedes C0's afterwards under the E3 rule written on the board.

**Commit decision (04:3x, superseded 04:5x by the integrator's ruling — C0's pre-coupling bytes go in, see the board):** the three panel files were to be withheld from the wave commit (the freshness test is now coupled to Sol's in-flight script — `reactive_gate_exercised` asserted by name; 2 failed / 9 passed against C0's gated script); B32 is committed. C0's disposition is unchanged as evidence; its artifact is superseded by Sol's remediation under the E3 rule on the board.
