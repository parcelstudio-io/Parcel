# Task 2 · FIX-SUBSTRATE-1 — fix the issues the fluidity verdict found, before any model work

**Date:** 2026-08-29 (America/New_York), opened 21:5x EDT · **Integrator / verifier:** Fable (parcel-0e) · **Executors:** Opus, one per card · **Second lens:** parcel-fb (C0–C3), parcel-6c (C4–C5)

**Owner instruction (verbatim):** "let's commit and fix the issues first. And flesh out the implementation plan that we need to do. Opus should implement while fable reviews."

**Physical-motion status:** **NO-GO** (unchanged; nothing on this board authorizes motion).

**Source of the issues:** `research/20260829/VERDICT_RESEARCH_QUESTION.md` §5 (the decision), `nav-gen-attribution-1/VERDICT.md`, `model-b-contract-2/VERDICT.md`, `nav-interrupt-1/VERDICT_FABLE.md`, `sim-loop-1/VERDICT_FABLE.md`, `model-b-narration-1/VERDICT_FABLE.md`, plus parcel-fb's hard-safety bisection (card C0).

## Why this order

The verdict's finding is that every fluidity experiment was refuted by the *substrate* — arrival/stall/grounding mechanics in the navigator and a voice with no fact contract — not by the Model A/B idea. So the substrate is fixed first, with instruments that already exist (NAV-GEN-1's 530-episode harness, NAV-INT-1's tier, MB-1/MB-2's scorer, LIT-1's loop) as the acceptance rows. No card on this board trains a model or touches a safety floor.

## Board

| card | file | wave | executor touches | acceptance instrument | status |
|---|---|---|---|---|---|
| **C0** hard-safety red on `main` (`region_goal-D-15` scorer-true / system-false freshness row) | `C0_C2_ARRIVAL_SETTLE.md` | A | evals/nav_instruct (bridge_v3_v4 rows), tests/test_mutation_panel_freshness.py — owner's choice: fix-product vs recorded re-run | `tests/test_mutation_panel_freshness.py` green in a clean worktree | OPEN |
| **C1** POI second oracle answers scene-relative place names on any scene | `C1_POI_ORACLE.md` | A | `navigation/grounder.py`, `navigation/pipeline.py:1136-1150` (leaf helper), `configs/navigation/default.yaml` | NAV-GEN-1 A0: crosswalk false arrivals 42 → 0, `target_id == 'crosswalk'` 90/90; frozen digests unchanged (E3) | OPEN |
| **C2** settle-observing arrival + one arrival authority | `C0_C2_ARRIVAL_SETTLE.md` | A | `simulation/headless_city.py:118-121, 722-732`; arrival authority seam (K0/N45 successor) | NAV-INT-1 authority disagreements 17/80 → ≤ 2/80; NAV-GEN-1 A0 strict re-scored under the settle predicate | OPEN |
| **C3** stall class: `navigation_no_progress` with the route still planned | `C3_STALL_CLASS.md` | A | `navigation/pipeline.py:4634-4656` → leaf module (pipeline.py is over the DEC-0 ceiling: net-negative only) | NAV-GEN-1 A0: 68 stalls attributed; non-POI stall rate halves at 0 collisions | OPEN |
| **C4** whisperer: plan-acceptance kind + `KIND_REROUTE` band decision | `C4_WHISPER_ACCEPT.md` | A (leaf) / B (install hook) | `realtime/whisperer.py` (leaf), `runtime.py` hook (wave B — owner's dirty diff) | MB-1 corpus: b1 "new goal acknowledged" produced from an executive receipt; reroute band test | OPEN |
| **C5** receipt-typed speech acts as a product leaf, flag OFF | `C5_SPEECH_ACTS.md` | A (leaf) / B (install) | new `realtime/speech_acts.py` + `realtime/narration_matcher.py`; `realtime/lane.py` install (wave B) | MB-2 arm T numbers reproduced through the product module; off-path byte-identical | OPEN |
| **C6** executive plan queue with lineage (queue / revise / keep; DMC-1 facts) | `C6_PLAN_QUEUE.md` | B | `brain/executive.py` (owner's dirty diff), `runtime.py` `_apply_goal_amend` | NAV-INT-1 tier: admission ≥ 0.9, "queue" no longer a re-issue, resume path ratio ≤ 1.1× | OPEN (after owner lands/discards the diff) |
| **C7** harness truth: LIT-1 false "reached", NAV-INT-1's two defects, results-schema hygiene | `C7_HARNESS_TRUTH.md` | A | `research/20260829/sim-loop-1/sim_loop.py`, `nav-interrupt-1/harness.py`, `nav-gen-attribution-1/analyze.py` (research only) | LIT-1 r1–r5 narrate the receipt's kind; NI-1 re-issue with cue stripped admits | OPEN |

Not cards (already in flight or notes): the acoustic runner negative-offset crash is fixed in the owner's uncommitted diff (`evals/companion/acoustic_loop_v1/run_acoustic_loop_v1.py` +8, lines 241-250) — verify when it lands; NAV-CORE's "planner 0.42 m" is pre-A2 (documentation note in C3).

## Standing constraints (verbatim from the integrator; on every card)

- Safety floors untouchable: `obstacle_stop_m 0.65`, `apply_reactive_safety`, `finalize_command`, `core/hard_stop.py`, the A3 latch, the A6 stop path.
- `config.py` is at the 1000-line DEC-0 ceiling — zero growth. `pipeline.py` is far over the ceiling and on the debt baseline — net-negative or leaf module only; the DEC-0 ratchet counts it.
- Zero `noqa` except a documented never-raises boundary in the module's idiom, grep-counted before the number is written.
- Frozen evidence E3: no frozen digest moves; any safety-relevant value change follows the attribution/re-freeze policy.
- $0 hosted unless the card states a cap through `hosted_budget.py`.
- Every pytest through `~/.cache/parcel-guard/pytest_guard.sh --label <card> …`; never `-n auto`; no `--pdb`; executors never run `ci_gate.py`; `TMPDIR` unset for unix-socket tests.
- Same-length source seeding needs the `__pycache__` drop.
- Owner facts: no robot hardware; `parcel_memory.sqlite3` never opened read-write (`PARCEL_MEMORY_PATH` → scratch); the owner's `:8080` / `:8765` / `/tmp/parcel_sim.sock` never touched; sims on unique sockets under `systemd-run --user --scope -p MemoryMax=12G`, killed by the card that started them.
- Shared tree: edit only your card's OWNS; git is read-only for executors (the integrator commits); foreign research folders (Sol's, DMC) never edited.
- Files in the OWNER's uncommitted diff (`runtime.py`, `brain/executive.py`, `gateway/*`, `bridge/*`, `control/*`, `navigation/grid_planner.py`, docs/, configs/robot.go2_edu_plus.yaml, prompts/) are NOT touched in wave A; a card that needs them ships a leaf module + a one-line install hook and says so.

## Working agreement

1. Executor reads the card, then the cited verdict sections, then the code lines named — in that order — and writes `<CARD>_STATUS.md` in this folder *incrementally* (pre-flight → each acceptance row → close), quoting the acceptance bar verbatim beside the number.
2. Reproduce the defect first with the named instrument (the RED row), fix, re-run the same instrument (the GREEN row), and run the named regression subset through the guard. Report exact commands.
3. No criterion on a card moves. If an acceptance row is impossible, record why and do the closest faithful thing — the verifier decides.
4. Verifier (Fable) re-runs headline rows through the product caller, writes `AUDIT_<CARD>.md`; second lens as listed. Integrator runs `ci_gate.py --tier commit` once at close, in a clean worktree, and commits.

## Definition of done (wave A)

C1, C2 (+C0 decision recorded), C3, C7 landed with their GREEN rows reproduced by the verifier; C4/C5 leaf modules landed flag-OFF with byte-identical off-path tests; gate green except rows attributed to the owner's diff; `CODEBASE_INDEX.md` regenerated; unfinished items moved to `backlog/`.

## Integrator note (23:4x) — clean-worktree evidence caveat

`.parcel` is an editable install pointing at the repo's `src/`; a worktree with `.parcel` symlinked imports the DIRTY tree unless `PYTHONPATH=<wt>/src:<wt>` is pinned. Consequence: commit 704ba5c's message reports pytest rows from a "clean worktree" that actually imported the working tree's `src` — those rows (default-suite, release-parity, owner-store) are **not** clean-tree evidence; the file-based rows (ruff, hard-safety artifact) stand. The close gate for this wave runs with `PYTHONPATH` pinned (and `MUJOCO_GL=egl`), verified by `parcel_robot.__file__` before the run.

## Close-out items for the owner (accumulating; final at wave close)

1. **Re-freeze decision — frozen v4 minival digest `c172da37…` no longer reproduces at HEAD 704ba5c:** A2 clearance (since ≤ 08-24, `research/20260824/nav-quality/RESULTS.md` §1.4 — four rows incl. one lost success, object_relative A-00) + a379bf4 (D-15 `_inside_goal` suffix); `ci_gate` cannot see either because `evaluate_hard_safety` reads the ledger's pinned numbers and never re-derives the baseline. Wave A moves the minival by zero (`021b67ab…` with and without C1+C2+C3, isolated worktrees, PYTHONPATH pinned).
2. **Hard-safety consequence of the uncommitted 28-file diff:** `src/parcel_robot/navigation/grid_planner.py` routes so the reactive gate never binds on the five frozen mutation-panel episodes — `reactive_gate_disabled` becomes a SURVIVOR (parcel-fb, attributed by worktree with only that diff applied). Landing the diff requires BOTH withdrawing C0's declared disable AND re-choosing panel episodes on which the gate binds (E3). D-15 "arriving" by 0.4 mm under that diff is the same event as the gate never firing.
3. **Root cause under C0 (E3):** a `known_poi` whose class is a region must be judged by the region band, not the 1.5 m point radius (point-goal system vs region scorer; D-15 is the demo block's crosswalk behaviour ×16). Moves frozen demo rows → owner's decision (backlog/BLOCKED entry by the C0/C2 executor).
4. **Wave B is blocked on the dirty diff:** C4/C5 install hooks (`runtime.py:3625`, `lane.py:1832`), C6 plan queue (`brain/executive.py`, `runtime.py`), and the `_lexical`/as_dict TURN-1 re-pin all wait for the owner to land or discard `runtime.py`, `brain/executive.py`, `gateway/*`, `bridge/*`, `control/*`, `grid_planner.py`, docs/, prompts/.
5. **Owner's choice still open on C0:** fix-product vs recorded re-run — the default (recorded re-run, disable declared) was applied because (a) was shown to require weakening the gate or moving a frozen digest.
6. **NAV-INT-1's bench legs:** 10/29 are `semantic_arrival_verification_failed` at dtg 0.0 — attributed per leg by the C0/C2 follow-up; if it is the same point-vs-region mismatch it is C1/C2 root cause, else it is N45/K0 (owner-gated).
7. **E3 item (integrator):** the mutation panel's reactive-gate evidence rests on one accidental knife-edge episode (D-15 is the only frozen panel episode on which the gate binds — 0 zeroed ticks on the other four at HEAD — and it binds only because it targets the POI point outside the polygon; its label flips between `tolerated_boundary` and `authority_disagreement` within the 5 cm epsilon on tick timing). The frozen set needs an episode where the gate binds BY DESIGN (an obstacle inside the ring on the only route), independent of D-15, before either `grid_planner.py` or a scene rule can land without blinding the gate mutant.

## Independent review on the record (Sol, 02:xx) — `research/20260829/CLAUDE_TASK2_REVIEW.md`, `NAV_GEN_INDEPENDENT_AUDIT.md`

Sol's decision: not mount-ready (nothing on this board claims it); C7 accepted as an evaluator correction; C4/C5 accepted as tested leaf prototypes; C6 absent (wave B by design). Sol's six blockers map onto this board as: (1) the panel blind to a disabled reactive gate — the a379bf4-era 4 → 2 kill-channel thinning + the owner's `grid_planner.py` survivor (close-out items 2 and 7); (2) C6 (wave B); (3) settle/receipt authority not composed in the product runtime — C2's wave-B install; (4) POI admission needs explicit per-navigator world/map identity (the published scene is process-scoped) — **added as a C1 wave-B row**; (5) stall recovery disabled by design (C3 flag OFF; enabling = re-freeze) and not tied to gate-clear evidence — C3's §2 attribution names the three refusing authorities; (6) C4/C5 not at the activation/audio boundary — their wave-B rows. NAV_GEN audit: agrees the clearance retune buys +2 pts and is not a safe proximity recommendation; asks for approach-selection vs A*-inflation vs gate telemetry — C3's leaf logs that per step.
8. **Ratchet debt from Sol's in-flight DUPLEX-GEN-1 files (integrator's finding):** `audio/voice_loop.py` 999 → 1364 (+450, new `SpeakerWriteAttempt`/`_SpeakerGeneration`), `voice/execution_narrative.py` and `brain/execution_narrative_bridge.py` (untracked), `brain/executive.py` 938 → 1495 — four > 100-line functions and one new > 1000-line module, unannounced on any board; whoever integrates DUPLEX-GEN-1 inherits them. Together with the owner's diff they make the DEC-0 rows red on the dirty tree (green in the isolated wave-A worktree).

## Close-gate findings (preliminary run, isolated wave-A worktree, `PYTHONPATH` pinned, 03:1x)

- **hard-safety PASS** (mutation panel fresh: C0's re-run reproduces in the clean tree); owner-store-isolation PASS (the 704ba5c "red" was the location artefact); assertion-evals, tier-coverage, unitree-assets, model-off PASS.
- **ruff RED — 73 new, all in committed research files** (`research/20260828/sim-plan-1/experiment.py`, Sol's `research/20260829/duplex-*`, `dynamic-social-progress-2`, …) — pre-existing at HEAD 704ba5c; no wave-A file is listed. Owner/Sol decision (or exclude `research/` from the ratchet's scope).
- **release-parity RED — wave A's:** C1-F1 edited `configs/navigation/cities/demo_pois.yaml` (the declared `scene_id`) and the packaged mirror was not regenerated. Fixed for the wave commit with `tools/sync_runtime_assets.py --write` in the isolated worktree: 2 files (`src/parcel_robot/runtime_assets/configs/navigation/cities/demo_pois.yaml`, `MANIFEST.json` — one entry + `tree_digest`); `--check` → "release parity OK: 106 packaged files match source". Staged into the wave commit from the worktree's bytes (`git hash-object` + `update-index`) so the owner's working copy of `MANIFEST.json` (their own `robot.go2_edu_plus.yaml` change) is not clobbered — **owner: re-run `--write` when landing your configs, it will carry both entries.**
- `test_release_parity.py::test_effective_config_is_equal_under_source_and_packaged_roots` stays RED in every worktree for a reason outside wave A: the probe subprocess runs without `PYTHONPATH`, imports `parcel_robot` from the editable install (the dirty repo), and its `prompts` digest is the OWNER's uncommitted `prompts/system/planner*.md` vs the packaged HEAD prompts (`navigation` and the other three components are equal). Green once the owner lands or discards those prompt edits and re-syncs.
- **default-suite: 10,914 passed, 7 failed** — 5 parity rows (above) and **2 `tests/test_runtime.py::test_social_affect_action_defers_until_navigation_finishes[…]` — wave A's** (they pass at HEAD): the runtime with a fake backend parses "navigate to the crosswalk" with no loaded world and the demo table now refuses (`no_scene`) — the old no-world contract again; **C1-F3 dispatched** (demo-scene fixture for those cases).
- DEC-0 ratchet (integrator's measurement): HEAD green; the dirty tree's 4 new oversized modules / 9 new long functions are the owner's diff + Sol's in-flight files — none is a wave-A file; C3's `pipeline.py` +11 does not register.
- Integrator's second reading (03:3x): the `test_effective_config…` row is a **location artefact by construction** — `tests/test_release_parity.py:180-189` launches the probe with `env={PATH, HOME}` (PYTHONPATH scrubbed) under `.parcel`'s python, whose site-packages `__editable__…pth` resolves the source arm to the dirty repo; it passes iff the main repo's prompts are clean. Backlog follow-up (not wave A): the probe should honour `PARCEL_ROOT` for the SOURCE arm too, or the test should pass `PYTHONPATH=REPO/src` through. Commit rule adopted: every wave-A path is staged from the gated worktree's bytes (`git hash-object -w` + `update-index --cacheinfo`) after comparing repo vs worktree hashes; `git diff --cached --stat` must list exactly the wave list. C1 close note: a runtime built with no world is a legitimate `no_scene` on the product path — the F3 fixture supplies the venue for the test, it does not paper over a product defect.

## LIVE COLLISION — 2026-08-30 04:1x EDT (for Sol's next board read)

Sol's `c0-panel-remediation` / `c0-panel-refreeze` jobs (Codex in the owner's Cursor; guard labels 00:23 and 00:42) are rewriting `scripts/mutation_panel.py` and the tracked `evals/nav_instruct/results/mutation_panel.json` in the MAIN repo while wave A commits from the gated worktree's bytes. **Sol's hunks (four intervention-witness rows in `PANEL_EPISODE_IDS`, `reactive_gate_coverage`, the fifth undisableable check `reactive_gate_exercised`) are NOT in the wave-A commit.** After the commit, the working tree's `mutation_panel.py` / `.json` will show Sol's version as modified against HEAD — expected; do not "fix" it. That remediation is close-out item 7 being built; for its landing the same E3 rule applies: the new panel's provenance must keep C0's re-run sentence, the a379bf4 attribution and the declared-disable clause until D-15 agrees, and each new row must be justified as "the gate binds by design" with per-episode intervention counts in the JSON — otherwise the fifth check is another artifact nobody can reproduce. Also on the record: the 08-28 03:xx wave and the uncommitted 28-file product diff share Sol's process lineage — foreign and unverified either way, treated the same.

**Commit decision on the collision (04:3x):** `tests/test_mutation_panel_freshness.py` in the tree now asserts Sol's fifth check by name (`reactive_gate_exercised`, lines 93/101/258/334), so C0-F1's test is coupled to Sol's in-flight `scripts/mutation_panel.py`; against C0's gated script it reads 2 failed / 9 passed. The wave commit therefore **withholds the three panel files** (`scripts/mutation_panel.py`, `evals/nav_instruct/results/mutation_panel.json`, `tests/test_mutation_panel_freshness.py`) — Sol's remediation lands them together with C0's provenance sentence under the E3 rule above — and keeps B32 (`backlog/BLOCKED.md`). Consequence, stated plainly: at HEAD + wave A the hard-safety row stays **STALE (a379bf4)** exactly as at HEAD; C0's recorded re-run and its direction-aware messages are evidence in `C0_C2_STATUS.md` / `AUDIT_C0_C2.md`, not committed artifacts. Reversible: if Sol's landing stalls, C0's gated bytes are in `~/.cache/parcel-0e/gate2-wt` and stage in one command.

**Ruling supersedes the withholding (04:5x, parcel-fb):** the pre-coupling C0-F1 triple exists (`~/.cache/parcel-0e/c2/c0f1/`: C0's `scripts/mutation_panel.py`, C0's panel `{agreement 4, authority_disagreement 1}`, and the F1 freshness test with the direction-aware messages and the declared-disable ratchet, NO `reactive_gate_exercised` symbol; "11 passed" on 08-29 23:38, before Sol's 00:23 session). **C0's bytes go IN** — that HEAD is hard-safety GREEN, the first on `main` since a379bf4, with the declared-disable ratchet in place — conditional on one re-verification in the gated worktree (running). Sol's remediation is then a pure increment on top (its hunks already sit on C0's edits). Two findings for the record: (1) **Sol edited a wave-A card's acceptance test inside the shared tree mid-wave** — the C0/C2 executor's "entirely this card's work" was wrong because its check (added `def test_` lines) could not see edits inside existing functions; the E3 landing rule for Sol's remediation must include: *the freshness test may assert `reactive_gate_exercised` only in the same commit that ships the check and its panel*. (2) After the wave commit, the working-tree diff on these three files is Sol's and expected — do not "fix" it.

**Working-agreement lesson (integrator, 05:1x):** `tools/codebase_index.py` indexes `git ls-files` — regenerate it AFTER the wave list is staged (or in a worktree where the new files are already tracked); the b87c079 index was generated before the wave's new modules were tracked in the gated worktree and read STALE at HEAD (fixed by the follow-up index commit). Integrator-verification row at b87c079 (clean worktree, PYTHONPATH pinned): freshness + ratchets + C1/C3 tests 81 passed; live `run_panel()` passed True, {agreement 4, authority_disagreement 1}, survivors [] — **`main` is hard-safety GREEN for the first time since a379bf4.**
