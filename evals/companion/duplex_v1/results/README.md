# DUPLEX_V1 results

Immutable JSON reports from
`python -m evals.companion.duplex_v1.run_duplex_v1`. One summary line per run
is appended to `ledger.jsonl`. Reports carry an explicit `does_not_prove`
list — scripted text turns are not live audio, and D0 frames observe behavior
rather than drive it.
