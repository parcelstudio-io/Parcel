"""PERSONAL_CONVO_V1 — text tier (Tier T) probe pack.

A multi-turn personal-conversation probe family on the proven
conversation_quality_v1 rails: the manifest sha-pins every fixture and
scorer, the runner refuses to run on edits, results are immutable and carry
``does_not_prove``. This package is the text-transcript tier only; the audio
tier (Tier A) and the local-judge calibration pack (PC-4) are separate,
owner-gated cards and are intentionally absent here.
"""
