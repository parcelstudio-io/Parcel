"""PERSONAL_CONVO_V1 — text tier (Tier T) probe pack.

A multi-turn personal-conversation probe family on the proven
conversation_quality_v1 rails: the manifest sha-pins every fixture, scorer,
and PC-4 judge/calibration artifact; the suite refuses to run on edits;
results are immutable and carry ``does_not_prove``. Tier A (audio e2e) and
the human-recorded corpus remain separate owner-gated cards.
"""
