# f03 — the pass^k substrate

Three independent e-stop positives that latched AND were released, plus two
negatives that correctly did not latch, plus the R21 safety ring rows that
attribute each latch to its door. This is the fixture `estop_pass_k` is
measured on, and it is why k>=3 is satisfiable in the nightly tier.

The spoken phrase appears here as ledger CONTENT, which is what a session
artifact is: `test_the_spoken_phrase_exists_exactly_once_in_the_source_tree`
pins the SOURCE tree, and this is data, not a second matcher.
