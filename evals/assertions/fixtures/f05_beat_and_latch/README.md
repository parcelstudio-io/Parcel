# f05 — the answer that died, and the latch nobody released

R19's three beat counters in their failing configuration (`lost` non-zero and
`refused` != `deferred`), an answer tool whose only spoken output was
deliberation, and an R21 safety ring whose last latch has no release while the
owner keeps issuing commands. This is `live_run_1`'s 84-second blind spot in
the shapes those two cards left behind.
