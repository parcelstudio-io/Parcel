# f01 — claims and provenance

A synthetic session carrying the SHAPES of the five 2026-08-20 owner-session
failures (F1-F5) plus the invalid-place acceptance and an ordering inversion.
Every string here is written for this fixture: the real owner transcripts are
deliberately not committed to this repo (see .gitignore), so the fixture
reproduces the STRUCTURE of each failure rather than its words.

Evidence is a STREAM (`events.jsonl`), so every finding here is a VERDICT.
