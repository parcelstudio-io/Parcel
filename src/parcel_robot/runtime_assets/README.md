# Packaged runtime assets

Generated by `tools/sync_runtime_assets.py`. Do not edit by hand: edit the
canonical repo-root `configs/`, `prompts/`, `maps/` and `fixtures/` trees and
re-run `tools/sync_runtime_assets.py --write`.

`MANIFEST.json` is the parity record; `--check` and the `release-parity` CI
gate fail if any packaged file drifts from its source.
MuJoCo meshes under `third_party/` are **not** packaged here.
