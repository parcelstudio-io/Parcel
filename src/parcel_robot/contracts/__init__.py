"""Pure versioned contracts for Parcel cross-track seams (K1).

No ROS, runtime, or agent wiring. Consumers import DTOs and freshness helpers
only; Opus owns later integration into ``runtime.py`` / ``agent.py``.
"""
