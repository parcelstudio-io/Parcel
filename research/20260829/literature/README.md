# literature/ — verified sweep for the nav + conversational fluidity wave (2026-08-29)

- `LITERATURE_REVIEW.md` — the synthesized, verdict-tagged review (written
  when the sweep lands). Tags: [S] supported, [P] partially supported with
  the verifier's correction, [n/v] finder-only.
- `sweep.json` — ten topics: findings (claim, URL, numbers, load-bearing
  flag), per-topic adversarial verification of load-bearing claims, the
  critic's gaps / strongest designs / risks / contradictions.
- `notes/<topic>.md` — each finder's full working note.

Topics: streaming/online VLN and embodied models; dialogue-navigation
datasets and models; dual-system speak-while-acting architectures (Hi Robot,
Helix, GR00T, ELLSA, DuplexSLA, RoboEgo); interruptible planning and plan
queues; robot self-narration and explanation; hosted realtime voice + local
planner patterns; nav / instruction / conversation benchmarks; Go2 sim-to-real
VLN and LLM-in-the-loop demos; streaming world-state representations;
liveness and attention during navigation. Nothing here is Parcel evidence.
