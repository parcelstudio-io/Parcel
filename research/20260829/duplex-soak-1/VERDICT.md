# DSOAK-1 verdict

## `SUPPORTED_PROCEDURAL_SOAK`

The retained DMC-1 artifact self-reports 12.050004 monotonic hours and 66,434
primary episodes, meets all 17 declared gates under post-run aggregate
consistency checks, records zero mismatches across 664 sampled replays, and
shows bounded RSS. The unsigned late-start observer partially corroborates the
last 80.377% of reported elapsed time, and a 32-case mutation campaign found the
post-hoc verifiers reject every exercised corruption. It does not strictly bind
process identity, source continuity, temporal ordering, or final-file handoff.

This supports one narrow statement: retained, self-reported and partially
corroborated evidence is consistent with the frozen desktop procedural
transaction program remaining operationally stable for the configured duration.

It does **not** support any of the following:

- learned-policy promotion—deterministic L0 remained stronger than A1;
- narration or terminal-claim truth—the frozen oracle is independently
  refuted;
- independent safety enforcement—the key zero counters are coupled structural
  invariants;
- camera/LiDAR perception, acoustic duplex, pedestrian prediction, locomotion,
  stopping distance, Go2/SDK2/ROS behavior, AGX Orin timing/thermals, or
  Starlink behavior; or
- observe-only, motors-disabled, or motion-enabled mount readiness.

The evidence strengthens confidence in typed task ledgers, bounded state,
rejection paths, and long-running replay plumbing. It leaves Model A
shadow-only, Model B non-authoritative, and autonomous physical motion
**NO-GO**. The next durability run should start an append-only, hash-chained,
externally witnessed log before the runner; retain successful replay digests;
freeze a truth-valid receipt/narration oracle; and record an independently
timestamped environment and source manifest. It must also bind exact process
identity and dependencies, sticky-latch source drift, and independently
regenerate retained episode/replay samples. The controlling machine scope is
[`INTERPRETATION.json`](INTERPRETATION.json).
