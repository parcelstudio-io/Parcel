# Fable cross-review — CODEX_RESEARCH_CROSS_REVIEW + PORTABLE_LIVING_DOG_HLD · 2026-08-24

Read in full (§0–§15) against the verdicts, the NAV-CORE results, and the
owner's decisions. The two Codex-run studies were **independently re-run by
this reviewer**: EVENT-BUDGET's model reproduces 14/14 numerical outputs
exactly (deterministic, seed 20260824); EMBODIMENT-KERNEL's audit output is
byte-identical on re-run (K3's nine `SimObservation` modules verified). Both
Codex "VERDICT.md" files are therefore ratified as verified results, with
the note that future studies keep the verdict role with the named verifier.

## Disposition: **ADOPT-WITH-AMENDMENTS — the HLD is the frozen architecture reference**

The HLD is the best system document this project has produced. It is
consistent with every measured finding (no-LLM tick; admission-dominated
cost; body-neutral intent with HOLD first-class; contract-over-localizer;
context-needing novelty; no self-initiated translation; desk/cloud as
optional accelerators), it converts H3's contact mechanism into a design law
(every action owns a terminal), it resolves the "continuously emitting
planner" language honestly (fixed-rate body/control lane refreshes setpoint
or HOLD; planners replan on change), and its Gate 0–8 sequence is
dependency-correct — including the correction I accept: **Follow after the
observation spine** (its own module imports `SimObservation`; the tracker
has no product installer). Reconciliation: `PORTABLE_LIVING_DOG_HLD.md` is
now the single architecture reference; `research/20260823/
MILESTONE1_DESIGN_FABLE.md` carries a supersession banner and remains the
historical M1 design record; the execution view is
`scrum/20260824/task_2/IMPLEMENTATION_PLAN.md` (v2, findings-bound, already
aligned to the Gates).

## Amendments (binding, folded into the plan; none reorders the Gates)
1. **NAV topology decider named.** Gate 5's "supervised point goal" is
   entered under the NAV-CORE rule: card A2 (NAV-GLUE) fixes the three glue
   defects and re-runs the frozen corpus unchanged; arm A ≥ 0.80 ⇒ retain
   the semantic ladder, else simplify to metric point-goals for M1.
   Delegation is refuted by measurement (all defects are Parcel glue; 8/8
   stalls inside Parcel's own brake rings).
2. **Discontinuity machinery is a build card, not prose.** The A4/A10 latch
   (caught 3/3 modelled kidnaps at 0.00 m) plus the relocalization
   runner-up margin (absent from the product — `ScanMatchLocalizer.
   _relocalize` keeps no second-best) and the journaled operator pose-reset
   transaction = card A3, BUILD_BLOCKER regardless of A2's outcome; the
   HLD's §8.3 health-monitor list is its acceptance basis.
3. **Arrival claims need calibrated confidence.** NAV-CORE R3 produced a
   real false arrival at p = 0.9922 from optimistic covariance (H7 L5
   reproduced as a wrong claim); §8.3's "mission code cannot declare arrival
   from a stale/uncertain pose" extends to: nor from an uncalibrated
   chance constraint — detector confirmation or a calibration floor is
   required (in A3).
4. **C8 transactional amend** (CLAUDE_RESPONSE A8) joins §5's lifecycle:
   goal amendment is a suspend-only, atomic-or-rolled-back transition with
   quiescence verified at the arbiter — pointer added so the HLD's
   lifecycle and the fix cannot drift apart.
5. **The $160+$40 envelope and `HostedCallGovernor`** (§10) are adopted
   verbatim into card A7; H1's measured rate-card correction (+336 %
   overcharge) is its calibration evidence; the governor's seeded-red
   (cap ⇒ refuse nonessential) is the acceptance row.
6. **Owner's floor wording.** §0/§10's degradation line is the owner's
   canned sentence; Follow is in M1 by owner decision (option (a)) and
   §12 Gate 6 is its enable path — the HLD already sequences it correctly.
7. **VOICE-GATE consolidations.** §7.1's pre-cloud rail + Codex's bars
   (no hosted bytes for TV/self-TTS/non-owner; ≤ 1 false opening/24 h;
   owner recall ≥ 95 %; STOP-LOCAL tail bars) are one pass rule in the
   VOICE-GATE v2 design; push-to-talk remains the honest default until the
   mounted bars pass.
8. **Verdict roles.** §13's "Fable reviews preregistration before results"
   is affirmed; Codex-authored studies are welcome and will be re-run for
   ratification exactly as done here.

## What this review does not settle
Everything §15 lists (ambient-vs-PTT, vendor answers, UWB, NX-vs-larger
compute, structured planner in M1 — owner input still wanted on whether
compound physical commands are binding for M1 — exploration, outdoor). The
HLD's rate targets and every physical number remain to be earned at the
Gates; adopting the document proves nothing physical.
