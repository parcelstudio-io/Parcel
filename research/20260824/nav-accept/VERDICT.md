# NAV-ACCEPT · VERDICT (Fable) · 2026-08-24

Basis: the executor's RESULTS.md + four result JSONs; my own frozen-manifest
check (21/21 OK, 0 failures, run by my hands); my own guard re-runs (label
`fable-navaccept-verify`): the reproduction control equals the frozen arm B
score dict, and my shipped corpus run reproduces the executor's — score
dicts EQUAL and all 60 rows field-identical (my artifacts:
`fable_control.json`, `fable_shipped.json`). `git diff -- src/` empty; both
DEC ratchets green; $0 hosted; zero product changes; the eye-read of
`nav_accept.py` confirms no clearance literal exists in it — every number is
read back out of a planner built by `DirectiveNavigator._create_navigator`,
the production owner's own method.

## Disposition: **CONFIRMED — the M1 nav acceptance bar is CLEARED, with
its scope and two priced costs carried loudly**

- **N1 = 1.000 (60/60) against ≥ 0.80**, N2 = 0, N3 = 0, on the frozen
  corpus, attributable to the commissioning alone (the control run is the
  attribution instrument, and it is exact). The mechanism is NAV-CORE fix 3
  working as designed: the planner now proposes only corridors the gate
  will drive, and arm B's 31 silent stalls disappear.
- **Scope, honestly**: a room-scale point-goal result. The 8×8 room's
  clearances (≥0.88 m everywhere, reachability audited at 0.95 m) are
  generous to the 1.0223 m isotropic inflation; A2's demo-city cost and
  Lane A's three reground STOPs already say the same number does not
  survive tighter geometry, and R3 proves it on a refuter: a 0.45 m box
  1.1 m ahead ⇒ unroutable ⇒ a SILENT 900-tick stall. **N4's 1.00 is
  vacuous** (empty denominator) and the silent-stall class is alive — the
  isotropic-vs-directional inflation follow-up (DOOR-1 H-2's successor,
  recorded at A2→A4) is now priced on a refuter, not just a demo test.
- **R4b is the row to carry to the owner**: with the shipped clearance the
  kidnap luck is SPENT — 3/3 false arrivals at 5.2 m, 0.988–0.998
  confidence, MAP HEALTHY. The harness's modelled latch holds it at 0.00 m
  3/3. One register correction, mine: the latch is NOT "a harness model
  with no product installer" — A3 productized it
  (`localization/discontinuity.py`) and A4 installed it
  (`localization/installer.py`, defaults unchanged) with the kidnap-onset
  row fired (2.64 m journalled) and A8 publishing `motion_latched`
  truthfully. What IS true: the HARNESS drives its own localizer stack, so
  these refuter rows exercise a model of the latch, and the product-path
  latch has never been driven against THIS aliased-twin scenario.
  **Consequence for the physical gate: the product latch is load-bearing
  before any physical point-goal session, and driving the product
  installer path against the R4b scenario belongs on the pre-physical
  checklist.**
- **Stage 2 — `require_relocalization_margin` stays OFF**: the delta is
  exactly zero because zero opportunities existed (0 rejects, 0 LOST, 0
  relocalizations across 29k instrumented ticks; the aliased kidnap
  registers at the twin and never enters LOST). The false-refusal risk is
  UNMEASURED, not disproven. The decision attaches to the normal-layout
  kidnap-onset scenario already recorded as owed (NAV-CORE v3 row).

Consequence recorded: the M1 nav acceptance row in the plan flips to
MEASURED-CLEARED (room-scale); the arrival distribution pressed against the
0.5 m band edge (median 0.477 m) is worth stating in the physical runbook —
the arm declares when probably-inside, not when close. Does not prove:
anything physical; everything NAV-CORE's own list names; the city.
