# Fable ratification — Codex research wave 2 · 2026-08-24

All four studies independently re-run by this verifier on this host:

| study | re-run outcome | ratification |
|---|---|---|
| safe-preference-adaptation | results.json identical after stripping timing fields | **RATIFIED** — regret −40.26 % but feedback/quiet-user/drift bars fail; implicit learning stays shadow-only; never authority/travel/safety bounds |
| conversation-opportunity | identical except `latency/*_ms` wall-clock fields | **RATIFIED** — the gate mechanism stands; 14/17 false-owner and 9/9 malformed cases speaking means NO enablement before commissioned owner/consent evidence + a typed fail-closed contract |
| spatiotemporal-noticing | completes only with a SHORT work-dir (AF_UNIX limit); collection is a LIVE perception daemon ⇒ stochastic (338 vs 339 detections served; fused-AUC medians 0.99824 vs 0.99834); every GATE field identical (`all_required_criteria_met: false`, S4 true, S5 exposure 0.35 min insufficient — both runs) | **RATIFIED-WITH-NOTE** — conclusions hold within run wobble; the wave doc's "arithmetic independently recomputed" is narrower than a re-run and this note supplies the re-run; the false-rate claim remains unsupported exactly as Codex disclosed |
| terminal-aware-continuous-action | identical except generated `place-<uuid>` target ids (11 fields, all ids; every numeric row equal) | **RATIFIED** — the refutation stands: stop-and-return increased contacts 319→323 and contact time 89.1→244.6 s; **safe-hold invariant + static-plus-dynamic receding-horizon replaces stop-and-return** as the initiated-leg terminal |

Plan consequences (applied): the H3-derived "every initiated leg gets a
return/stand-aside terminal" is AMENDED — the terminal is a **safe-hold
invariant chosen against live people-flow (receding-horizon)**, not a
scripted return leg, which the wave measured to be worse than the disease.
Proactive translation remains off; implicit preference learning remains
shadow-only; the conversation opportunity gate requires commissioned
owner/consent evidence before any enablement; noticing keeps separate
identity/place/change/social evidence with GAZE_VERIFY before memory or
speech. None of this promotes anything physical.

## HLD §16 ratification (2026-08-24, after Codex's amendment edit)
The four §16 amendments are RATIFIED as written — each restates a
consequence this note already ratified from the re-run evidence
(fail-closed social admission with `DROP_INVALID` before scoring;
structured world deltas with `GAZE_VERIFY` gating memory/speech/hosted;
explicit preferences enacted, implicit shadow-only behind a longitudinal
human study; the safe-hold invariant with typed terminals replacing
return-home + stop-only TTC, translation radius zero until promotion).
None reorders Gates 0–8; none claims physical readiness.
