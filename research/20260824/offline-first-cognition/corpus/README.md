# compound_v1.jsonl — authored by the designer (Fable) on 2026-08-24, BEFORE any run
60 items: seq ×10, and ×10, until ×10, return ×10 (in-grammar, gold skill
sequence with slots), out_of_grammar ×20 (gold = honest refusal or a
clarification; any produced plan counts as a miss). Skill names follow
voice/local_plans.py and the instructnav SearchEntity spec: NavigateTo(query,
relation), SearchEntity(query, budget_s, max_radius_m), FollowFormation
(distance_m, behind), Hold, Pose(name), OrbitOwner; `Until(fact, target)` is
the grammar's termination node — the executor maps it to the compiler's
success-condition on the preceding step (a step with fact=arrived/target=X or
owner_says/stop), not to a new skill. Scoring: O1 validity = compiler+validator
accept; O2 order = exact skill sequence and slot match after normalization;
O3 = refusal on out_of_grammar. Do not edit this file after the first run.
